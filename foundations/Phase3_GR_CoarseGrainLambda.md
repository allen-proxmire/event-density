# Phase-3 GR — Coarse-Graining `λ`: the Khronon as a Planck-Scale Goldstone, and the Naturalness Challenge

**Foundations analysis — coarse-grains the single decisive coupling `λ = c₂` (`α₁ ≈ −4λ`). Not a corpus edit, not a new primitive. The honest outcome: `λ` is identified as the kinetic weight of the khronon — the (pseudo-)Goldstone of the time-reparametrization symmetry that the arrow breaks — and the *generic structural expectation* from breaking that symmetry at the substrate (Planck) scale is `λ = O(1)`, hence `α₁ = O(1)`, in tension with the bound. Small `λ` is technically natural (radiatively stable, KM-I-protected) but ED supplies no mechanism to *generate* it small. So ED faces the standard naturalness challenge of Lorentz-violating gravity — shared with the whole class, not unique to it, and unresolved.**

The protection hunt settled that small `λ` is *allowed* (technically natural) and that `λ = 0` is *forbidden* (the khronon must propagate). This note asks what value the coarse-graining actually expects, and the news is sobering.

**Crank rail — and I weigh the unfavorable heuristic as honestly as I weighed the favorable ones.** This is a *structural expectation* (the Planck-scale-breaking → `O(1)` logic), not a computed coefficient; I label it as such and do not fake a number. But I do not soften it either: the discipline cuts both ways, and after over-reading favorable results, an unfavorable structural argument gets stated plainly.

---

## 1. What `λ` is — the khronon's kinetic weight

`λ = c₂` is the coefficient of `(∇·u)² = θ²`, the expansion-kinetic term of the khronon. Physically, the khronon is the field that becomes dynamical because the arrow (P11/P13) breaks **time-reparametrization invariance** — it is the **(pseudo-)Goldstone boson of broken time-diffeomorphisms** (exactly as in the EFT of inflation / ghost-condensate language, where the khronon is the Goldstone of broken time translations). `λ` is its **kinetic weight** — the analogue of a Goldstone decay constant, `λ ∼ f²/M_P²`, where `f` is the symmetry-breaking scale.

`λ = 0` ⟹ unbroken ⟹ pure GR (no khronon); `λ = O(1)` ⟹ maximal breaking. This is the same number as the protection hunt's, now read as a symmetry-breaking order parameter.

## 2. The coarse-graining expectation — and it is unfavorable

Coarse-graining integrates out the substrate's fast (Planck-scale) modes to get the khronon's effective action. The Goldstone kinetic weight `λ ∼ f²/M_P²` is set by the scale `f` at which time-reparametrization is broken. In ED:

> **The arrow breaks time-reparametrization at the *substrate* scale.** Commitment (P11) and the tick (P13) are Planck-scale processes — the preferred time is defined tick-by-tick at the fundamental scale. So `f ∼ M_P`, and `λ ∼ f²/M_P² = O(1)`.

This is the **standard, well-known result for Planck-scale Lorentz violation**: breaking Lorentz (or time-diff) invariance at the UV scale generically produces `O(1)` Lorentz violation in the infrared. Two independent heuristics agree:

1. **Goldstone scale:** the breaking is at `M_P` ⟹ `λ = O(1)`.
2. **Shared cone:** in the hyperbolic build the khronon rides the *same* P05 transport as the tensor modes, so its kinetic weight is comparable to the gravitational (Einstein–Hilbert) one ⟹ `c₂ = O(1)`.

Both point the same way: **`λ = O(1)`, hence `α₁ = −4λ = O(1)`, grossly in tension with `|α₁| ≲ 10⁻⁴`.** This is the *generic* expectation, and it is unfavorable.

## 3. Why technical naturalness does not rescue the generation

The protection hunt established that small `λ` is technically natural — radiatively stable, KM-I-protected. That is true and important, but it addresses **stability, not generation**:

- Technical naturalness says: *if* `λ` is small, radiative corrections keep it small (the GR limit's enhanced symmetry, with matter shielded by KM-I). 
- It does **not** say: `λ` *is* small. If the substrate *generates* `λ = O(1)` at the Planck scale (§2), technical naturalness keeps it `O(1)`, not small.

This is precisely the unresolved naturalness problem of Lorentz-violating gravity: **Planck-scale breaking generically gives `O(1)` IR violation, and stability arguments protect whatever value is generated — they do not make a `O(1)` generation small.** ED, breaking time-diff at the substrate scale via the arrow, sits squarely in this problem.

## 4. The honest verdict — leans unfavorable, the standard LV-gravity challenge

> **`λ` is identified as the khronon's Goldstone kinetic weight, set by the scale of time-reparametrization breaking. ED breaks it at the substrate (Planck) scale, so the generic structural expectation is `λ = O(1)`, giving `α₁ = O(1)` — in tension with the preferred-frame bound. Small `λ` (safe) is technically natural (radiatively stable, KM-I-protected) but ED provides no mechanism to *generate* it small. So ED faces the standard, unresolved naturalness challenge of Lorentz-violating gravity.**

Fairness, stated plainly so this is neither doom nor spin:

- **This is not unique to ED.** *Every* Planck-scale Lorentz/time-diff-violating gravity (Hořava, Einstein-aether, ghost condensate) faces exactly this — `O(1)` IR violation is the generic expectation, and the viable members live in a small-coupling corner whose smallness is technically natural but not dynamically explained. ED is in the same boat, not a worse one.
- **Viable members exist.** The small-`λ` corner is non-empty and observationally allowed; ED *could* be there. But the generic expectation is that it is not, and ED has not produced a generation mechanism.
- **It is the careful expectation, not a proof.** `λ`'s rigorous value still needs the full coarse-graining; the `f ∼ M_P` step is the standard heuristic, not a computed coefficient. ED could harbor a structural reason for `f ≪ M_P` (a suppression of the time-diff breaking) — none is found, but none is excluded.

So the honest lean, after genuinely attempting the coarse-graining, is **mildly-to-moderately unfavorable**: the structural expectation is `α₁ = O(1)` (tension), with safety requiring a small `λ` that is natural-but-unexplained. This corrects the neutral reading toward the generic expectation of the theory's class.

## 5. The whole front, finally mapped

Eleven steps in, the preferred-frame front is now completely characterized:

- ED's gravity is **khronometric** — Einstein's tensor sector + one physical khronon (the time-diff Goldstone). *(robust)*
- The single open falsification number is `α₁ ≈ −4λ`, `λ` = the khronon's kinetic weight = the time-reparametrization-breaking order parameter. *(robust)*
- `λ = 0` is forbidden (khronon must propagate); small `λ` is technically natural (stable, KM-I-protected); but the generic generation expectation from Planck-scale breaking is `λ = O(1)` → tension. *(the careful structural picture)*
- ED thus faces the standard naturalness challenge of Lorentz-violating gravity: **viable iff `λ ≲ 2.5×10⁻⁵`, which is natural-if-true but generically not generated.** *(the honest open state)*

This is a complete, honest, and *falsifiable* account: ED predicts a nonzero preferred-frame effect, generically `O(1)` but technically-naturally suppressible, and its viability rests on a single coupling whose smallness it does not yet explain.

## 6. Verdict

**The coarse-graining identifies `λ` as the khronon's Goldstone kinetic weight and expects, on the standard Planck-scale-breaking heuristic, `λ = O(1)` — so `α₁ = O(1)`, in tension with the bound, with safety requiring a technically-natural-but-unexplained small `λ`.** ED faces the generic, unresolved naturalness problem of Lorentz-violating gravity — shared with the whole class, with viable members existing in a small-coupling corner ED is not shown to occupy. The honest lean is mildly-to-moderately **unfavorable**, corrected toward the class's generic expectation. The rigorous `λ` still needs the full coarse-graining (a research computation, not faked), and a substrate mechanism suppressing the time-diff breaking (`f ≪ M_P`) would change the verdict but is not found. **The front is fully mapped, falsifiable, and honestly leans against ED on the generic expectation — pending either the rigorous coarse-graining or a discovered suppression mechanism.**

## 7. Next — honest options

1. **Search for a suppression of the time-diff breaking** (`f ≪ M_P`) — is there an ED structural reason the arrow's gauge-breaking is *soft* (the khronon nearly pure gauge) despite P11 being full-strength? This is the one thing that would move the verdict back toward safety; it is the right question, and it is hard.
2. **The full coarse-graining of `λ`** — compute `f` rather than assume `f ∼ M_P`. Real research; the honest prior is now `O(1)` (unfavorable).
3. **Bank with the honest verdict** — ED's gravity is khronometric with one open, falsifiable number `α₁ ≈ −4λ`, generically `O(1)` (tension) but technically-naturally suppressible, viability resting on an unexplained small coupling. A complete and honest account of the front, including that it leans against the theory on present analysis.

---

*Coarse-graining `λ = c₂`. Identified as the khronon's kinetic weight = the (pseudo-)Goldstone decay constant of broken time-reparametrization, `λ ∼ f²/M_P²`. ED breaks time-diff at the SUBSTRATE (Planck) scale (commitment/tick are fundamental), so the generic structural expectation is `f ∼ M_P ⟹ λ = O(1) ⟹ α₁ = O(1)`, in tension with `10⁻⁴` (two heuristics agree: Goldstone scale; khronon shares the P05 cone). Technical naturalness (protection hunt) gives STABILITY of small `λ`, NOT generation — Planck-scale breaking generically generates `O(1)`. So ED faces the standard, unresolved naturalness challenge of Lorentz-violating gravity (shared with Horava/aether/ghost-condensate; viable members exist in a small-coupling corner ED isn't shown to occupy). Honest lean: mildly-to-moderately UNFAVORABLE, corrected toward the class's generic expectation. Rigorous `λ` needs the full coarse-graining (not faked); a substrate mechanism with `f ≪ M_P` would rescue it but is not found. Front fully mapped, falsifiable, leans against ED on present analysis. No corpus edits, no new primitives; Einstein not derived; the number not fabricated, the unfavorable expectation not softened.*
