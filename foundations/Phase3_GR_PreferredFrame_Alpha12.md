# Phase-3 GR — The Preferred-Frame Front (`α₁, α₂`): How Far Structure Reaches, and Where the Number Begins

**Foundations analysis — the GR-II falsification front. Not a corpus edit, not a new primitive. This does NOT compute `α₁, α₂`; it establishes the structural suppression ED's derived results impose and scopes exactly what the number-computation requires.**
GR-II named the preferred-frame PPN parameters `α₁, α₂` as the theory's falsification target — `F`-dependent, deferred. The hyperbolic build localized them to the foliation knob; the `ε`-derivation fixed that knob (`ε = 0`). This note asks how far the *structure* now constrains `α₁, α₂`, and — crucially — states honestly where structure stops and a genuine PPN number-crunch begins.
**Crank rail (load-bearing here):** do **not** fabricate PPN values. A strong structural *lean* toward suppression is a lean, not a computed number; the front stays formally **open** until the numbers exist and are compared to bounds. Say so.

---

## 1. The front, precisely

ED's gravity carries a **preferred frame** — the cosmic rest frame (the khronon background = the Hubble flow; GR-II/KM). A system moving at velocity `w` relative to it can feel preferred-frame effects, parametrized in the PPN expansion by:

- **`α₁`** — `O(w/c)` effects (a dipole in the gravitational field's response to the system's motion); bound `|α₁| ≲ 10⁻⁴` (lunar laser ranging) to `~10⁻⁵`.
- **`α₂`** — `O(w/c)²` effects (anisotropy; e.g. the Sun's spin would precess away from the ecliptic over its lifetime); bound `|α₂| ≲ 10⁻⁷` (solar-spin alignment) to `~10⁻⁹` (pulsars).

These are the tightest constraints on any preferred-frame gravity, and they are what generically *execute* khronometric/Einstein-aether theories. ED must land inside them.

## 2. Four independent suppression mechanisms ED has derived

ED's arc has, without aiming at `α₁/α₂`, produced four structurally distinct reasons the preferred-frame sector is suppressed:

1. **Universal, not differential, Lorentz violation (GR-II).** Matter and gravity are one substrate with one cone, so the *leading* preferred-frame effect is a single global frame shared by all species — removable by one rescaling at leading order. `α₁, α₂` are therefore the **sub-leading** residual, not the leading effect. This alone is what lets ED survive the `10⁻²⁰` matter-sector bounds; it also pushes the gravity-sector `α₁, α₂` down a rung.
2. **`c_T = c` (single cone, GR-II).** The tensor sector is at light speed — no spin-2 preferred-frame dispersion.
3. **`c_s = c` (`ε = 0`, the `ε`-derivation).** The scalar (khronon) is *also* at light speed. In khronometric/aether gravity, `α₁, α₂` are functions of the couplings that are **most suppressed precisely in the limit where the mode speeds approach `c`** — ED is driven to that limit by derivation, not tuning. *(This is the known structural feature of the class; the exact functional form is the §4 computation, not fabricated here.)*
4. **Universal metric-only matter coupling (KM-I).** The khronon couples to matter **only through the metric** — there is no separate khronon–matter coupling (the feature that saved lensing). Direct khronon–matter couplings are a primary source of `α₁`; ED structurally lacks them.

Four independent directions, all pointing the same way. ED is not near the suppressed corner by accident — it is driven there by four separate derived results.

## 3. The ED-specific mechanism: the dissipative near-field

There is a fifth suppression that is **new and specific to ED**, and it comes from the `ε`-derivation. `α₁, α₂` are evaluated from the **near-field** of a source (the metric around the Sun). The `ε`-derivation showed the khronon, near active matter, is **overdamped** by the dissipative commitment-reserve (the same one-way reserve that forces `c_s = c`): there, the khronon mode does not propagate — it relaxes (the simulation: the reserve overdamps the khronon at strong coupling, `hyperbolic_modes.py`).

> **A khronon that is overdamped where matter is dense cannot build a coherent, frame-dependent near-field around a moving source.** The dissipative reserve that keeps `c_s = c` *also* damps the very near-field response that `α₁, α₂` measure. This is an ED-specific suppression beyond the generic khronometric corner — and it is the *same* P11 irreversibility once more.

So the arrow's third hat (the khronon at light speed, via the dissipative reserve) carries a corollary: the dissipative reserve suppresses the near-field preferred-frame response too.

## 4. The number boundary — stated honestly

**What structure establishes:** ED is driven, by *five* independent mechanisms (§2's four + §3's dissipative near-field), into the maximally-suppressed corner of preferred-frame parameter space. The **lean is strong**: `α₁, α₂` small, plausibly well inside the bounds.

**What structure does NOT establish — and is not faked here:** the *values*. "Small," "suppressed," "plausibly inside the bounds" are **structural leans, not computed numbers.** Producing `α₁, α₂` requires one of:

- (a) **map ED's couplings onto the khronometric PPN formulas** — express the rule's coefficients (`κ`, `D`, the band weights) as the Einstein-aether/khronometric `c_i`, then evaluate the Foster–Jacobson (2006) / Blas–Pujolàs–Sibiryakov expressions for `α₁(c_i), α₂(c_i)`. This needs `κ, D` **pinned** (still open — the minimal-model coefficients) and the standard PPN machinery applied without error; or
- (b) **a direct PPN expansion of `F`** — expand the dynamical-bandwidth rule to post-Newtonian order around a moving source and read off the `O(w/c)` and `O(w/c)²` field corrections.

Neither is performed here, and **neither is fabricated.** The front remains, in the strict sense, **open as a number** — even though the structural pressure is strongly toward safety.

## 5. Structural vs contingent

| Item | Verdict |
|---|---|
| ED has a preferred frame (cosmic rest frame) | **structural** (GR-II/KM) |
| leading preferred-frame effect removable (universal LV) | **structural** (GR-II) — `α₁,α₂` are sub-leading |
| `c_T = c`, `c_s = c` (both cones at light speed) | **derived** (GR-II; the `ε` note) — the most-suppressed corner |
| metric-only matter coupling (no direct khronon–matter term) | **structural** (KM-I) |
| dissipative near-field (overdamped khronon near matter) suppresses the PPN response | **structural + measured** (§3; the `ε`-note sim) |
| ED driven to the maximally-suppressed corner (5 mechanisms) | **established** — strong lean to PPN-safe |
| `α₁, α₂` **values** | **NOT computed** — needs pinned `κ,D` + FJ/BPS formulas, or a direct PPN expansion of `F` |
| the front as a falsifiable **number** | **formally open** (the lean is not the number) |

## 6. Verdict

**ED is structurally driven into the maximally-suppressed corner of the preferred-frame front — by five independent derived mechanisms — but the front is not yet closed as a number, and this note does not close it.** The five: universal (not differential) Lorentz violation makes `α₁, α₂` sub-leading (GR-II); `c_T = c` and `c_s = c` put both gravitational cones at light speed (GR-II; `ε`-derivation), the corner where the khronometric `α₁, α₂` are most suppressed; the metric-only matter coupling removes the direct khronon–matter term that sources `α₁` (KM-I); and — ED-specific — the dissipative reserve **overdamps the near-field khronon around matter**, suppressing the very response `α₁, α₂` measure (the arrow's irreversibility, once more). Five arrows, one direction: strong structural pressure toward PPN-safety.

**The honesty line, load-bearing.** None of this is the *number*. `α₁, α₂` are not computed here, and "suppressed / plausibly safe" is a structural lean, not a value to compare against `10⁻⁴` and `10⁻⁷`. The genuine computation needs the minimal-model coefficients `κ, D` **pinned** and either the Foster–Jacobson/BPS khronometric PPN formulas evaluated, or a direct post-Newtonian expansion of `F` — a real calculation, deliberately not faked. **The falsification front stays formally open; what has changed is that the structure now pushes hard toward safety, and the remaining computation is definite and small (no free foliation knob), not open-ended.**

## 7. Next (the actual number-crunch)

1. **Pin `κ, D`.** Express the minimal-model coefficients in terms of the P04 band weights and the `α=1` commitment-rate strength, removing the last free constants.
2. **Map to khronometric `c_i` and evaluate.** Identify ED's `(κ, D, ε=0)` with the Einstein-aether/khronometric couplings; evaluate the standard `α₁(c_i), α₂(c_i)` — *with the PPN machinery, not from memory*. Compare to `10⁻⁴, 10⁻⁷`.
3. **Cross-check by direct PPN expansion of `F`.** Post-Newtonian expansion of the dynamical rule around a moving source; read off the `O(w/c)`, `O(w/c)²` corrections; confirm (or correct) the mapping.
4. **Then, and only then,** the front closes — favorably or not.

---

*Analysis of the preferred-frame front. ED is driven into the maximally-suppressed corner by five independent derived mechanisms — universal (not differential) Lorentz violation making `α₁,α₂` sub-leading (GR-II); both gravitational cones at light speed (`c_T=c`, `c_s=c` via `ε=0`), the most-suppressed khronometric regime; metric-only matter coupling removing the direct khronon–matter `α₁` source (KM-I); and the ED-specific dissipative near-field, where the overdamped khronon around matter cannot build the frame-dependent response `α₁,α₂` measure (P11 again). Five arrows, one direction: strong pressure toward PPN-safety. **But the values are NOT computed and NOT fabricated** — "suppressed" is a structural lean, not a number; closing the front needs `κ,D` pinned plus the Foster–Jacobson/BPS formulas or a direct PPN expansion of `F`. The falsification front stays formally open; the remaining computation is now definite and foliation-knob-free. No corpus edits, no new primitives; Einstein not derived; the number deliberately not faked.*
