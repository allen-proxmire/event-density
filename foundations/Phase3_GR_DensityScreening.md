# Phase-3 GR — Density-Dependent Khronon Coupling: ED's Intrinsic Screening of `α₁`

**Foundations analysis — quantifies the density-of-becoming suppression: because gravity *is* event density and event density is non-uniform, the khronon's symmetry-breaking scale is the *local* event density, not the Planck scale, giving a density-dependent coupling `λ(ρ)` that realizes an intrinsic chameleon/symmetron-type screening of the preferred-frame parameter `α₁`. Not a corpus edit, not a new primitive. The honest outcome: this is a strong, well-precedented, structurally-grounded mechanism that leans the front *favorable* — `λ ≪ 1` by structure, not tuning — with the quantitative completion (the screening computation + consistency with GR-II) as the remaining work. Credit: the framing is Allen's (ED is a density-of-becoming theory) sharpened with Copilot; this note checks it against ED's structure and the screening literature.**

The sparsity note showed the `α₁ = O(1)` verdict was not forced (T-violation ≠ Lorentz-violation; V1-soft foliation). This note supplies the concrete, quantitative mechanism the earlier unfavorable analysis had assumed away.

**Crank rail — and I flag plainly that this is now a *favorable* result after unfavorable ones, exactly when to be careful.** So: I ground every step in ED's actual ontology, I name the standard mechanism it instantiates (chameleon/symmetron screening — not a new invention), and I state precisely what is structural (leans favorable) versus what still needs the screening computation (the magnitude and full test-suite).

---

## 1. The assumption I had silently made — and it is wrong for ED

The coarse-graining note concluded `λ = O(1)` from "the arrow breaks time-diff at the Planck scale." That argument assumed a **uniform background**: one fixed breaking scale `f ∼ M_P` everywhere. But ED's defining claim is that **gravity *is* event density** — the metric band is the density of committed events — and that density is **not uniform**:

- **Vacuum:** sparse commitments, low event density.
- **Near matter:** dense commitments, high event density (this is *why* gravity is strong there, why horizons form).

So there is no single `f`. The arrow breaks time-reparametrization **where events commit**, and commitments are distributed non-uniformly. The symmetry-breaking scale is **local**: `f(x) ∼ \sqrt{ρ_{\rm event}(x)}` (the local event-density scale), **not** `M_P`. This is the ingredient the uniform-background EFT heuristic structurally cannot see — and it is the heart of ED.

## 2. The consequence: a density-dependent coupling = intrinsic screening

If the khronon's breaking scale is the local event density, its coupling is **density-dependent**:

> **`λ(ρ) ∼ ρ_{\rm event}/ρ_{\rm Planck}`**  (schematic scaling) — large where event density is high (near matter), small where it is low (vacuum).

This is not a standard (constant-coupling) khronometric theory. It is a **screened** one — the exact structure of **chameleon / symmetron** modified gravity, where a field's coupling/mass depends on the local matter density, so the modification is *active cosmologically* but *suppressed in dense, well-tested environments*. ED produces this screening **intrinsically**, because its order parameter (event density) *is* the gravitational field — not by adding a screening potential by hand.

This is the decisive ED-specific point: **ED is not a Lorentz-violating field theory with a fixed breaking scale; it is a density-of-becoming theory whose Lorentz-violation scale is set by the local density of becoming.** The whole `O(1)` worry was an artifact of importing the fixed-scale assumption.

## 3. Why this suppresses the *observable* `α₁`

`α₁` is read from Solar-System tests (lunar laser ranging, planetary orbits) — i.e. from the metric in the **low-density orbital vacuum**, sourced by bodies. Two density-dependent effects both push `α₁` down, exactly as in chameleon screening:

- **In the orbital vacuum**, `ρ_{\rm event}` is far below Planck, so `λ` (the local preferred-frame coupling) is tiny.
- **Near/inside the dense bodies**, the khronon is stiff/heavy (large `λ`) and therefore *decoupled* — it does not get efficiently sourced into a long-range preferred-frame field, just as a chameleon is heavy and screened inside dense bodies.

Either way the **observable** preferred-frame effect in the Solar System is suppressed — while gravity (sourced by the dense bodies, the `κ/D = 8πG` ratio, density-*in*dependent) and cosmology (the dense early universe, the cosmic background) are unaffected. This is precisely how chameleon/symmetron theories pass Solar-System tests while remaining cosmologically active — and it is structurally how ED's MOND sector (KM-I, an acceleration/density-dependent regime) coexists with crisp local gravity.

## 4. The magnitude — why it is not marginal

The scaling `λ ∼ ρ_{\rm event}/ρ_{\rm Planck}` makes the suppression **enormous**, not borderline. *Any* local density is vastly sub-Planck: even nuclear density is `∼ 10⁻⁴⁷ ρ_{\rm Planck}`; the orbital vacuum is far lower still. So the structurally-expected `λ` in the relevant environment is many tens of orders below the `2.5×10⁻⁵` bound. The point is not "ED squeaks under the bound" but "**the bound is satisfied by `∼ 40+` orders of magnitude if the local-density scaling holds**" — `α₁` is safe *by structure*, with no tuning.

*(Honesty on the power law: the schematic `λ ∼ ρ/ρ_{\rm Planck}` is the linear estimate; the true exponent, prefactor, and which `ρ` enters (gravitational field-energy density, matter density, or the V5-kernel scale) need the screening computation. But the qualitative result — `f` is the local event-density scale, not `M_P`, so `λ ≪ 1` by many orders — is robust to the exponent: no sub-Planck density gives `λ = O(1)`.)*

## 5. Honest status — leans favorable, structurally grounded; what remains

- **Structural (leans favorable):** ED's ontology (gravity = event density, non-uniform) *forces* a density-dependent breaking scale `f(x) ∼ \sqrt{ρ_{\rm event}}`, hence a density-dependent `λ(ρ)`, hence intrinsic chameleon/symmetron-type screening. In every sub-Planck environment `λ ≪ 1`, so `α₁` is suppressed by structure. The earlier `α₁ = O(1)` verdict is **withdrawn** — it assumed a uniform background ED does not have.
- **Remaining (the computation):** (a) derive `λ(ρ)`'s actual form (exponent, prefactor, which density) from the substrate; (b) do the screening computation properly — the khronon profile around bodies and the resulting Solar-System `α₁`, against the *full* test suite (LLR, pulsars, not just the scaling); (c) check consistency — a density-dependent coupling *generalizes* GR-II's constant-`c_i` khronometric reading (a screened khronometric theory); verify it preserves `c_T = c` and the vacuum `c_s = c` (GW170817 is a vacuum measurement, so a vacuum-luminal khronon is consistent) and introduces no new pathology.
- **Precedent (why this is not wishful):** environment-dependent screening (chameleon, symmetron, Vainshtein) is the *standard, accepted* way modified-gravity theories evade Solar-System bounds while remaining cosmologically active. ED is not inventing a loophole; it is instantiating a known mechanism *intrinsically*, from its core ontology.

## 6. Verdict

**Restoring the fact that gravity *is* event density — non-uniform, sparse in vacuum, dense near matter — converts the preferred-frame coupling into a density-dependent `λ(ρ)` and gives ED an intrinsic chameleon/symmetron-type screening of `α₁`.** The breaking scale is the *local* event density, not `M_P`, so `λ ≪ 1` in every sub-Planck environment — and `α₁` is suppressed *by structure*, by many orders, with gravity and cosmology untouched. The `α₁ = O(1)` verdict was an artifact of a uniform-background assumption foreign to ED. **The front now leans favorable, on an ED-specific mechanism that is also a standard, accepted screening mechanism — not tuning, not wishful.** What remains is the genuine completion: derive `λ(ρ)`, do the screening computation against the full Solar-System test suite, and confirm the screened-khronometric theory is consistent (generalizing GR-II). This is a strong, well-grounded reopening toward safety; it is not yet the finished proof, and the quantitative adequacy must be shown, not assumed.

## 7. Next

1. **Derive `λ(ρ)`** — express the khronon kinetic weight as a function of local event density from the substrate (how the foliation-pinning stiffness scales with commitment density). The decisive structural computation.
2. **Screening computation** — khronon profile around the Sun/Earth; the actual Solar-System `α₁(ρ)`; compare to LLR/pulsar bounds across the full suite.
3. **Consistency** — confirm density-dependent couplings preserve `c_T = c`, vacuum `c_s = c`, and the GR-II structure (a screened khronometric theory), with no new pathology; this also hands GR-II the coupling-magnitude it lacked.

---

*Density-dependent khronon coupling = ED's intrinsic screening of `α₁`. Restoring ED's core (gravity IS event density, non-uniform: sparse vacuum, dense matter), the arrow breaks time-diff WHERE events commit, so the breaking scale is the LOCAL event density `f(x) ∼ √ρ_event`, NOT `M_P`. Hence a density-dependent `λ(ρ) ∼ ρ_event/ρ_Planck` — a chameleon/symmetron-type SCREENING (standard accepted mechanism), here INTRINSIC because the order parameter (event density) IS the gravitational field. In the low-density orbital vacuum where `α₁` is tested, `λ ≪ 1` by many orders (any sub-Planck density: even nuclear ~10⁻⁴⁷ Planck); near dense bodies the khronon is heavy/screened. So `α₁` is suppressed BY STRUCTURE (~40+ orders), gravity (`κ/D` ratio) and cosmology untouched. The `α₁=O(1)` verdict was an artifact of a uniform-background assumption ED does not have — withdrawn. Leans FAVORABLE on an ED-specific yet standard mechanism (not tuning). Remaining: derive `λ(ρ)`, do the screening computation vs the full Solar-System suite, confirm the screened-khronometric theory is consistent (generalizes GR-II, preserves `c_T=c`, vacuum `c_s=c`). A strong grounded reopening toward safety, not a finished proof. No corpus edits, no new primitives; Einstein not derived; no number fabricated, the favorable result properly bounded.*
