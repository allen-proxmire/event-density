# Gauge Program · Step 11 — The Single Hypercharge U(1) Is the One Global P09 Phase (Polarity Against the Arrow); Electroweak Mixing Is Higgs-Gated

**Foundations — closes `Gauge_01` open-piece 2 (the single shared U(1)). Result: the SM's single hypercharge `U(1)_Y` is the ONE global P09 phase — polarity measured against the ONE external flow (the commitment/arrow flow). It is one U(1), not one per family, because P09 is one primitive with one reference (the arrow). The per-family `U(1)_i` from `U(N)=SU(N)×U(1)` collapse into it (shared arrow-reference); the `SU(N)_i` stay per-family (internal channel mixings). So the gauge group is `∏_i SU(N_i) × U(1)_P09 = SU(3)_c × SU(2)_L × U(1)_Y` for families {3,2}. Hypercharge VALUES (per-particle Y) are the per-channel P09 phase-advance rates — inherited. The electroweak MIXING/breaking (Weinberg angle, `SU(2)_L × U(1)_Y → U(1)_EM`) is gated on the Higgs (substrate-Higgs, a wall). And it lands on the arrow-primitive convergence: hypercharge = the phase relative to the arrow.**

---

## 1. The question

`Gauge_01` derives `U(N)=SU(N)×U(1)` per channel-family of multiplicity N; the SM group would then be `∏_i [SU(N_i) × U(1)_i]` — a U(1) *per family*. But the SM has **one** U(1) (hypercharge). `Gauge_01` open-piece 2: reduce the several U(1)s to the single hypercharge.

## 2. The answer — P09 is one primitive with one reference

The U(1) factor of each `U(N)` is the family's **common P09 phase** (rotate all N channels together; Gauge_01 §2). The key substrate fact:

> **P09 polarity is one primitive: "the phase of the whole rule against the external flow" — and there is exactly ONE external flow, the commitment/arrow flow (P11).**

Every channel's phase is measured against the *same* reference (the one arrow-flow). So the per-family common phases are not independent — they are all the *one* global P09-vs-arrow phase. A global phase rotation (shift the arrow-reference) is therefore **one** U(1) symmetry, under which each channel advances by its own P09-charge. That single global U(1) is **hypercharge**:
- the per-family `U(1)_i` collapse into the one global `U(1)_P09` (they share the one reference);
- the `SU(N)_i` (traceless internal mixings) stay per-family (no shared reference — the internal channel-relabeling is family-specific).

$$\boxed{\;\text{SM gauge group} \;=\; \prod_i SU(N_i)\times U(1)_{\text{P09}} \;=\; SU(3)_c \times SU(2)_L \times U(1)_Y\;}$$

for stable families of size {3, 2} plus the global P09. Color and weak are the traceless mixings (SU(N)); hypercharge is the one global polarity-vs-flow phase.

**Hypercharge assignments (the values).** Different particles have different Y (quarks 1/6, leptons −1/2, Higgs 1/2, …). In ED these are the different **per-channel P09 phase-advance rates** under the one global U(1) — how fast each channel's polarity winds against the arrow. The *existence* of one U(1) is grounded (one P09, one arrow); the specific *charges* are inherited (as all values are). This matches the SM structure (one U(1)_Y, particle-dependent charges), not by fitting but because "one global phase, per-channel charges" is exactly what a single P09-U(1) gives.

## 3. Electroweak mixing is Higgs-gated

The observed low-energy group is `U(1)_EM`, from `SU(2)_L × U(1)_Y → U(1)_EM` via electroweak symmetry breaking (the Weinberg angle mixes `W³` and `B` into the photon + Z). This mixing/breaking is the **Higgs mechanism (EWSB)** — a *separate* piece, gated on the substrate-Higgs, which is a wall in ED (the substrate-Higgs/EWSB mechanism is named-not-derived; `Substrate_Higgs_Emergence`). So:
- **The unbroken group structure** `SU(3)×SU(2)×U(1)_Y` — grounded (§2).
- **The electroweak mixing/breaking** (Weinberg angle, `U(1)_EM`, W/Z masses) — Higgs-gated, open. The group is derived; the breaking is not.

## 4. The arrow-primitive convergence (flagged)

This lands on the session's recurring theme and AP's independent arrow-only-primitive idea. **Hypercharge = the phase relative to the arrow.** The one `U(1)_Y` is the U(1) of P09-against-the-commitment-flow — the arrow's own phase structure. So the reason there is a *single* hypercharge is that there is a *single* arrow to phase against. Combined with the arrow-as-keystone synthesis (the arrow is time + the pointer basis + the covering law + matter/antimatter + gravity's direction + information-preservation), this adds: **the arrow is also the reference of hypercharge (the one gauged U(1)).** If P09 (polarity) is itself the phase-structure of the arrow/commitment — as the arrow-only-primitive reading suggests — then hypercharge is not a separate primitive but the arrow's phase. A genuine convergence, worth developing: the electroweak U(1) may be the arrow wearing a phase.

## 5. Tier + net

**Grounded:** the single hypercharge `U(1)_Y` = the one global P09 phase (one primitive, one arrow-reference); the gauge-group structure `∏ SU(N_i) × U(1)_P09`. **Inherited:** the hypercharge charge values (per-channel P09 advance rates). **Open/gated:** the electroweak mixing/breaking (Weinberg angle, EWSB) — on the substrate-Higgs wall. **Net gauge-program state (now):** structure `SU(N)` + gauging (non-abelian, generic) + action `F²` + gap-mechanism + **single hypercharge `U(1)_Y`** all derived (structural); {1,2,3} a wall; couplings and the electroweak breaking inherited/Higgs-gated. The group `SU(3)×SU(2)×U(1)` is now structurally accounted for; only the *values* and the *breaking* remain inherited/open — the consistent ED pattern.
