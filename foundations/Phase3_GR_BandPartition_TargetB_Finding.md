# Phase-3 GR Keystone — Band-Partition Target B: λ_J Is Not Independently Lattice-Measurable; the Consistency Check Is an Analytic Identity

**Foundations finding — attempts Target B (`Phase3_GR_BandPartition_Scoping.md` §3–§4): independently *measure* `λ_J = κ_J/κ` (the directional response of a *moving* commitment) on the lattice, then run the consistency check `c₂ =? λ_J/(1−2λ_J)`. Honest result: **the clean independent lattice measurement of `λ_J` is NOT available** — for three concrete reasons (below). So `λ_J` is fixed *analytically* by the `c_s=c` family relation `λ_J = c₂/(1+2c₂)`, the consistency check is an **analytic identity** given the reconciliation + Target A's measured `c₂` (not an independent lattice confirmation), and this **corrects the scoping doc's optimism** (§3/§4 listed `λ_J` as a clean lattice output). The keystone's α₁ status is consolidated: it rests on four pieces, three already in hand. The genuinely artifact-free lattice item that *remains* is confirming `c_s=c` — the analytic input to the family relation — which a lattice can test without the boost-artifact contamination that defeats `λ_J`. No `λ_J` number is produced or faked.**

**Crank-rail: this is a "no" on a specific lattice measurement, reached by examining it, not by failing to try. I confirmed the load-bearing fact (the wake is O(1), §1) by running the sim, and I state plainly what the keystone does and does not now rest on.**

> **CORRECTION (2026-06-26, same day).** §4 below originally called confirming `c_s=c` "the real remaining lattice item." **That was wrong — `c_s=c` was already done**, both analytically (`Phase3_GR_DerivingEpsilon_KhrononSpeed.md`: ε=0 derived from P04+P11 minimality — P05 gives one speed `c`, the P11 reserve is dissipative not kinetic) **and by simulation** (`hyperbolic_modes.py`: the reserve damps/overdamps the khronon but does not shift its cone — the very dispersion/mode-speed measurement §4 proposed). So there is **no load-bearing remaining lattice item for α₁**: the safety chain (§3) is complete — α₂=0 verified, c_s=c derived+sim-confirmed, c₂∝sparsity measured (Target A), α₁≠0 confirmed — with only the absolute `ρ_event^vac` magnitude off-lattice EFT *by design*. §4 is corrected accordingly.

---

## 1. Why `λ_J` is not cleanly lattice-measurable (three reasons, confirmed)

**(i) The metric-band non-covariance wake is O(1) but pure gauge — not the physical λ_J.** `boost_noncovariance.py` (run): a moving source gives a dipolar wake `Δ = B − b_s` that is nonzero, linear in `w`, with `|Δ|/w ≈ 125` and dipole/`w ≈ 120` (these units) — a large, O(1) preferred-frame distortion (the covariance escape is closed; α₁ ≠ 0). **But** that wake is in the *scalar* band, and `directed_flux.py` measured the scalar flux **curl-free → pure gauge** → it sources no physical `g_{0i}`. The physical `λ_J` is in the *vector* (directed-flux) sector (the F2 rectification: `g_{0i} = λ_J w_i U` is the vector cross-term, not the scalar one).

**(ii) The physical λ_J (∝ sparsity) is swamped by the lattice's own O(1) boost-breaking artifact.** The reconciliation gives `λ_J = c₁₄ = c₂/(1+2c₂) ≈ c₂ ≈ sparsity` — i.e. `λ_J ∝ ρ_event/ρ_Planck`, vanishingly small. A lattice has its *own* preferred frame (the discretization rest frame), which breaks boost at O(1) — exactly the `|Δ|/w ≈ 125` scale of (i). A signal `~10⁻⁹³` cannot be read out from under an O(1) artifact. The physical preferred-frame coupling and the lattice discretization artifact are the same *kind* of object (a frame-dependent directional response), so they cannot be separated on the lattice.

**(iii) Modeling the directional response *is* the boost-covariance choice — the physics question itself.** "How strongly a moving commitment imparts directional bandwidth" (`κ_J`) is precisely the question of whether the foliation-tied commitment couples to the source's motion covariantly or not. *That choice determines `λ_J`.* Any lattice implementation encodes the assumption: build it boost-covariant → `λ_J` = the covariant value; build it reserve-mediated → `λ_J ∝ ρ_event` by construction. Both the *value* and the *scaling* come out as whatever was put in. There is no model-independent measurement to be had — which is exactly the "a careless model smuggles in the answer" hazard flagged when scoping this stage.

## 2. Consequence: the consistency check is an analytic identity, not a lattice test

The consistency relation `c₂ = λ_J/(1−2λ_J)` is algebraically equivalent to `λ_J = c₂/(1+2c₂)` — which is *how `λ_J` was derived* in the reconciliation (from `c_s=c`, ε=0). So checking it while computing `λ_J` from `c₂` via that very relation is **circular by construction**. It is an **analytic identity** that holds given (a) the `c_s=c` derivation and (b) Target A's measured `c₂` — *not* an independent lattice confirmation. The scoping doc's §4 "decisive consistency check" is therefore decisive only if `λ_J` is measured *independently* — which §1 shows it cannot be. This corrects §3/§4's framing.

## 3. What the keystone α₁ status actually rests on (consolidated)

With Target B's hoped-for independent `λ_J` measurement unavailable, the α₁ safety rests on four pieces — **three already in hand, one analytic**:

1. **`α₂ = 0`** — verified analytically against published khronometric formulas (arXiv:1105.5149, 1412.4828) + numerical check; a general feature of any luminal-cone khronometric theory. *Done.*
2. **`c_s = c` → the family relation `λ_J = c₂/(1+2c₂)`** — analytic, from ε=0 (the reserve is dissipative, not a separate kinetic cone). *Analytic; the one piece a lattice could still independently test (§4).*
3. **`c₂ ∝ sparsity` (`c₂ ∝ ρ_event/ρ_Planck`)** — **MEASURED from the band dynamics (Target A, today)**: `M_P²` density-independent, `f²` linear in `ρ_event` through the dilute regime. *Done.*
4. **Boost-non-covariance exists (α₁ ≠ 0, the covariance escape closed)** — confirmed (`boost_noncovariance.py`, §1; the dipolar wake). *Done.*

Together: `λ_J = c₂/(1+2c₂) ≈ c₂ ≈ sparsity`, so `α₁ = −4λ_J ≈ −4·sparsity`, safe by ≥70 orders (scaling). The **absolute magnitude stays an EFT estimate** (`ρ_event^vac`, off-lattice) — by construction, and not needed, since the *scaling* (piece 3) delivers the safety. The chain is: [α₂=0 done] + [c_s=c analytic → relation] + [c₂∝sparsity measured] + [α₁≠0 confirmed] ⟹ α₁ tiny. The missing "independent λ_J" was never going to come from a lattice.

## 4. There is no load-bearing remaining lattice item (c_s=c was already done)

I initially flagged confirming `c_s = c` as "the real remaining lattice item." **That was an error — it is already done**, and checking (the right move, per the question that prompted it) shows why:

- **Derived:** `Phase3_GR_DerivingEpsilon_KhrononSpeed.md` derives `ε = 0` (hence `c_s = c`) from P04+P11 minimality — P05 transport supplies one kinetic term at one speed `c` for the whole metric, and the P11 commitment-reserve is **dissipative, not kinetic** (monotone one-way drain → imaginary part of `ω`, damping; not the conservative `λθ²` that a second cone would need). No primitive supplies a foliation-kinetic term, so `ε = 0`.
- **Simulation-confirmed:** `hyperbolic_modes.py` ran exactly the dispersion/mode-speed measurement I proposed — adding the dissipative reserve term **damps and (at strong coupling) overdamps the khronon but does not move its cone**, cleanly distinguished from the `ε`-knob which shifts the cone. So the kinetic-vs-dissipative distinction is measured, not semantic.
- **Observationally consistent:** GW170817 sits in the `c_s → c` vacuum regime.

So the `c_s = c` input to the family relation `λ_J = c₂/(1+2c₂)` is **derived + sim-confirmed + observation-consistent**, not an open target. **There is no load-bearing remaining lattice item for α₁:** the safety chain (§3) is complete. The only thing not lattice-derived is the absolute `ρ_event^vac` magnitude — off-lattice (Planck/system EFT) **by construction**, and not needed because the scaling (Target A) already delivers ≥70-order safety.

## 5. Verdict

**Target B's hoped-for outcome — an independent lattice measurement of `λ_J` — is not available**, for three confirmed reasons: the metric-band non-covariance is O(1) but pure gauge (not λ_J); the physical λ_J (∝ sparsity) is swamped by the lattice's O(1) boost-breaking artifact; and modeling the directional response *is* the boost-covariance choice that determines the answer (smuggling). So the consistency check `c₂ =? λ_J/(1−2λ_J)` is an **analytic identity** given the reconciliation + Target A's `c₂`, not a lattice confirmation — **correcting the scoping doc**. The keystone α₁ rests on **[α₂=0 verified] + [c_s=c analytic relation] + [c₂∝sparsity measured (Target A)] + [α₁≠0 confirmed]**, with the absolute magnitude EFT/off-lattice. **And `c_s=c` is already derived + sim-confirmed (§4), so there is no load-bearing remaining lattice item** — the band-partition program delivers the `c₂` scaling (Target A) and *clarifies* that λ_J is not lattice-independent but is fixed by the (already-established) `c_s=c`. The α₁ front is, on the substrate's own terms, complete to the scaling level: every piece derived or measured, only the absolute `ρ_event^vac` magnitude an off-lattice EFT estimate by design. No λ_J faked.

---

*Phase-3 GR keystone, band-partition Target B. Attempted independent lattice measurement of λ_J=κ_J/κ + the consistency check. Finding: λ_J NOT cleanly lattice-measurable — (i) metric-band non-covariance wake is O(1) (Δ/w≈125, dipolar, run) but PURE GAUGE (directed_flux curl-free), not the physical λ_J; (ii) physical λ_J (vector sector, ∝sparsity~10⁻⁹³) swamped by the lattice's own O(1) boost artifact (same kind of object, inseparable); (iii) modeling the directional response IS the boost-covariance choice (smuggles value AND scaling). So the consistency check c₂=?λ_J/(1−2λ_J) is an ANALYTIC IDENTITY (λ_J=c₂/(1+2c₂) was derived FROM c₂ via c_s=c) — not a lattice test; corrects the scoping doc §3/§4. Keystone α₁ rests on: [α₂=0 verified] + [c_s=c → family relation, analytic] + [c₂∝sparsity MEASURED (Target A)] + [α₁≠0 confirmed (boost_noncovariance)]; magnitude EFT/off-lattice. Real remaining lattice item = confirm c_s=c (a dispersion/mode-speed measurement, artifact-free, unlike λ_J) to upgrade the family-relation input from analytic to measured. Honest "no" on the specific measurement, reached by examining it; no λ_J number produced or faked. Crank-rail: ran boost_noncovariance to confirm the O(1) wake (the load-bearing 'swamped' fact); stated plainly what the keystone does/doesn't rest on.*
