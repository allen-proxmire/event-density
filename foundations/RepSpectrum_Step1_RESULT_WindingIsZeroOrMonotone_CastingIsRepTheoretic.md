# Rep-Spectrum · Step 1 RESULT — the Commitment-Map Winding Is Identically Zero in the Parity-Clean Substrate (a Theorem) and Monotone-N When Broken: the Transport-Winding Does NOT Fix the Casting; the SM's "N=2 Special" Is Representation-Theoretic, Inherited — Not an ED Dynamical Fact

**Foundations — runs the computation `RepSpectrum_Step1_StabilityFormalism_*` teed up (`evaluation/ChiralGauge/rep_spectrum_casting_winding.py`): does the point-gap winding of the N-channel commitment map fix the chirality casting (N=2 chiral / N=3 vector)? Crank-rail ON, negative held to the positive bar. RESULT: NO — and the "no" is a clean theorem, not a fizzle. Parity-cleanness *forces* the winding to be **identically zero for all N** (the clean substrate is vector, full stop); a natural symmetry-breaking gives **winding = N** (monotone); the SM's *non-monotone* {vector, chiral, vector} matches neither. So the casting is **not** a transport-winding fact. It is the representation-theoretic fact `T4_12` already isolated (SU(2) fundamental pseudoreal, SU(N≥3) complex), which ED **inherits**. This closes the casting arc: form-forced / value-inherited, exactly as everywhere else in ED.**

## 1. What was computed
The formalism's casting criterion: chirality = point-gap winding of the non-Hermitian commitment map. Lifting the 1+1D scalar toy (`chiral_winding.py`) to `N` lanes, with the one constraint the corpus forces: **parity-cleanness** (no reflection primitive, `T4_09`) means parity — `(k → −k) × (lane reflection S: i ↦ N+1−i)` — is a symmetry, which *forces* the backward hop to mirror the forward hop:
`H(k) = e^{ik} A + e^{-ik} B`, parity-symmetric `⟺ B = S A S⁻¹`.
`A` (the retarded forward cross-channel hop = the U(N) link P05 + arrow P11) is the only free structure; `B` is then fixed. We compute the winding of `det H(k)` around `E=0` for `N=1..6`, over many random forward-hops `A`.

## 2. The result (numbers in, verdict out)
- **Part A (sanity):** reproduces the toy — Hermitian winding 0, one-way (arrow) winding ±1. ✓
- **Part B — parity-CLEAN (`B = S A S⁻¹`, the substrate's actual rule): winding = 0 for every `N=1..6`, every random `A`.** This is a **theorem**, not a numerical accident: `S H(k) S⁻¹ = H(−k)` ⟹ `det H(k) = det H(−k)` ⟹ `det H(k)` is **even in `k`** ⟹ as `k: 0→2π` the determinant path **retraces itself** ⟹ winding is **exactly 0**. Model-independent (holds for *any* `A`). **The parity-clean substrate is vector for all `N`.**
- **Part C — orientation-BROKEN (SSB, gate i-a; `B = εA`, `S`-symmetry broken): winding = `N`** for every `N` (each lane winds once). **Monotone in `N`**, chiral for all `N≥1`. *(Tier: illustrative — one natural breaking, not exhaustive. The robust result is Part B.)*

## 3. The verdict — the winding does not fix the casting
The SM casting to reproduce is **non-monotone**: `N=1` vector, `N=2` chiral, `N=3` vector. The computation gives:
- parity-clean → **0 for all `N`** (all vector),
- broken → **`N` for all `N`** (all chiral, monotone).

**Neither matches {vector, chiral, vector}.** So the point-gap winding of the transport — the mechanism step 1 proposed — **does not select the casting**. The formalism's explicitly-flagged *permissive risk* is realized, and decisively: in the parity-clean substrate the proposed invariant is **identically zero**, so it *cannot* distinguish any channel-count. This is the honest parallel to Gauge_09's gauge-multiplicity verdict, now for the matter chirality.

## 4. What this positively establishes (the closure)
The negative is informative and it *closes* the arc:
- **Parity-clean ⟹ vector, provably (Part B theorem).** This is a real result: it *proves* parity violation cannot come from the clean substrate's transport at all — it **must** come from the spontaneous orientation choice (SSB, gate i-a), exactly as the corpus always held (`MS-II §5`: chirality traces to the arrow's first commitment / SSB, not to a law). The computation converts "parity violation is spontaneous" from a stance into a theorem about the winding.
- **The casting's "N=2 special" is representation-theoretic, not dynamical.** Since neither the clean (0) nor the broken (monotone-N) winding is non-monotone, the SM's *non-monotone* pattern cannot be a transport-winding fact. It is the group fact `T4_12` isolated: the **SU(2) fundamental is pseudoreal** while **SU(N≥3) fundamentals are complex** — an "N=2 is unique" fact of representation theory. **ED inherits it**, the same way it inherits the gauge-group specifics ({1,2,3}, Gauge_09) and every value-layer constant.
- **Convergence (three independent routes, one answer).** Geometry (`T4_13`: permissive, capability-not-selection), and now arrow-stability (this: winding 0 or monotone), **both** deliver capability and **both** miss the "N=2 special" selection. That two independent ED mechanisms land in the same place is strong evidence the casting selection is genuinely *not* an ED dynamical/geometric output — it is inherited rep-theory. Consistent with scope §5 (discreteness/selection from topology, not stability).

## 5. Honest tier and the matter-sector status this leaves
- **Tier:** Part B is a **theorem** (robust, model-independent); Part C is **illustrative**; the attribution of the casting to pseudoreality is the **surviving candidate** (matches `T4_12`), not a fresh proof. The overall claim — *the transport-winding does not fix the casting* — is solid on the strength of the Part B theorem alone.
- **Matter-sector casting: RESOLVED AS INHERITED.** ED's own machinery builds the entire *stage* — the chirality operator `γ⁵ = arrow × orientation` (T4_10), its spontaneous arrow-tied direction (gates i/ii-a), the capability structure (T4_13), and the proof that the clean substrate is vector so parity violation is necessarily spontaneous (this note). What ED does **not** derive is the *assignment* (which channel-count is chiral): that is the pseudoreal-vs-complex rep fact, **inherited**. This is the standard ED verdict — **form-forced, value-inherited** — now established for the last open piece of the matter sector, by computation rather than assertion.
- **Distinctive prediction stands, sharpened by the theorem:** because the clean substrate is *provably* vector, ED's parity violation is *necessarily* a spontaneous, contingent, per-universe orientation choice tied to the arrow — hence correlated with the baryon sign (both faces of the one first-commitment), versus the SM's law-level fixed handedness. The Part B theorem is the firmest footing this prediction has had.

## 6. Net
Ran the computation the formalism demanded. The proposed casting mechanism (point-gap winding of the commitment map) is **provably zero in the parity-clean substrate** and **monotone-N when broken** — so it does **not** fix the SM casting. That is a negative, reported straight, and it is also the arc's **closure**: the casting is **representation-theoretic and inherited** (SU(2) pseudoreality), while everything ED *can* build (the operator, the spontaneity, the capability, the provable clean-vector baseline) stands. The matter sector is now, like the rest of ED, **form-complete and value-inherited** — with the one gain that "parity violation is spontaneous" is now a theorem about the winding, not a posture. No shortcut was hidden; the honest map is complete to this line.
