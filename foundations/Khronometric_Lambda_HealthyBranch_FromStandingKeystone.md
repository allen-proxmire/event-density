# The Khronometric λ, Resolved from the STANDING Keystone: ED Is Forced onto the Healthy Low Branch (λ < 1/3, λ ≠ 1 — not GR); the Exact Value Is F-Dependent (Inherited)

**Foundations — #5 of the open-derivations ledger (the exact khronometric λ = the scalar-mode-speed / K²-coefficient). DISCIPLINE: the prior λ work (`Lambda_HealthyLowBranch`) is in the SCRUBBED 2026-07-09 reinvention pile and is NOT used. This note re-derives the *structural* λ result from the STANDING keystone (`Paper_GR-II_KhronometricClass`) + standard khronometric theory alone, and tiers what stays open. Crank-rail ON. Result: the healthy-branch bound (λ < 1/3, λ ≠ 1) follows *structurally* from GR-II's khronometric class + the arrow breaking 4-diffeo; the *exact* λ in [0,1/3) is F-dependent — the dynamical-bandwidth-rule coefficient — hence inherited. So #5 closes into ED's standard form-forced / value-inherited pattern, on standing provenance.**

## 1. What #5 is, and its standing status
`Paper_GR-II` (standing) fixes ED's gravitational *class* as **khronometric** (Hořava infrared): 2 tensor + 1 scalar (the khronon = the arrow made geometric), because the arrow (P11/P13) lives in the *law* and reduces full 4-diffeomorphism invariance to foliation-preserving diffeomorphisms. GR-II is explicit that the khronon parameters are **F-dependent** (need the linearized dynamical-bandwidth rule's coefficients) and declares as **open work**: "compute the scalar-mode speed and settle the §6 over-constraint." That scalar-mode speed is set by the **K²-coefficient λ** of the khronometric action — exactly #5. So #5 is GR-II's own declared-open scalar-sector number, not a scrubbed-only artifact.

## 2. The structural result — ED is forced onto the healthy low branch (derived from the standing keystone)
The khronometric action carries the standard kinetic structure `K_ij K^ij − λ K²` (`K_ij` = extrinsic curvature of the foliation; GR is `λ = 1`). Decompose `K_ij` into traceless shear `σ_ij` and trace/expansion `K`:
$$ K_{ij}K^{ij} - \lambda K^2 = \sigma_{ij}\sigma^{ij} + \left(\tfrac{1}{3}-\lambda\right)K^2. $$
- **Tensor (shear) mode:** weight `1`, same as GR → tensor gravitational waves are GR-like for *any* λ. (Consistent with GR-II §6: `c_T = c`.)
- **Scalar (expansion/khronon) mode:** weight `(1/3 − λ)`. Ghost-free (positive energy) requires `(1/3 − λ) > 0`, i.e. **λ < 1/3**.

Now the load-bearing step, and it comes straight from the standing keystone:
> **GR's `λ = 1` gives weight `1/3 − 1 = −2/3 < 0` — the expansion mode is a negative-energy conformal ghost. Pure GR survives only because *full* 4-diffeomorphism invariance makes that conformal mode pure gauge (non-propagating). ED is khronometric (GR-II): the arrow breaks 4-diffeo, the gauge is only foliation-preserving, so the conformal/expansion mode *propagates* — it is the khronon. ED therefore cannot use GR's escape. To have a ghost-free propagating scalar, ED is *forced* onto the healthy branch `λ < 1/3`, and in particular `λ ≠ 1` (not pure GR).**

This is **derived** (structural): from GR-II's established khronometric class (the arrow breaks 4-diffeo → propagating khronon) + the standard `K`-decomposition + positive-energy. It uses nothing from the scrubbed docs. It is also a distinctive, falsifiable statement: **ED gravity sits on the healthy low Hořava branch, not at GR's `λ = 1`** — while remaining tensor-GR-like (`c_T = c`) and Lorentz-safe (GR-II §6–§7). The natural/minimal point is **λ = 0** (the action `K_ijK^ij` alone, shear and expansion both positive and isotropic); that value is a naturalness argument, not a pinned number (§3).

## 3. What stays open — the exact value is F-dependent (inherited)
The *exact* λ within `[0, 1/3)` — the precise scalar-mode speed `c_s²` and, through it, the deep-MOND interpolation constant — is set by the **coefficients of the linearized dynamical-bandwidth rule** `ḃ = F(ρ, ∇ρ, b)`, which GR-II states plainly are "not pinned … model-building, not derivation." That is ED's **value layer**. So the exact λ is **inherited**, exactly as `G`, `a₀`, `κ`, and `Λ` are (GR-II preamble 7). The lower edge and the natural-0 argument (a `P04`-conservation reading that compression is suppressed not enhanced, pushing `λ ∈ [0, 1/3)`) are structural *plausibility* for where in the branch it sits, not a derived decimal — tier them as account, not derived.

## 4. Verdict — #5 closes into form-forced / value-inherited (on standing provenance)
| piece | tier |
|---|---|
| ED is khronometric (2 tensor + 1 propagating scalar khronon) | **derived** (GR-II, standing) |
| tensor GW GR-like, `c_T = c` (λ-independent) | **derived** (GR-II §6) |
| **λ < 1/3 and λ ≠ 1 (healthy low branch, NOT GR)** | **derived (this note, from the standing keystone)** — the arrow's propagating khronon forbids GR's conformal-ghost escape |
| natural value λ = 0; the `[0, 1/3)` placement | **account** (naturalness / P04 reading), not a pinned value |
| **exact λ in [0,1/3)** (scalar-mode speed, MOND interpolation constant) | **inherited** — F-dependent (the dynamical-rule coefficients = the value layer) |

**So #5 is resolved:** the *last quantitative gravity number* is **form-forced at the structural level** (ED is on the healthy low branch, `λ < 1/3`, `λ ≠ 1` — a derived, distinctive, falsifiable departure from GR) and **value-inherited at the exact level** (the precise λ needs the F-rule coefficients). This is the identical pattern as the rest of ED gravity (khronometric *class* derived; couplings/constants inherited), and it is reached cleanly from the standing GR-II keystone + standard khronometric theory — the scrubbed `Lambda_HealthyLowBranch` reached the same structural conclusion, but this derivation does not rely on it, so the result now stands on clean provenance. Crank-rail: derived only the healthy-branch *bound* (which follows rigorously from the propagating-khronon fact GR-II already establishes), tiered the natural-0 as account and the exact value as inherited, and did not resurrect the scrubbed decimal or claim a pinned λ. **Gravity's last open quantitative piece is now classified: form-forced bound + inherited value.**
