# Continuum / Curvature-Emergence Arc — Exhaustion: ED's Native Continuum Is GEOMETRY (the Metric + Trajectories), Derived; the Smooth FIELD-PDEs (Maxwell, Diffusion) Are Coarse-Grained SHADOWS, by ED's Own Tested Result; Scale and Dimension Inherited/Walled

**Foundations — third structural arc (continuum / curvature-emergence), read to exhaustion the way T4 was, from the STANDING corpus only. Standing inputs: the 4 curvature-emergence docs (`CurvatureEmergence_MetricFromBandwidth_Foothold / ReachLaw_Derived_Holographic / LengthScale_IsThePlanckGrain / 3DIsotropy_LayerOneBallistic`), the closed gravity keystone (`Paper_GR-I..IV`, `Paper_KM-I/II`), the tested continuum result (`Paper_Continuum_KineticLatticeGas`), and the Maxwell edge (`Paper_ChargeAsTopology_B4` §7). The 2026-07-09 gravity-reinvention is SCRUBBED and is not used. Crank-rail ON. Verdict: the arc exhausts into ED's standard pattern, with one deep unifying characterization — ED natively emerges *geometry*, not *smooth fields*; the smooth continuum PDE is a coarse-grained shadow, which is ED's monist position and a *tested* result (for diffusion), not a gap.**

## 1. The deep unifying insight (the thing that organizes the whole arc)
The Continuum paper is a *tested, came-back-no* result with a positive core: the certified determinate substrate coarse-grains to a **kinetic lattice-gas** — ballistic worldlines scattering off ρ-disorder — **not** a diffusion PDE, and both routes to a smooth PDE (injected noise, emergent mixing) reach it *only by leaving ED* (deleting the Σ-selection, or a mixing ED's committal/trapping dynamics never produce). Its §9 states the ontology cleanly:

> **ED makes trajectories first; fields are what their density makes.** The substrate's native continuum is *geometric* (geodesic/kinetic worldlines, and the metric their density defines), not a smooth relaxed *field*. The smooth PDE layer is the coarse-grained *shadow* of the determinate substrate, not something it casts from within.

This one fact organizes the entire arc and predicts the pattern below: **the geometry side emerges natively (derived); the smooth-field side is a shadow (recovered by coarse-graining a thick/ensemble limit, not cast by the determinate dynamics).**

## 2. Thread-by-thread exhaustion
**A. The emergent METRIC (geometry) — DERIVED (native), 3D-unique.** Standing, measured: a metric genuinely emerges from the raw graph's bandwidth-connectivity (hop-distance, no assigned lengths, R²=1.000); a bandwidth dip ("mass") reads as *far* (the curvature signature). The connectivity law is **derived, not chosen**: reach `∝ b^{1/(d−1)}` is forced by the holographic channel-count (bandwidth = independent channels = boundary cut `∼ R^{d−1}`; measured 0.984 in 2D, 2.008 in 3D), and feeding it back gives `g∼1/b²` in 2D, **`g∼1/b` (GR-I) uniquely in 3D**. Isotropy emerges as a layer-2 object under coarse-graining (raw ballistic paths are faceted; a clean single exponent + exact isotropy live at layer-2). **Tier: derived (holographic fixed-point).** Inherited/walled: the length scale `ℓ_P` (P08 grain, no new scale introduced) and *which* dimension (`d=3` is a P06 wall — though independently 3-selected twice, by the holographic reach law here and by the linking argument).

**B. Gravity DYNAMICS — CLOSED (native).** The nonlinear/dynamical sector is the standing keystone `Paper_GR-I..IV` + `Paper_KM-I/II`: ED gravity is a healthy khronometric (preferred-frame = the arrow, P11) deformation of GR — GR-like tensor radiation + a MOND scalar (the khronon), `c_T=c` consistent with GW170817, MOND/BTFR from the khronon. **Derived (the closed #1 keystone).** *(All the 2026-07-09 λ/radiative/field-equation reinvention of this is scrubbed; the keystone is where the dynamics actually lives, and it is done.)* Couplings/`a₀`/`G` value-inherited, as everywhere.

**C. Smooth-FIELD continuum (diffusion) — a coarse-grained SHADOW (tested).** The certified substrate does **not** natively cast a diffusion PDE (diffusion excluded to R²≈0.001–0.004; ρ is a slaved deposition field; the missing ingredient is *scale separation*, not conservation — τ ≈ transport time, no fast/slow split; Σ-selection is a *trapping* operator, not a *collision* operator). The smooth PDE is reached only in a thick/over-damped or noise-injected limit that *deletes* ED's determinate selection. **So the smooth-field continuum is an emergent shadow, not a native output — and this is ED's monist position made empirical.** Tier: tested negative-with-positive-core (form/kinetic-structure yes; smooth-PDE-from-the-determinate-substrate no).

**D. Smooth-FIELD continuum (MAXWELL) — the same shadow; skeleton native, smooth field a coarse-graining edge (B4 §7).** B4 established the *geometry/topological* side natively: a quantized integer winding (`π₁(U(1))=ℤ`, measured) and an exact integral Gauss law (`2πw`, loop-independent) — the topological skeleton of charge, and the Maxwell action is **latent** in the coherence term (`cos²(Δφ/2) ≈ 1 − ¼(∇φ)² → ¼(∇φ)²`). What is *not* native is the *smooth determined Coulomb field*: orientation-blind Σ does not minimize the coherence action at the micro layer (the phase sector is Σ-blind), so the smooth Maxwell field is the **continuum limit of the lattice-gauge holonomies** — recovered only if the coarse-graining ensemble is coherence-weighted. This is exactly the diffusion situation (thread C) transposed to the gauge sector: **the topological skeleton + latent action are native/derived; the smooth field is a coarse-grained shadow.** Tier: skeleton derived (measured); smooth Maxwell = the standing B4 §7 analytic edge, structurally the same shadow as diffusion (the determinate substrate does not cast smooth fields; the Continuum result makes that a *tested* expectation, not a hope). *Not independently re-run here — flagged as structural, matching thread C.*

## 3. The exhaustion verdict
**The continuum/curvature arc exhausts into ED's standard pattern, with an ontological twist that is a feature, not a gap:**
- **Geometry (the metric, geodesic/kinetic trajectories): DERIVED natively** — the metric emerges from the length-less graph, `g∼1/b` forced holographically and uniquely in 3D; gravity's dynamics closed as khronometric+MOND.
- **The smooth-field continuum (diffusion, Maxwell): a coarse-grained SHADOW** — ED derives the *form/skeleton* (the kinetic equation, the topological charge skeleton, the latent Maxwell action, the lattice-gauge structure) *and* the *tested fact that the smooth PDE is a shadow the determinate substrate does not cast from within*. This is ED's monism (two description-scales, one reality), now empirical. The smooth determined field is recovered by coarse-graining a thick/ensemble limit — **not** a hole ED must fill, but the level ED says is derivative.
- **Value/structure inherited or walled:** the length scale `ℓ_P` (P08), `G`/`a₀`/couplings (value layer), and *which* dimension `d=3` (P06 wall, independently 3-selected twice).

So, in the user's "ED-derived or ED-inherited" test: geometry = **ED-derived**; smooth-field continuum = **ED-derived-as-emergent-shadow** (form + the shadow-status derived; the smooth field is the coarse-grained limit, by ED's design and tested for diffusion); scale/couplings/dimension = **inherited/walled**. Nothing in the arc is an un-classified open hole; the one live *technical* edge (B4 §7, does the specific DCGT measure select Maxwell) is analytic and its answer is set by the tested Continuum result (the determinate substrate does not cast smooth fields — the Maxwell field is the thick-limit shadow).

## 4. Interlock with T4 (both arcs resolve together)
T4's one inherited *structure* — the Cl(3,1) spinor via the acoustic-metric signature (Paper_017) — is **this arc's derived-native emergent metric**. So T4's "inherited acoustic metric" is not a free inheritance; it is supplied by the curvature-emergence arc (derived). The two arcs interlock: the metric this arc derives is the acoustic metric T4's spinor stands on. That tightens both — the matter sector's spinor rests on a *derived* (not postulated) emergent geometry.

## 5. Net
**Third arc: exhausted.** ED's native continuum is geometry (derived: the metric emerges, 3D-unique; gravity closed khronometric+MOND), and the smooth-field continuum (Maxwell, diffusion) is a coarse-grained shadow — ED derives its form/skeleton and, as a *tested* result, that it is a shadow the determinate substrate does not cast from within; the smooth field is the thick-limit coarse-graining, with scale and dimension inherited/walled. This is the same form-forced / value-inherited shape as gravity, gauge, and T4, plus the monist ontological reading that the smooth continuum is *supposed* to be emergent, not fundamental. Of the three GUT arcs, this one and T4 are now both exhausted; the remaining live rigor tail is **Solèr** (the QM-foundations keystone). Crank-rail: used only standing docs (scrubbed reinvention excluded); tiered Maxwell as structural-not-re-run; did not spin the "smooth continuum is a shadow" result as a derivation of Maxwell — it is ED's tested monist position, and the honest classification is emergent-shadow, not a closed field-theory derivation.
