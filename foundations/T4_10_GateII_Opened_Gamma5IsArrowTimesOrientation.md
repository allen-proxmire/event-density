# T4 · Step 10 — Gate (ii) Opened: γ⁵ = (the arrow) × (the spontaneous orientation) — the Chirality *Operator* Is Now ED-Grounded (ii-a ✓); the Asymmetric *Coupling* Is the Hard Core (ii-b)

**Foundations — T4 step 10. Opens gate (ii) of AP's orientation-parity route (`T4_08`/`T4_09`): does the emergent field content *couple* to the now-confirmed spontaneous global orientation, giving observable parity violation? Crank-rail ON. Splitting gate (ii) yields a clean, real sub-result (ii-a: the chirality operator γ⁵ is grounded as arrow × orientation) and isolates the genuine hard core (ii-b: the asymmetric coupling = the rep-spectrum alignment).**

## 1. Gate (ii), split
A background handedness (T4_09, confirmed to exist) is a bare convention until the field content *references* it. That reference is two things:
- **(ii-a)** a well-defined **chirality operator** — the L/R split of the spinor — tied to the orientation;
- **(ii-b)** an **asymmetric coupling** — the weak force actually treating L and R differently (the observable parity violation).

(ii-a) is necessary (you need a defined L/R at all); (ii-b) is the physics.

## 2. (ii-a) — CONFIRMED: the chirality operator is γ⁵ = (arrow) × (spatial orientation)
The chirality operator is, by definition, `γ⁵ = i γ⁰ γ¹ γ² γ³`. Read its two factors in ED terms:
- **`γ⁰` is the arrow.** ED's timelike direction is the commitment order — the arrow (P11 / retarded-only V1, T18). `γ⁰` *is* ED's process primitive.
- **`γ¹γ²γ³` is the spatial orientation.** The product of the three spatial gammas is the oriented spatial volume element — a choice of `γ¹γ²γ³` vs `γ¹γ³γ²` **is** the handedness of the 3-frame. This is exactly the **spontaneous global orientation T4_09 confirmed exists** (and under parity `γ¹γ²γ³ → −γ¹γ²γ³`, so it flips sign — the parity-odd object, as it must be).

> **So γ⁵ = i·(the arrow)·(the spontaneous spatial orientation).** Both factors are ED-native and now established: the arrow is the defining primitive; the orientation is the confirmed spontaneous global handedness. **ED therefore has a well-defined chirality operator, grounded in its own structure** — the first clean ED location of γ⁵.

**This reconciles the arc.** T4_04 found *no per-channel "screw"* carries chirality — correct, and now explained: **γ⁵ is not a per-channel feature at all; it is a *global* object** (the arrow × the global orientation). T4_04 was looking for γ⁵ in the wrong place (a local screw); T4_09 supplied the right one (a global orientation), and `γ⁰γ¹γ²γ³` assembles it. The apparent dead end (no local screw) and the confirmed orientation fit together here.

**And it gives the C/P link a concrete basis.** γ⁵ literally *contains both* the arrow (γ⁰ — the C-side ingredient, which sets matter/antimatter via R4) and the orientation (γ¹γ²γ³ — the P-side ingredient). So the "one lock, two faces" the pulled account wanted is grounded in one operator: **chirality = arrow combined with orientation**, so its two faces (C via the arrow, P via the orientation) are attributes of the single γ⁵, both set at the arrow's early action. Tier: structural (the *definition* of γ⁵ is standard; the ED content is that both factors are native and established).

## 3. (ii-b) — the hard core: the asymmetric *coupling* (the rep-spectrum alignment)
Having a defined γ⁵ (a clean L/R split) is *not yet* parity violation. Parity violation is the **dynamics treating L and R differently** — in the SM, the weak SU(2) couples to the **left doublet** but only a **right singlet**. Relabeling L↔R changes nothing unless the coupling is asymmetric. So (ii-b) is:

> **Does the emergent weak coupling reference γ⁵ asymmetrically — i.e., are the weak-SU(2) doublets specifically the L-chirality (γ⁵ = +1) components, and the singlets the R?**

This is the genuine hard core, and the orientation does **not** hand it over:
- The weak SU(2) comes from **N=2 channel multiplicity** (Gauge_01) — an *internal* structure.
- γ⁵ = arrow × orientation is a *spacetime/chirality* structure.
- For parity violation, the internal doublet must **align** with the chirality (doublet = L). *A priori these are independent* (internal multiplicity vs spacetime chirality), and no established ED result forces the alignment. **This alignment is the rep-spectrum question** (which channel-topology classes carry which chirality *and* which gauge rep) — the major open program the whole matter sector funnels into.

So gate (ii-b) = the channel-topology→representation-spectrum alignment, unchanged in difficulty, but now entered with a *defined chirality operator* (ii-a) rather than an undefined one.

## 4. What the orientation route DOES deliver (and the sharpened prediction)
Even without (ii-b) closed, gates (i)+(ii-a) deliver something real and distinctive:
- **The chirality operator is ED-grounded** (arrow × orientation), reconciling the no-local-screw finding with the confirmed global orientation.
- **The *direction* of parity violation is a spontaneous global choice, tied to the arrow.** Because γ⁵ = arrow × orientation, and the orientation is spontaneously chosen (gates i/b), the assignment of "left" is contingent (per-universe) — and it is *the same arrow* that sets matter/antimatter. So **the gauge-handedness and the matter/antimatter sign are correlated** (both are faces of the one γ⁵ = arrow × orientation, set at the arrow's early action) — a concrete grounding, at last, for the "handedness ↔ matter-sign" correlation the account always wanted, now with an operator behind it rather than an assumption.
- **Distinctive prediction, sharpened:** ED says parity violation, *if it exists* (ii-b), has a **spontaneous, contingent direction correlated with the baryon sign** — not a fundamental law with a fixed handedness. That is a falsifiable-in-principle departure from the SM's law-level, fixed-handedness parity violation.

## 5. Verdict and next sub-question
**Gate (ii) is opened, and (ii-a) is confirmed:** ED has a well-defined chirality operator, `γ⁵ = i·(arrow)·(spontaneous orientation)`, both factors native and established — the first clean ED grounding of γ⁵, reconciling T4_04 (no local screw) with T4_09 (global orientation). **(ii-b) — the asymmetric coupling — is the genuine hard core: the alignment of the internal weak doublet with the L-chirality, i.e. the channel-topology→rep-spectrum program.** The route's foundation is now complete through the *operator*; the remaining burden is entirely the *coupling asymmetry*.

**First tractable sub-question for (ii-b):** *is there any structural reason the N=2 channel-multiplicity (weak doublet, Gauge_01) would align with the γ⁵ = arrow×orientation chirality, rather than being independent?* Candidate to test (from the arc): the weak force is the **commitment-coupled / flavor-changing** force (it acts *at* commitments — channel *transitions*), while the strong force is **transport-coupled** (channel *persistence*, between commitments). Since γ⁵'s γ⁰ factor *is* the commitment/arrow, a **commitment-coupled** force would reference γ⁰ (hence γ⁵) by construction, while a transport-coupled force would not — a candidate reason the *weak* (commitment-coupled) force, specifically, is chiral and the strong (transport-coupled) force is vector. *(This is the `FirstArrival` §6.2 hook, previously refuted in a narrow flavor-conservation test; it is worth re-examining now that γ⁵ = arrow×orientation gives the commitment-coupling a concrete chirality operator to reference — the refutation targeted "chirality = flavor-change," not "commitment-coupling references γ⁰". Re-scope, do not assume.)* This is the sharp, decidable entry to (ii-b), and it is where to point next.
