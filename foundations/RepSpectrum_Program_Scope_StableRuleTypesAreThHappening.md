# The Rep-Spectrum Program, Scoped: the Rule-Type Taxonomy as a Dynamical Stable-Pattern Classification — Particles Are Stable Patterns of the Happening, Not Shapes on a Stage

**Foundations — scoping the matter sector's finish line: the channel-topology → representation-spectrum program (P07 §7.4 / channel.md §7.1), the single wall the whole matter sector (chirality assignment, generations, anomalies) funnels into (`T4_11` located it precisely). This is a *plan* for a major Phase-2/3 program, not a solution. Its organizing strategy is AP's steer (2026-07-10): "not a stage on which things happen — it is the happening." Crank-rail ON: the SM's particle content is one of physics' deepest unexplained facts; ED's contribution is a *different framing* that may be more tractable, not a promise of one.**

## 1. What the program IS
The rep-spectrum program is the **rule-type taxonomy** (channel.md §2, §7.1): *the complete structural classification of channel-compatible rule-types the participation graph supports.* A "rule-type" is the update rule a channel sustains; **a particle species is a rule-type**; the taxonomy is ED's *"why these particles and no others."* Delivered fully, it assigns each species its:
- **spin** (0 / ½ / 1),
- **gauge representation** (`SU(3)` × `SU(2)` × `U(1)_Y` — singlet/doublet/triplet + hypercharge),
- **chirality** (which `γ⁵` eigenspace — the located wall: why the 2-channel weak family couples to one, the 3-channel strong to both),
- **generation** (why three copies),
and reproduces the SM's content with nothing extra.

## 2. The organizing strategy — AP's steer, made a method
The corpus has mostly posed §7.4 *statically*: enumerate channel *topologies*, ask which stable ones map to which rep — channels as shapes. **AP's steer inverts this, and it is ED-native:**

> A rule-type is an *update rule* — a pattern of committing/participating, i.e. **a pattern of the happening.** So a particle is not a shape placed in a slot; it is a **stable pattern of commitment.** The taxonomy is therefore not a static-topology enumeration but a **dynamical stable-pattern classification: which patterns of the happening are stable, and what are their labels?**

This is not decoration — it changes the math and it is the arrow-keystone applied to matter (commitment is the only process; a particle is a *stable process*, not an object). Three consequences that make it the right attack:
- **It defines "which rule-types exist" as a stability problem on the Σ-landscape (P12)** — the stable rule-types are the **stable attractors / gapped fixed points of the commitment dynamics**. That is a dynamical-systems question (spectral gap / Lyapunov), not a topology census.
- **It is the *same* stability question already flagged everywhere** — channel.md §7.3 ("stability-condition formalism, open"), Gauge_08's internal-`d=3` (a *stability/orthogonality* bound), Gauge_04 §7.1 ("study the *stability dynamics* of directional participation-patterns"). The steer unifies them: **the taxonomy = the stable-attractor spectrum of the participation/commitment dynamics.**
- **It gives the labels a natural home** — spin, gauge rep, chirality, generation are *properties of a stable attractor*, read off *after* the attractor is found, not inputs.

## 3. The pieces already in hand (labels, not the taxonomy)
Each of these attaches a *label* to a stable rule-type once it exists; none is the taxonomy itself:
| label | ED handle | status |
|---|---|---|
| spin-½ (double cover) | channel tethering → `π₁(SO(3))=ℤ₂` (T4_03) | grounded (account) |
| gauge rep `SU(N)` | channel multiplicity `N` (Gauge_01), with `{1,2,3} ⟺` internal `d=3` (Gauge_08) | derived-conditional; `{1,2,3}` a wall |
| the chirality *operator* `γ⁵` | `= (arrow) × (spontaneous orientation)` (T4_10) | grounded (this session) |
| hypercharge `U(1)_Y` | the one global P09 phase (Gauge_11) | structural; values inherited |
| flavor / generation | channel-mixing; mass eigenstates = fixed points of propagation (channel.md §5.4) | a hook, not a count |
| vector vs chiral *sector* | abelian/V1 vs non-abelian/V5 (Gauge_05) | structural; does NOT pick weak-vs-strong |

**The unbuilt core is the taxonomy itself** — *which* stable rule-types exist — and the four label-assignments that only become well-posed once it does.

## 4. The sub-problems, ordered (the roadmap)
1. **The stability-condition formalism (the FOUNDATION) — WRITTEN 2026-07-10 (`RepSpectrum_Step1_StabilityFormalism_NonHermitianCommitmentAttractors.md`).** Result: a rule-type = an **attracting fixed point of the irreversible commitment map `T`** (non-invertible, P11); stable ⟺ spectral radius `ρ(DT) < 1` (spectral gap). It is a *third* stability notion, distinct from MS-II §6 (Welch → force families, done) and Gauge_09 (Hermitian coherence-Hessian → permissive). **Key move:** because `T` is irreversible, `DT` is **non-Hermitian**, and the **casting = the point-gap winding of `DT`** (nonzero ⟹ chiral, zero ⟹ vector; MS-II §4.2) — the one arrow-carrying object the permissive parity-even notions (Gauge_09, T4_13) structurally cannot see. **Discreteness stays with topology** (spin/charge/Welch/`γ⁵`), stability is the *filter* not the source. Concrete handoff: lift `chiral_winding.py` from 1+1D toy to the N-channel commitment map, compute point-gap winding vs `N`. Live risk (held to bar): `DT` may be permissive too (winding≠0 for both N=2,3) ⟹ ED underdetermines the casting. Formalism DEFINED + mechanism LOCATED; spectrum not yet computed. **COMPUTED 2026-07-10 (`RepSpectrum_Step1_RESULT_*`, `evaluation/ChiralGauge/rep_spectrum_casting_winding.py`) — the risk is REALIZED, and it CLOSES the arc.** Parity-cleanness forces `B = S A S⁻¹`, making `det H(k)` **even in `k`** ⟹ **winding ≡ 0 for all N (theorem): the clean substrate is VECTOR for every channel-count, so parity violation is *necessarily* SSB (now a theorem).** A natural breaking gives winding = N (monotone). The SM's *non-monotone* {vector, chiral, vector} matches neither ⟹ **the transport-winding does NOT fix the casting.** The casting is the **representation-theoretic** fact (SU(2) pseudoreal, `T4_12`), **inherited** — form-forced/value-inherited, same as everywhere. **Matter-sector casting RESOLVED-AS-INHERITED; residual open = anomaly cancellation.**
2. **The stable-rule-type spectrum (the CORE / taxonomy).** With (1), enumerate the stable attractors of the commitment/Σ dynamics — the ED particle inventory. Success criterion: a *finite, discrete* set matching the SM content (and its "no others").
3. **The chirality assignment (the located wall, `T4_11`).** As a stability question in the oriented space: does a stable **2-channel** attractor sit in a single `γ⁵ = arrow×orientation` eigenspace (chiral) while a **3-channel** attractor straddles both (vector)? This is now "which `γ⁵` eigenspace is a stable attractor compatible with, as a function of channel-count" — a dynamical question, not a static pairing. *(This is where the wall actually is; it becomes tractable only after (1)–(2).)*
   - **LEADING CANDIDATE — AP's lane-parity (2026-07-10).** Split the wall into *capability* vs *selection*, and the metaphor supplies the capability rule: the spatial reflection (the orientation factor of `γ⁵`) acts on the **channel set**, and a set of `N` lanes has a **central self-mirror lane iff `N` is odd**. Odd `N` (1 = EM singlet, 3 = strong) has a fixed central lane symmetric under reflection → **vector**; even `N` (2 = weak) pairs off with no fixed center → a genuine L/R exists → **chiral-capable**. This matches all three SM forces (1, 2, 3 → vector, chiral, vector). **RAN 2026-07-10 (`T4_12`):** the arithmetic core is exactly true (a fixed central self-mirror lane exists iff `N` is odd), and it correctly singles out **N=2 as the chiral non-abelian force** in ED-native terms (the channel-side shadow of the doublet's specialness) — so it is the **leading casting candidate for N=2**. But the run also railed the overreach: the **even/odd generalization does not survive** (the real invariant is "N=2 special / pseudoreal," not parity-of-`N`, so the N≥4 extrapolation is unsupported); the **abelian sector is a miss** (hypercharge `U(1)_Y` is chiral though "1 lane" — handle it separately as the global P09 phase, Gauge_11); and the group theory is **not** in tension but *permissive/silent* (vector-vs-chiral is contingent SM matter content the lane rule is trying to *derive*). The one genuinely open piece is the **bridge**: "no middle lane" currently *permits* but is not shown to *force* chiral. So: **candidate ED-native mechanism for N=2, not yet a derivation.** As before it delivers only the **capability**; the **selection** ("which handedness") is *supposed* to remain contingent (gate i-a's confirmed spontaneous orientation, ED's distinctive falsifiable claim: handedness ↔ baryon sign). **Bridge RAN 2026-07-10 (`T4_13`) — does NOT close, informatively.** Made the coupling into operators: parity-cleanness forces chiral (orientation-referencing) couplings into the `S`-odd sector of the gauge algebra, `dim = 2pq` with `(p,q)=(⌈N/2⌉,⌊N/2⌋)`. Result: **N=1 forced vector** (empty sector — pins EM); but **N=2 (dim 2) and N=3 (dim 4) are both chiral-*capable*** → geometry does **not** force N=3 vector. Chiral-*capable* ≠ chiral-*forced*: the geometry grants **capability** (matching rep theory's permissiveness exactly), the **selection** (which chiralities are populated → vector vs chiral) is **dynamical**. This *derives AP's capability/selection split at the operator level* and **validates the scope ordering: the casting has no geometric shortcut** — it is scope step 1 (stability dynamics: which stable fermion attractors populate one vs both `γ⁵` eigenspaces). Next brick = **step 1**, not more geometry.
4. **Generations (why three).** channel.md §5.4: flavor = channel-mixing, mass eigenstates = fixed points. Does the stable-attractor structure carry a **3-fold** degeneracy/replication? (A genuine, hard, SM-open question; scope it, do not promise it.)
5. **Hypercharge values + Yukawa/masses.** Read off the attractor's P09 phase-advance (Gauge_11) and its Σ-depth; largely inherited (value layer), per ED's form-forced/value-inherited pattern.

## 5. Honest tier, risks, and the concrete next move
- **Tier: a major Phase-2/3 program**, multi-session, no shortcut (every shortcut — spatial-dimension bound, channel-stability-for-`{1,2,3}`, the commitment-hook for chirality — has been tried and broken; those failures are *why* the honest path is the full taxonomy). This note scopes it; it does not solve it.
- **Risk (crank-rail):** the stable-attractor spectrum might come out *not* matching the SM (too many species, wrong counts) — which would be a real result (a falsifier), not a failure to hide. And "particles = stable attractors" is a *reframe with teeth* only if step (1) produces a genuine, non-tuned stability criterion; if the criterion has to be hand-fit to give the SM, the program has not delivered. Hold it to that bar.
- **What ED brings that standard physics lacks:** a *reason the inventory is finite and specific at all* — because the substrate supports only finitely many stable patterns of commitment (channel.md §1: "countable, usually small"). Standard physics *stipulates* the field content; ED's claim is that it is the *stable-attractor spectrum of the one process.* That is the distinctive prize, and it is exactly AP's "it is the happening."
- **Concrete next move: step (1) — the stability-condition formalism.** Write the explicit spectral-gap / Lyapunov criterion for a stable rule-type on the Σ-landscape (P12), the ED-native definition of "a stable pattern of the happening." It is self-contained, decidable, the foundation everything else needs, and the honest first brick of the matter sector's finish line.
