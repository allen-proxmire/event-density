# Phase-3 GR — The EFT Coarse-Graining: What Actually Sets `α₁`, and an Honest Downgrade

**Foundations analysis — coarse-grains the two-band substrate to the emergent khronometric effective action and asks what determines `α₁`. Not a corpus edit, not a new primitive. The honest outcome is a *clarification that cuts against the earlier optimism*: `α₁` is set by a conservative coupling the screening argument does not obviously touch, so the prior "leans safe" is downgraded to "genuinely open," with the remaining problem now sharply posed.**

The simulation route ended at "leans safe, magnitude needs an EFT." This note does the EFT framing. It does **not** compute the coupling (that is the hard transport-coefficient problem), but it determines *which* substrate quantity `α₁` depends on — and in doing so it exposes a subtlety the screening picture had papered over.

**Crank rail — maximally load-bearing here.** I have drifted optimistic twice this session and corrected twice; Allen flagged the whiplash. So this note states the *least* favorable honest reading and refuses to claim a safe lean it cannot support. If the careful analysis says "open," it says open.

---

## 1. The matching framework

Coarse-grain the substrate to its long-wavelength effective action. The emergent fields: the metric `g_{μν}` (from the conserved band `b`) and the khronon `u_μ` (the preferred foliation, from the arrow P11/P13). The symmetry is foliation-preserving diffeomorphisms ⟹ the EFT is **khronometric**, with action `R + c₁(∇u)² + c₂(∇·u)² + c₃(∇_μu_ν∇^νu^μ) + c₄ a_μa^μ` (`a^μ = u^ν∇_νu^μ`). Standard combinations: `α = c₁+c₄`, `β = c₁+c₃`, `λ = c₂`.

Established already: `c_T = c ⟹ β = 0`; `c_s = c ⟹ λ = α/(1−2α)`. The verified PPN result: `α₁ = 4(α−2β)/(β−1)`, so at `β = 0`,

> **`α₁ = −4α = −4 c₁₄`,  `c₁₄ ≡ c₁ + c₄`** — the **acceleration coupling**.

So everything is one number: `c₁₄`. The EFT question is what sets it.

## 2. `c₁₄` is a *conservative* coupling — and that is the catch

`c₁₄ a_μa^μ` is a term in the **conservative** part of the action: `a_μ = u^ν∇_νu_μ` is the bending of the preferred foliation, and near matter `a_i = ∂_i ln N = ½ ∂_i ln b`. So `c₁₄` measures how much the substrate **penalizes foliation bending** beyond what the metric (Einstein–Hilbert) sector already costs — a *static, conservative* property of the action, generated by the conserved metric/transport sector, **not** by the dissipative reserve.

Structurally, `c₁₄` is tied to **P13** (the homogeneous tick): the preferred foliation is the cosmic-time slicing, which the homogeneous tick wants flat; bending it near matter (where the lapse varies) costs action iff the tick's homogeneity is independently enforced. So `c₁₄ ~` the P13 foliation-bending stiffness. Generically `O(1)`; it is `0` only if foliation bending carries no cost beyond the metric — which is *not* established. **There is no symmetry forcing `c₁₄ = 0`** (that would be the GR point, and ED is khronometric, not GR).

## 3. The subtlety the screening argument missed

The "leans safe" verdict held that the dissipative reserve *screens* `α₁`. The EFT framing exposes the problem with that: **`α₁` is a quasi-static PPN parameter set by the conservative coupling `c₁₄`, and a dissipative reserve does not change a conservative coupling.** Two distinct things were being conflated:

- **A drain / mass term** `Γ·J` (what the `Γ`-note and `directed_flux.py` used) gives a *screened-Poisson* equation → suppresses the **static** field → would screen `α₁`.
- **Damping / friction** `γ·∂_t J` (what the `ε = 0` note actually derived — "the reserve enters as the imaginary part of `ω`, not the real part") affects **propagation**, not the static configuration.

These are physically different, and *which one P11 gives matters decisively.* The `ε = 0` derivation says the reserve is **damping** (imaginary `ω`). Damping does **not** statically screen — so on ED's own established characterization of the reserve, the screening argument for `α₁` **does not go through as stated.** The `Γ`-note implicitly switched the reserve from damping to a mass term to get static screening; that switch was not justified.

**The one honest nuance that keeps it open rather than closed-bad:** `α₁` lives at `O(w)`, and friction `γ∂_t J ~ γ\,w·∇J` also enters at `O(w)`. So the damping is *not negligible* for `α₁` — it contributes a genuine `O(w)` correction. But "contributes an `O(w)` correction" is **not** "screens to zero." Whether the dissipative correction cancels, suppresses, or merely shifts the conservative `−4c₁₄` is an actual computation, not a lean.

## 4. Honest verdict — downgrade to OPEN

Assembling the careful EFT picture:

> **`α₁ = −4 c₁₄ + (dissipative O(w) correction)`**, where `c₁₄` is a conservative foliation-bending coupling (generically `O(1)`, tied to P13, not symmetry-forced to zero) and the dissipative correction is a genuine but *uncomputed* `O(w)` effect (damping, not static screening).

- **Both pieces are uncomputed**, and neither is forced small by any symmetry.
- The earlier **"leans safe via screening" is downgraded**: it rested on treating the reserve as a static-screening mass term, whereas ED's own `ε = 0` result makes it damping, which does not statically screen. The screening intuition addresses the *radiative* khronon (correctly — clean GWs in vacuum), not the *static* `α₁`.
- So the honest standing is **genuinely OPEN, with no confident lean**: `α₁` could be `O(1)` (if `c₁₄` is `O(1)` and the dissipative correction does not cancel it — *tension with the bounds*), or small (if `c₁₄` is structurally small, or the dissipative `O(w)` correction cancels the conservative part). The EFT does not decide which, and I will not pretend it does.

This is the most careful reading of the front, and it is less comfortable than the sim-era "leans safe." That is the correct direction for the correction: the sims showed a *radiative* screening mechanism and I over-extended it to the *static* parameter. The EFT separates them.

## 5. What the EFT did settle (the genuine advance)

Even without the number, the framing is a real sharpening:

1. **`α₁` is exactly `−4c₁₄` + a dissipative `O(w)` correction** — two well-defined pieces, not a vague worry.
2. **`c₁₄` is identified**: the conservative foliation-bending stiffness, tied to **P13**'s enforcement of homogeneous time. *That* is the substrate quantity to compute — a concrete target.
3. **The damping/drain distinction is exposed**: the safety of `α₁` is **not** the same question as the cleanliness of the khronon GW (which the screening genuinely handles). Conflating them was the error.
4. **The remaining computation is now definite**: (a) coarse-grain to get `c₁₄` from the P13 + metric sector; (b) compute the `O(w)` dissipative (friction) correction to the PPN near-field; (c) sum and compare to `10⁻⁴`.

## 6. Verdict

**The EFT coarse-graining clarifies `α₁` to `−4c₁₄` plus a dissipative `O(w)` correction, identifies `c₁₄` as a conservative foliation-bending coupling tied to P13 (not symmetry-forced to zero), and — honestly — downgrades the front from "leans safe" to "genuinely open."** The downgrade is itself the result: the screening mechanism the sims demonstrated acts on the *radiative* khronon (real, gives clean vacuum GWs), but `α₁` is a *static* parameter set by a conservative coupling that damping does not screen — so the earlier safe lean over-extended the screening argument. ED's preferred-frame fate now rests on two uncomputed quantities — the conservative `c₁₄` and the `O(w)` dissipative correction — neither forced small. **No lean is honestly available; the front is open, and the number genuinely could come back either way.** The remaining work is a hard but now sharply-posed two-part computation; no number is faked.

## 7. Next

1. **Compute `c₁₄`** by coarse-graining the conserved-band + P13 sector — the foliation-bending stiffness. This is the decisive conservative number.
2. **Compute the `O(w)` dissipative correction** to the near-field from the reserve friction (`γ∂_t`), properly (not as a static mass term).
3. **Sum, compare to bounds.** Only then does the front close — and on this honest reading it could close either way.

---

*EFT coarse-graining of the preferred-frame front. Matching to the khronometric action gives `α₁ = −4c₁₄` (`β=0` from `c_T=c`) plus a dissipative `O(w)` correction. `c₁₄ = c₁+c₄` is the conservative acceleration/foliation-bending coupling — tied to P13's homogeneous-tick stiffness, generically `O(1)`, NOT symmetry-forced to zero. Key honest correction: the reserve is DAMPING (`γ∂_t`, imaginary `ω`, per the `ε=0` note), not a static-screening mass term — so it does NOT statically screen `α₁`; the `Γ`-note's screening of `α₁` implicitly (and unjustifiably) switched damping for a mass term. Damping still enters at `O(w)` (so it is not negligible, but "correction" ≠ "screened to zero"). Net: the sim-era "leans safe" is DOWNGRADED to "genuinely open" — the screening handles the radiative khronon (clean vacuum GWs), not the static `α₁`. `α₁` rests on two uncomputed, not-symmetry-small quantities (`c₁₄`, the `O(w)` correction); no lean is honest. Sharply-posed remaining computation: coarse-grain `c₁₄` (P13 sector) + the friction `O(w)` correction. No corpus edits, no new primitives; Einstein not derived; the number not faked, and not spun.*
