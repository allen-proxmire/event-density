# Khronon–MOND — Round 6: The Λ Reconciliation — One Boundary, Two Faces, One Constant

**Foundations consistency round — the last derivation-side item on the cosmology thread. Not a rule proposal, not a corpus edit, not a new primitive. Resolves the standing flag from KM Round 4 / Paper KM-I §7.4: the khronon vacuum term and the corpus's V1-backreaction Λ (Paper_038.5) — two faces to be reconciled.**
**Crank rails (held):** Paper_038.5 was read, not remembered — its actual mechanism and verdict tier are quoted; no value of Λ is derived; the matter-sector cosmological-constant problem is left exactly where 038.5 honestly left it; the reconciliation is tiered as *structurally indicated with a named open computation*, not proven.

---

## 1. The two faces, stated from their sources

**Face 1 — the V1 face (Paper_038.5; substrate level).** Λ is the **integrated curvature-induced vacuum backreaction of the V1 kernel over the cosmological boundary `R_H = c/H₀`**: a UV-*finite* vacuum-energy density (the V1 form factor cuts off at `ℓ_P`; no mode-tower divergence at substrate level) integrated over the boundary domain. Verdict M3 (form-IDENTIFIED + value-INHERITED + IC-INHERITED), with the *smallness* question reduced to Route-A closure plus Friedmann inheritance, landing the value at

$$
\rho_\Lambda = \tfrac{3}{8\pi}\,H_0^2 M_P^2 \;\sim\; \frac{H_0^2 c^2}{G}.
$$

038.5's own honesty stands and is not touched here: the constant vacuum piece is not removable; the naïve cutoff estimate failed by `∼10^{60}` and is preserved as a record; the matter-sector hierarchy is open.

**Face 2 — the khronon face (KM Rounds 4–5; EFT level).** The foliation EFT's deep-IR sector, evaluated at the cosmological point, contributes a constant term of order

$$
\rho_\Lambda \;\sim\; \frac{a_0^2 c^2}{G} \;\sim\; \frac{H_0^2 c^2}{G},
$$

because **the khronon's only scale is the cosmic rate** — the single-scale-consistency reading of the `Λ ∼ a₀²` coincidence. Form-tier; the `O(1)` constant `𝒲₀` undetermined.

**The first observation is now documentary, not suggestive: the two faces carry the *same* scale, `H₀²c²/G`, by each paper's own statement.**

## 2. Why the scale agreement is automatic — the shared anchor

The agreement is not a coincidence to be explained; it is a **common origin** to be recognized. Both faces are anchored to the *same object*:

- The V1 face's integration **domain** is the SCBU boundary `R_H = c/H₀` (038.5 cites Paper_028 / ED-SC 4.1 directly).
- The khronon's **background** is the Hubble flow, whose one scale is `H₀` — and KM Round 4 identified the khronon as the SCBU boundary's dynamical carrier ("one scale, four roles").

So the two faces are **two projections of the one SCBU boundary**: the *kernel* projection (V1's vacuum integral over it) and the *foliation* projection (the khronon's vacuum term at it). Finding both `∼ H₀²c²/G` is exactly what one boundary with two descriptions must produce. **Scale identity: automatic, and now explained rather than flagged.**

## 3. The level diagnosis — and the matching thesis

The faces are **not peers in one action**, and recognizing this dissolves the double-counting worry:

- The V1 face is a **substrate-level mechanism** — a kernel computation (vacuum density × boundary domain).
- The khronon face is an **EFT-level constant** — a coefficient (`𝒲₀`) in the low-energy foliation action.

In standard effective-field-theory logic, an EFT's constants are not *additional physics* alongside the underlying theory's — they are **matching coefficients** that *encode* it (as chiral perturbation theory's low-energy constants encode QCD dynamics rather than adding to them). Hence the reconciliation thesis:

> **One Λ, two descriptions.** The khronon's vacuum constant `𝒲₀ a₀²/16πG` is the **EFT encoding of the substrate-level V1 backreaction integral**: `𝒲₀` is fixed, in principle, by matching the foliation EFT to the substrate computation of Paper_038.5. There is no second contribution and no double-counting — the EFT term *is* the substrate term, written one level up.

**Status, tiered honestly:** *structurally indicated, not proven.* What is established: the level distinction (they cannot legitimately be added in one equation), the shared anchor (§2), and the scale identity (§1). What is **not** performed: the matching computation itself — deriving `𝒲₀` from the V1 integral. That computation is hereby the **named open derivation** of this thread (it is also, note, exactly the shape of work Route A already is on the 038.5 side: substrate-parameter content flowing upward into an inherited coefficient).

## 4. What each face contributes to the unified picture

The reconciliation is not redundancy-elimination; each face supplies what the other lacks:

| | V1 face (038.5) | Khronon face (KM) |
|---|---|---|
| **Level** | substrate (kernel) | EFT (foliation sector) |
| **Supplies** | the *mechanism* (finite vacuum integral) and the Route-A smallness path; M3 verdict machinery | the *carrier* (a dynamical field) and the *naturalness* of the foliation-sector size (only-scale argument) |
| **Λ appears as** | a computed boundary integral | the vacuum term of the same field that does MOND (deep IR) and the dark fluid (FRW) |

If the matching holds, the corpus's Λ-story becomes one sentence: **Λ is the SCBU boundary's vacuum imprint — computed at the substrate by the V1 kernel (038.5), carried in the effective theory by the khronon (KM) — the same constant, at two levels of description.** And the khronon's portfolio closes into a striking shape: *one field* carrying the arrow (GR-II), the Einstein sector at high accelerations (GR-I), the MOND sector below `a₀` (KM-I), the dark-fluid/dust dial on FRW (R5), and the Λ-order vacuum term (here) — every role anchored to the one scale `H₀`.

## 5. The clamps (what this round does NOT change)

1. **The matter-sector cosmological-constant problem is untouched.** Why matter zero-point energy does not gravitate at `M_P^4` is exactly where 038.5 left it (the discrete substrate's finite mode content + the Route-A reduction). The khronon face says only that the *foliation sector's own* vacuum term is naturally IR-sized — it does not police the matter sector. No smallness claim is added.
2. **No `O(1)` is derived** on either face (`𝒲₀`; 038.5's inherited value) — value-inheritance stands, `Θ_ED` think-don't-chase stands.
3. **Sign consistency is unchecked** — part of the named matching computation.
4. **The falsifiable edge:** if the matching computation is someday performed and the substrate-side V1 integral and the EFT-side `𝒲₀` (as constrained by data through the R5 dark-fluid sector) **disagree in sign or beyond `O(1)`**, the framework has a genuine internal tension — the reconciliation thesis is refutable, which is its virtue.

## 6. Verdict, and the thread's ledger after Round 6

**The two-faces flag is resolved into a one-Λ thesis with a named computation.** The scale identity is documentary (§1), automatic via the shared SCBU anchor (§2); the level diagnosis converts double-counting from a worry into a category error (§3); the matching computation (V1 integral → `𝒲₀`) is the remaining derivation-side task of the cosmology thread; and each face's contribution to the unified picture is identified (§4) without adding any smallness claim (§5).

**The cosmology thread's remaining ledger, post-R6:**
- **Derivation-side:** the Λ-matching computation (named here); the guarded primitives-origin of the deep-IR branch (unchanged).
- **Model-building-side:** the R5 dial — one function `𝒲(Θ)`, five filters, data as judge.
- **Observation-side:** wide binaries; the dynamical τ-sector; eventually the CMB verdict on the chosen dial member.

---

*Round-6 consistency analysis. Paper_038.5's Λ (the V1 vacuum integral over `R_H = c/H₀`; M3, value-inherited, read from source) and the khronon's vacuum term (`∼ a₀²c²/G`, form-tier) carry the same scale because they are two projections of the one SCBU boundary — the kernel projection and the foliation projection. They are not peers in one action: the khronon's `𝒲₀` is, by the matching thesis, the EFT encoding of the substrate-level V1 integral — one Λ, two descriptions, no double-counting. The matching computation is the named open derivation; the matter-sector hierarchy stays where 038.5 left it; no value, sign, or `O(1)` is derived. The khronon's portfolio now reads: arrow, Einstein sector, MOND sector, dark-fluid dial, and the Λ-order vacuum term — one field, one scale, five roles.*
