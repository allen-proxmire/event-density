# ED Gravity — Keystone Status: What Is Open, Precisely

**Allen Proxmire · June 2026 · a short, linkable status note for the ED gravity program**

This one-page note states, without hedging, exactly what remains open in the Event-Density (ED) emergent-gravity program and what would settle it. It is meant to be linked from the longer papers (the PhilPapers synthesis, GR-I/II/III/IV, KM-I/II) so a reader can see the live edge at a glance. Every line is tiered.

---

## The one-line state

**Everything structural in the gravity line is in place; the preferred-frame numbers `α₁, α₂` — long the one open falsification front — now resolve *favorably*. `α₂ = 0` exactly (the two light-speed cones force it; literature-verified), and `α₁ = −4λ_local` is density-suppressed because commitment is sparse (forced by ED's quantum sector), safe by ≥70 orders. ED's gravity is a *screened* khronometric theory: observationally General Relativity, distinct only at the Planck-density frontier.**

## What is settled (derived or measured)

- **Emergent Lorentzian metric** from the bandwidth field `g ∼ b⁻¹`, lapse `N² ∼ b`. *(derived.)*
- **Weak-field Einstein metric**, Schwarzschild relation `g₀₀ g_rr = −1`, and **factor-of-two light bending** (ray-tracing ratio 2.09 vs 2.000). *(derived + measured.)*
- **Einstein, not Nordström** — the branch is forced by the irreversibility of commitment. *(derived.)*
- **Gravitational class is khronometric** — Einstein's two tensor modes plus one physical scalar (the khronon), by Lovelock + mode count. *(structural.)*
- **Both gravitational cones at light speed:** `c_T = c` (single transport cone) and `c_s = c` (the khronon, from `ε = 0`, because the relevant sector is dissipative not kinetic). *(derived.)*
- **Dynamical rule** `ḃ = D∇²b − κρ`: steady state is the Newtonian field equation (corr. 0.999), `r_s ∝ M`, and a **spontaneously forming horizon** carrying **area-law entropy `S ∝ A`** and **Hawking scaling `κ ∝ 1/r_h`**. *(measured / derived.)*
- **Matter follows the timelike geodesics** of the emergent metric (massive eikonal, correct relativistic velocity dependence). *(measured.)*
- **Newton's `G` is derived downstream** (`G = c³ℓ_P²/ℏ`): form forced, value inherited through the single Planck scale — one fewer independent constant. *(derived form, inherited value.)*

## The preferred-frame number — closed favorably

**`α₁, α₂` — the preferred-frame PPN parameters.** Bounds: `|α₁| ≲ 10⁻⁴` (lunar laser ranging), `|α₂| ≲ 10⁻⁷`–`10⁻⁹` (solar-spin / pulsars). Once ED's two derived luminal cones (`c_T = c`, `c_s = c`) are imposed, both reduce to a single coupling — and that is where the front resolves.

- **`α₂ = 0`, exactly.** On ED's luminal family (`β = 0` from `c_T = c`, plus the `c_s = c` relation `λ = α/(1−2α)`), the published khronometric `α₂` formula vanishes *identically* — the same `c_s = c` condition that fixes the kinetic coupling nulls the `α₂` numerator (`−λ + α + 2αλ → 0`). Verified against the literature (Hořava-gravity PPN derivation arXiv:1105.5149 eq. 31; Blas–Sibiryakov arXiv:1412.4828) and checked numerically. This is a *general* feature of luminal khronometric gravity, independent of the sparsity argument below. **The tighter preferred-frame bound is therefore satisfied for free; only `α₁` survives, against the looser bound.** *(derived + literature-verified.)*

- **`α₁ = −4λ_local`, density-suppressed and safe by ≥70 orders.** `α₁` probes the *local* khronon stiffness `λ_local`. The two stiffnesses come from different primitives: the metric stiffness `M_P²` is the **always-on P02 sharing** (density-independent), the khronon stiffness `f²` is the **sparse P11 commitment-pinning** (density-dependent), so their ratio is the commitment sparsity, `λ_local = (k₁₁/s₀₂)·ρ_event/ρ_Planck ∼ ρ_event/ρ_Planck`. Commitment is *forced* sparse: dense commitment is the **quantum Zeno limit** (continuous measurement freezes all evolution → no quantum mechanics), and ED has a quantum sector. So in every sub-Planck environment `λ_local ≪ 1` — `α₁ ≲ 4×10⁻⁹³`, below the bound by ≥70 orders, robustly across every density identification. *(derived; the magnitude is an order-of-magnitude estimate, robust by ≥70 orders. Full argument: GR-IV.)*

- **The two couplings are one, and the conservative sector is already safe.** The `c_s = c` relation ties the preferred-frame coupling `c₁₄` to the kinetic coupling `c₂` by `c₁₄ = c₂/(1+2c₂) ≈ c₂`, and `c₂` *is* the sparsity — so `α₁ = −4c₁₄` is intrinsically tiny: nonzero (the khronon propagates) but `~10⁻⁹³`. The dissipative near-field (the overdamped khronon near matter) adds further suppression but is **not needed** — the conservative coupling is already the sparsity. *(reconciled; `foundations/Phase3_GR_Alpha2_Vanishes_Alpha1_Reconciled.md`.)*

- **MOND is untouched (a distinction, not a conflict).** `a₀ = cH₀/(2π)` is the khronon's *cosmological* face — a projection of the cosmic decoupling surface `R_H = c/H₀` (Paper 029), set by `H₀`, with no local stiffness in it. MOND is strongest in low-density outskirts, exactly where `λ_local` is *smallest* — so MOND was never a `λ_local` effect. ED has MOND *and* preferred-frame safety simultaneously. *(done; GR-IV §7.)*

**The residual is precision, not safety.** What remains is confirmation, not verdict: a band-level lattice run to confirm `c₂ = sparsity` and the `c₂ =? c₁₄/(1−2c₁₄)` consistency from the four-band dynamics (scoped turnkey in `foundations/Phase3_GR_BandPartition_Scoping.md`; the lattice gives the O(1) band-fractions + the scaling, *not* the absolute magnitude — and need not, since the scaling already delivers ≥70-order safety); a factor-of-2 normalization (`λ_J` vs the Will-form `2c₁₄`); and the `ρ_event^vac` magnitude (robust but unpinned). The front is closed favorably; these tighten it.

**How the front was reached (one line, since it nearly went the other way).** Under the naive effective-field-theory estimate — Lorentz violation generated against a *uniform* Planck-density background — `α₁` came out `O(1)` and the front looked fatal. It reversed when that assumption was replaced by ED's actual structure: the substrate's background is not Planck-dense but a *non-uniform density of becoming*, and the symmetry-breaking events are the rare acts of commitment, not every tick. The favorable result is offered with that history attached.

## Other honest debts (named, not gravity-proper, or hardening)

- **Cosmological-constant magnitude** — sign, structure, and `Λ ∼ H₀²` scaling derived; the magnitude is the cosmological-constant smallness problem, **open**.
- **Deep-MOND small-scale origin** — reproduced phenomenologically; the first-principles derivation is deliberately **unattempted (guarded)**.
- **Clusters + CMB**, and **quantum-information unitarity** (Page-curve *structure* reproduced; full unitarity open).
- **Strong-field / nonlinear solutions**; a fully-direct near-horizon-slope simulation (resolution-limited); a clean 3D `1/r` confirmation. **Hardening, not blocking.**
- **Band-level confirmation of the sparsity scaling** (`c₂ ∝ ρ_event/ρ_Planck`) — scoped, turnkey; a tightening of the already-favorable `α₁`, not a jeopardy.

## The honest standing

The theory could have lost, and the place it could lose was marked: `α₁, α₂`. That front is now **closed favorably** — `α₂ = 0` exactly (the luminal cones, literature-verified), and `α₁ = −4λ_local ≲ 4×10⁻⁹³` (sparse commitment, forced by ED's quantum sector), below the bound by ≥70 orders. Not a structural lean but a result on ED's own primitives. The Route-A/Route-B work that set this up is complete; what it leaves is a band-level *confirmation* of the sparsity scaling — a tightening, not a verdict. The live debts have moved off the falsification front onto the named items above (cosmological-constant magnitude, clusters/CMB, full unitarity, strong-field). The sharp front held.

---

*Companion to the ED gravity papers. The full derivations live in the foundations notes (`Phase3_GR_*` — including `Phase3_GR_Alpha2_Vanishes_Alpha1_Reconciled.md` and `Phase3_GR_BandPartition_Scoping.md` — and `KhrononMOND_*`) and the published papers GR-I/II/III/IV and KM-I/II; this note is only the live open/closed edge, kept short on purpose.*
