# SCBU from the Substrate-Evaluation Side — A Bridge Note

**Foundations bridge note. Not a corpus edit, not a verdict change, not a new primitive. Form-only.**
Connects the corpus's **SCBU** (Substrate-Cosmology Boundary Unification, `Paper_SCBU` + `ED-SC 4.x`) to this session's substrate-evaluation results (A1, A2, ED-10 horizons, ED-Information, the Round-2 bandwidth dynamics). The two have been describing **the same surface from opposite ends** — SCBU top-down (cosmological projections), the evaluation work bottom-up (the substrate mechanism). This note makes the identification exact and states honestly what it does and does **not** do.

---

## 1. The exact identification

SCBU's load-bearing definition (`Paper_SCBU`, Preamble item 7 + §3.1):

> *"Substrate-cosmology boundary = the substrate-level surface at `R_H = c/H₀` (Paper_028) beyond which cross-locus substrate influence cannot reach coherently … a structural property of the substrate participation graph under P11 commitment-irreversibility + P13 time homogeneity + the kernel-propagation rate c."*

That is, term for term, the object the bits-measurement program measured:

| SCBU (top-down) | Substrate-evaluation (bottom-up) |
|---|---|
| "cross-locus substrate influence cannot reach coherently" | **A1: controlled channel capacity = 0** across the surface |
| "structural property of the participation graph under P11" | **A2: a decoupling surface**, irreversibility-frozen |
| the surface where coherent bandwidth between loci vanishes | **`b → 0`** (ED-10 metric-degeneration locus) |
| `Γ_cross → 0` at the boundary (`Paper_039`, V5-saturated) | the same `b → 0`, in the cross-chain band |

**The SCBU boundary *is* the A1 capacity-zero determinability boundary = the `b → 0` surface — at cosmological scale.** This is not analogy; it is the same structural object, which is exactly why `Paper_047.5` already unifies BH / Rindler / cosmological / acoustic horizons as "projections of the same V5-saturated boundary."

## 2. What the bridge *adds*: a second formation mechanism

My ED-10 work derived horizon **formation** from one mechanism — the commitment-driven bandwidth drain (Round-2): a local runaway collapse to `b = 0`. SCBU supplies the **other** formation mechanism, the one ED-10 had as a *type* (two-way / cosmological) but not as a *process*:

- **Commitment-driven (local) collapse** → `b → 0` by reserve drain. Gives a **one-way** surface (P11 makes it directional) = **black-hole horizon**. (My A2 / ED-10 R2.)
- **Expansion-driven (cosmological) decoherence** → `b → 0` because the kernel-propagation rate `c` loses to the Hubble expansion rate `H₀`; coherent cross-locus bandwidth vanishes beyond `R_H = c/H₀`. Gives a **two-way** surface (symmetric, de Sitter) = **cosmological horizon**. (SCBU's `R_H`.)

So the two formation mechanisms map cleanly onto the two causal types my ED-10 taxonomy already separated — **one-way (commitment/BH) vs two-way (expansion/cosmological)** — both reaching the same `b → 0 = A1 capacity-zero` surface. ED-10 had the *types*; SCBU completes the *processes*. That is the genuinely new content of the bridge.

## 3. What the substrate-evaluation work gives SCBU

SCBU's central claim is a **postulate** — `P-Substrate-Cosmology-Unified`: that one substrate boundary object underlies the six projections. The evaluation work provides **independent substrate-side grounding for that postulate's core**: the boundary is a real, characterized substrate object, not just an inferred common anchor. Specifically it supplies, from first principles / measurement:

- **existence + capacity-zero** (A1: exactly-zero controlled transmission — the operational meaning of "influence cannot reach coherently");
- **a formation mechanism** (A2 / Round-2 bandwidth dynamics, admissible because it formalizes the commitment-reserve dynamics P04+P11 already declare);
- **causal-type classification** (one-way vs two-way → BH vs cosmological — matching SCBU's BH-arm/cosmic-arm split in `ED-SC 4.1`);
- **a holographic/information picture** (ED-10 R3 + ED-Information: missing-information `∝ area` from A1 severance; hidden-not-lost; Page-curve structure).

This **strengthens the postulate's grounding**; it does **not** upgrade any SCBU verdict (still M3) and does **not** close the open `ξ_canonical(H₀)` derivation (Route A). Honest scope below.

## 4. The Θ_ED open item — a principled "think, don't chase"

The SCBU tightening frontier (`RA-OPEN-4a`) is the *substrate-graph derivation of the `Θ_ED` value* (`Θ_ED ≈ 10⁻¹²²` in Planck units — the cosmological-constant smallness, carried as a primitive substrate parameter at the `ℓ_P, ℏ, c` tier, with `H₀ = (8π/3)^½ ℓ_P c^½ Θ_ED^½`).

The substrate-evaluation work supplies a **principled reason this is inherited, not derivable** — converting "open derivation we haven't done" into "expected-inherited by the same principle that makes the constants inherited":

> **A1** found the only observer-independent substrate quantity is **zero** — the substrate produces *no intrinsic positive scalar in isolation*. The **Facts paper** says the same ontologically: constants are *global relational facts*, not substrate-fragment outputs. `Θ_ED` is a global, cosmological-scale parameter — precisely the kind of quantity A1/Facts says is **value-inherited in principle**, like asking one molecule for the temperature.

So the honest call on `RA-OPEN-4a` is **think, don't chase**: don't burn effort trying to grind `10⁻¹²²` out of the substrate graph; by the A1/Facts logic it's a holistic inherited value, and the corpus is already correct to carry it as primitive. (This is the constants-frontier "C" verdict, now with a reason, applied to `Θ_ED`.)

## 5. The per-projection exponents — the one live derivation

The other open item — **per-projection scaling exponents** (`M_crit ∝ H₀^α`, `Q ∝ H₀^β`, `ξ ∝ H₀^γ`; currently "a family of falsifiers indexed by free exponents") — is *not* dissolved by the A1/Facts logic, because exponents are **scaling forms**, not intrinsic scalars. This is the genuinely live derivation. The ED-10 R3 method is the natural tool: **form-forced scaling, value-inherited coefficient** — derive each projection's *exponent* from how the `b → 0` boundary couples to that projection's observable, leaving only the coefficient inherited. That would sharpen SCBU's "co-variation in some sense" into a single-exponent law per axis. Hard, but it's the right shape of problem, and it needs no EFE keystone.

## 6. Honest scope (what this note does NOT do)

- It does **not** change any SCBU verdict (all six projections stay M3) and does **not** close Route A (`ξ_canonical(H₀)` / `ℓ_V5(H₀)`).
- It does **not** derive `Θ_ED`'s value — it argues, via A1/Facts, that the value is *inherited in principle* (a reason, not a derivation).
- It is **form-only**: ED's substrate-evaluation and SCBU's cosmology share the boundary *object*, not new phenomenology. No new primitive, no rule change.
- The bridge's one positive new piece is the **second formation mechanism** (expansion-driven `b→0` at `R_H`) completing the ED-10 causal-type taxonomy.

## Verdict

**SCBU and this session's substrate-evaluation program are two ends of one object: the `R_H` substrate-cosmology boundary = the A1 capacity-zero `b→0` determinability surface.** The bridge supplies independent substrate-side grounding for the SCBU postulate, completes the cosmological (two-way) horizon's *formation* mechanism (expansion-driven, distinct from commitment-driven BH collapse), and delivers a principled "think-don't-chase" on the `Θ_ED` value (inherited, by A1/Facts) while flagging the **per-projection exponents** as the one live, EFE-free derivation worth pursuing. No verdicts move; no numbers are claimed; the open derivations stay open — but the picture is now one connected surface instead of two programs.

---

*Bridge note only. The SCBU boundary is the A1/`b→0` determinability surface at cosmological scale; the evaluation work grounds the SCBU postulate and completes the two-way horizon's formation mechanism; `Θ_ED` value is inherited-in-principle (A1/Facts), per-projection exponents are the live derivation. Form-only, no corpus edits, no new primitives, no verdict changes.*
