# Phase-3 GR — Computing `c₁₄`: the Conservative Number, and the Genuine Wall

**Foundations analysis — attempts the decisive conservative coupling `c₁₄` (`α₁ = −4c₁₄`) by identifying exactly what the substrate must generate. Not a corpus edit, not a new primitive. The honest outcome: `c₁₄` is identified precisely as the strength of the arrow's gauge-breaking in the foliation-acceleration sector, but its value is genuinely *undetermined* — soft structural arguments pull both ways, no symmetry decides it, and the rigorous value needs a full coarse-graining I will not fake. This is the wall: the front is open, and I will stop generating leans the analysis cannot support.**

The EFT note reduced `α₁` to `−4c₁₄` (conservative) plus an `O(w)` correction. This note pushes on `c₁₄` and reaches the honest limit of derivation-without-full-coarse-graining.

**Crank rail — maximally load-bearing, and explicitly anti-oscillation.** This session I read favorable results too kindly three times and corrected each. The risk now is over-correcting into *unfavorable* over-reading. So: I weigh both directions honestly and land on **undetermined**, because that is what is true — not a fourth lean.

---

## 1. What `c₁₄` is, precisely

`c₁₄ a_μa^μ` is the khronon acceleration term, `a_μ = u^ν∇_ν u_μ`, the bending of the preferred foliation. For a foliation with lapse `N`, `a_i = ∂_i \ln N`; in ED `N² ∼ b`, so

> `a_i = \tfrac12 ∂_i \ln b`,  term `= \tfrac14 c₁₄ (∂ \ln b)²`.

The Einstein–Hilbert action contains a `(∂\ln N)²`-type contribution with a *fixed* coefficient (the `α₁ = 0` value). **`c₁₄` is the excess over that GR value** — the genuinely khronometric piece. `c₁₄ = 0` is GR; `α₁ = −4c₁₄`, safe only if `c₁₄ ≲ 2.5×10⁻⁵`.

## 2. What sets `c₁₄` in ED — identified

The khronon is physical because the arrow (P11/P13) breaks the time-reparametrization gauge; "arrow off" ⟹ khronon pure gauge ⟹ all `c_i = 0` (GR). So

> **`c₁₄` = the strength of the arrow's gauge-breaking in the acceleration (foliation-bending) sector** — a dimensionless combination of P04 band-fractions (the khronon-gradient analogue of `s_{02}, k_{11}`).

Precise identification; **not** free-floating. But its *value* needs the khronon effective action's gradient couplings derived from the substrate update rule — a full coarse-graining (a microscopic-to-EFT transport-coefficient computation).

## 3. Is `c₁₄` small? — two honest arguments, opposite directions

**Argument that `c₁₄ = O(1)` (unfavorable).** It is a dimensionless coupling with **no symmetry protection**, generated by P11, which is a **full-strength primitive** (the arrow is fully on, not a small parameter). Standard EFT naturalness then expects `O(1)` — which would put `α₁ = O(1)`, in tension with `10⁻⁴`. This is the generic khronometric situation (these theories are "easy to kill" for exactly this reason).

**Argument that `c₁₄` could vanish (favorable).** The khronon acceleration is `a_i = ½∂_i\ln b` — *slaved to the metric band* `b`, not an independent field gradient. If the foliation-bending carries **no cost beyond the metric** (the khronon is physical as a *propagating* mode but its static acceleration is tied to the lapse the metric already sets), then there is no excess term and `c₁₄ = 0`, `α₁ = 0`. The propagating khronon (`c_s = c`) and a vanishing *static* acceleration coupling are not contradictory — different sectors of the action.

**Neither is rigorous.** Naturalness is a soft heuristic (physics has unprotected small numbers); the metric-slaving argument is plausible but unproven (it is not established that the acceleration sector carries no excess). The established results do **not** decide it: `γ = 1` (the Schwarzschild relation) pins the *static isotropic* sector, not the preferred-frame `c₁₄`. So `c₁₄` is **genuinely undetermined.**

## 4. The honest verdict — undetermined, and this is the wall

> **`c₁₄` is identified (the arrow's acceleration-sector gauge-breaking, a band-fraction) but its value is genuinely undetermined. Naturalness expects `O(1)` (tension); metric-slaving allows `0` (safe); no symmetry or established result decides between them. The rigorous value requires a full coarse-graining of the khronon effective action from the substrate rule — a hard research computation I cannot execute reliably and will not fake.**

So `α₁` is **open, with no honest lean** — I am not going to call it "leans safe" (the screening over-reach, corrected) nor "leans unfavorable" (naturalness alone does not establish it). The truthful state is: one well-defined conservative number, `c₁₄`, sits between a GR-like `0` and a generic `O(1)`, and which it is needs a computation beyond derivation-by-inspection.

## 5. What the whole arc established (so the wall is not mistaken for failure)

The nine-step front did real, durable work even though the number is open:

- ED's gravity is **khronometric** — Einstein's tensor sector + one physical scalar (the khronon). *(structural)*
- Both gravitational cones are at light speed (`c_T = c`, `c_s = c`). *(derived)*
- The **one** falsification number is `α₁ = −4c₁₄ + O(w)`, and `c₁₄` is **precisely identified** (the arrow's acceleration-sector coupling). *(this arc)*
- The covariance escape is closed (ED is not boost-covariant); the dissipative reserve cleans the **radiative** khronon (vacuum GWs) but does **not** statically screen `α₁`. *(λ_J / EFT notes — corrections that sharpened the front)*

That is a sharply-posed, honest falsification front: **ED is safe iff `c₁₄ ≲ 2.5×10⁻⁵`, and whether it is requires computing one coarse-grained coupling.** Most theories of this class cannot even name their open number so precisely.

## 6. Verdict

**`c₁₄` is identified exactly — the strength of the arrow's gauge-breaking in the foliation-acceleration sector, a dimensionless band-fraction — and its value is genuinely undetermined: naturalness expects `O(1)` (tension with the bound), a metric-slaving argument allows `0` (safe), and nothing established decides between them.** This is the honest wall of the preferred-frame front: the rigorous `c₁₄` needs a full coarse-graining derivation of the khronon effective action, which is a real, hard computation I will not fake. **The front is open, with one precisely-named conservative number to compute, and no honest lean — the favorable and unfavorable arguments are both soft.** The arc's durable result stands: ED's gravity is khronometric with exactly one open falsification number, `α₁ = −4c₁₄`, sharply posed. No number is fabricated; no lean is claimed in either direction.

## 7. Next — honest options

1. **The full coarse-graining of `c₁₄`.** Derive the khronon effective action's acceleration coupling from the substrate update rule and settle `O(1)` vs `0`. The decisive computation — genuine research, not a turn of analysis; honest prior is *uncertain*, not safe.
2. **Search for a protection mechanism.** Is there an ED symmetry or band identity that forces `c₁₄ = 0` (metric-slaving made rigorous) or `α = 2β`? If yes, safe; if provably no, the naturalness expectation (tension) hardens.
3. **Bank with the honest verdict.** ED's gravity is khronometric; the single open falsification number `α₁ = −4c₁₄` is precisely identified and genuinely undetermined (safe iff `c₁₄ ≲ 2.5×10⁻⁵`); settling it needs one coarse-grained coupling. A clean, honest, falsifiable resting point.

---

*Computing `c₁₄`. Identified exactly: `c₁₄ a² = ¼c₁₄(∂\ln b)²`, the excess foliation-acceleration coupling over the GR value = the strength of the arrow's gauge-breaking in the acceleration sector, a dimensionless P04 band-fraction. `α₁ = −4c₁₄`, safe iff `c₁₄ ≲ 2.5×10⁻⁵`. Value GENUINELY UNDETERMINED: naturalness (unprotected coupling, P11 full-strength) expects `O(1)` (tension); metric-slaving (`a_i=½∂_i\ln b` tied to the lapse the metric sets) allows `0` (safe); `γ=1` and the other established results do NOT decide it. Rigorous value needs a full coarse-graining of the khronon effective action — a hard computation, not faked. Front: OPEN, one precisely-named conservative number, NO honest lean (favorable and unfavorable arguments both soft) — the anti-oscillation correction after three swings. Durable arc result intact: ED gravity is khronometric, one open falsification number `α₁=−4c₁₄`, sharply posed, safe iff `c₁₄` tiny. No corpus edits, no new primitives; Einstein not derived; no number fabricated, no lean spun.*
