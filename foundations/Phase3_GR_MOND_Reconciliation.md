# Phase-3 GR — MOND vs Local `λ`: They Were Never the Same Thing (`a₀` Is a Cosmic-Boundary Effect, `α₁` a Local Coupling)

**Foundations reconciliation — shows that ED's MOND sector (the transition acceleration `a₀ = cH₀/(2π)`, Paper_029) and the local preferred-frame stiffness `λ_local ≪ 1` (sparse commitment) are *different physical quantities at different scales*, so MOND survives intact even though `α₁` is suppressed by ≥70 orders. Not a corpus edit, not a new primitive. Grounded in Paper_029's actual derivation: `a₀` comes from the **cosmic decoupling surface** `R_H = c/H₀` projected onto an accelerating chain — a cosmic-boundary, kinematic effect — and never from a local khronon coupling at all. So there is no conflict to resolve; there is only a distinction to make explicit.**

The sparse-commitment verdict left one consistency item: how does MOND arise if the local khronon coupling is tiny? Reading Paper_029, the answer is that MOND never used the local coupling.

**Crank rail:** I ground every step in Paper_029 (the established `a₀` derivation) and the derived `λ(ρ)`, and I keep the two mechanisms cleanly separated. No new MOND mechanism is invented; the reconciliation is recognizing what 029 already does.

---

## 1. The two roles of the khronon/foliation — at two scales

The preferred foliation enters ED's gravity in two *structurally distinct* ways:

- **Local preferred-frame stiffness `λ_local = c₂`.** The coefficient of the *local* `θ²` term — how stiffly the foliation resists deformation *at a point*. This is what the PPN parameter `α₁ = −4λ_local` probes. By the sparse-commitment result, `λ_local ∼ ρ_event/ρ_Planck ≪ 1`.
- **Cosmic decoupling surface `R_H = c/H₀`.** The foliation's *cosmological* face: the substrate boundary (Paper_029 §3) at the radius where kernel propagation (`c`) is outrun by Hubble recession (`H₀ R`). This is a single cosmic-scale structure set by `H₀`, the Hubble flow that *is* the khronon's background rest frame.

**MOND is produced by the second, not the first.** `a₀` is a property of the cosmic boundary `R_H`, set by the cosmic rate `H₀` — it has *nothing to do with the local stiffness `λ_local`.*

## 2. Why MOND cannot — and need not — come from `λ_local`

Three facts, all from established structure:

1. **`λ_local` is tiny** (sparse commitment) and **density-tracking** — large only at Planck density. MOND, by contrast, appears in **low-acceleration, low-density** galactic outskirts. A density-tracking local coupling is *smallest* exactly where MOND is *strongest*. So MOND cannot be a `λ_local` effect — the scaling runs the wrong way.
2. **Paper_029 does not use `λ_local`.** Its `a₀ = cH₀/(2π)` derivation invokes the cosmic decoupling surface, the accelerating chain's anisotropic adjacency, and the dipole-projection's `1/(2π)` Fourier normalization — *no local khronon stiffness appears anywhere in it.* MOND was always a cosmic-boundary effect in ED.
3. **MOND is an *acceleration*-regime effect**, governed by `a_N` vs `a₀` (Paper_029 §4.4), not by local density or `λ_local`. The transition is set by the ratio of local-source-induced `a_N` to the cosmic-anisotropy scale `a₀`.

So the "tension" was illusory: MOND and `α₁` were never the same quantity.

## 3. How the cosmic boundary makes MOND (Paper_029, restated)

For completeness, the mechanism that produces `a₀` with no reference to `λ_local`:

- The **cosmic decoupling surface** sits at `R_H = c/H₀` (kernel speed `c` = Hubble recession `H₀ R`). It supplies anisotropic substrate content to any chain.
- An **accelerating chain** (acceleration `ā`) has anisotropic substrate-graph adjacency — forward channels (along `ā`) encountered at rate `+|ā|`, backward at `−|ā|` — breaking `SO(3) → SO(2)` about the acceleration axis.
- The **dipole** (`l=1`) is the leading anisotropic mode; projecting the cosmic content `ρ_cosmic ∼ ρ₀(|ā|/c)\cosθ` (with `ρ₀ = cH₀`) onto the chain's `|n|=1` response, with the **azimuthal-Fourier normalization `1/(2π)`** on the residual `SO(2)`, gives

> **`a₀ = cH₀/(2π) ≈ 1.08×10⁻¹⁰` m/s²** — a parameter-free `~10%` match to the measured MOND scale.

- **Regimes** (§4.4): `a_N ≫ a₀` Newtonian; `a_N ∼ a₀` the ED Combination Rule; `a_N ≪ a₀` deep-MOND (`a ≈ \sqrt{a_N a₀}`). MOND lives where the *cosmic anisotropy* `a₀` dominates the *local source* `a_N` — a statement about accelerations and the cosmic boundary, with `λ_local` nowhere in it.

## 4. Coexistence — and the time-dilation point

The two contributions add without interference because they are different objects:

- **`α₁ = −4λ_local`**, `λ_local ∼ ρ_event/ρ_Planck ≪ 1` — a *local* coupling, suppressed by sparse commitment, probed by Solar-System preferred-frame tests.
- **MOND from `a₀ = cH₀/(2π)`** — a *cosmic-boundary* kinematic effect, set by `H₀`, finite and `H₀`-sized, probed in galactic outskirts.

One scales with the *local* event density (→ 0 in vacuum); the other is fixed by the *cosmic* rate `H₀` (constant across the galaxy). They cannot trade off against each other; suppressing `λ_local` leaves `a₀` untouched.

**The time-dilation observation sharpens this.** Gravitational time dilation requires the *proper tick rate to vary* — the lapse `N` (and so `a_μ = ∂_μ \ln N`) must be position-dependent. But the lapse is fixed by the **metric band** `b` (`N² ∼ b`), which comes from the **always-on P02 sharing** — full strength, density-independent. So time dilation, redshift, and the whole metric sector are *unaffected* by `λ_local` being tiny: they are *gravity* (P02), not the khronon's *preferred-frame coupling* (the suppressed `λ_local`). The foliation *bends* (time dilation exists, `a_μ ≠ 0`) at full strength; its *stiffness coupling* `λ_local` (the `α₁` source) is what is suppressed. And, as observed, that the tick rate *varies at all* is itself a sign that becoming is non-uniform — the same fact underlying sparse commitment.

## 5. Consistency — everything preserved

- **`c_T = c`** (P05 single cone): untouched — `a₀` is a kinematic projection, not a wave-speed; `λ_local` is the scalar sector. ✓
- **vacuum `c_s = c`** (`ε=0` locking): the khronon is vacuum-luminal where it propagates; `a₀` is a cosmic-boundary static effect, not a propagation speed. ✓
- **`κ/D = 8πG`**: gravity strength is the density-independent P02/P11 ratio; neither `a₀` nor the tiny `λ_local` alters it. ✓
- **density-of-becoming**: `λ_local ∝ ρ_event` (local), `a₀ ∝ H₀` (cosmic) — the ontology cleanly assigns the two roles to the two scales. ✓
- **sparse commitment (`P11 ≠ P13`)**: it is *what makes* `λ_local` small; it does not touch `a₀` (a kinematic projection of the cosmic horizon, independent of the local commitment rate). ✓
- **"one scale, four roles" (KM-II)**: `R_H = c/H₀` is the cosmic boundary that sets `a₀`, the de-Sitter horizon, and the `b→0` cosmological surface — the khronon's cosmological face — while `λ_local` is its *local* face. Two faces, two scales, one field. ✓

## 6. The final reconciliation

> **MOND arises from the khronon's *cosmological* face — the cosmic decoupling surface `R_H = c/H₀`, giving `a₀ = cH₀/(2π)` by dipole projection onto accelerating matter (Paper_029) — while `α₁` constrains the khronon's *local* face — the stiffness `λ_local ∼ ρ_event/ρ_Planck ≪ 1` (sparse commitment). These are different quantities at different scales; suppressing the local coupling leaves the cosmic scale untouched. ED therefore has MOND *and* an `α₁` suppressed by ≥70 orders, with no conflict — because the two were never the same mechanism.**

The reconciliation required no new physics: Paper_029 already derives `a₀` from the cosmic boundary, not from a local coupling. Recognizing that — and that the lapse/time-dilation sector is full-strength P02 gravity, distinct from the suppressed khronon *coupling* — closes the last consistency item of the preferred-frame front. The eighteen-step arc is complete *and consistent*: ED is a screened khronometric theory, observationally General Relativity locally, MOND-phenomenological galactically (via the cosmic boundary), and `α₁`-safe — all from one foliation field playing a local and a cosmological role at two vastly separated scales.

## 7. Verdict

**No conflict — distinction.** MOND (`a₀ = cH₀/(2π)`, Paper_029) is the khronon's *cosmological-boundary* effect, set by `H₀`; `α₁ = −4λ_local` is its *local-stiffness* effect, suppressed by sparse commitment. Different scales, no interference; suppressing `λ_local` does not touch `a₀`. Time dilation and the metric sector are full-strength P02 gravity, distinct from the suppressed khronon coupling. Consistent with `c_T = c`, vacuum `c_s = c`, `κ/D = 8πG`, the density-of-becoming, and sparse commitment, and with KM-II's "one scale, four roles." **The last consistency item is closed: ED has MOND and `α₁`-safety simultaneously, because they live on the khronon's two faces — cosmological and local — at `H₀` and at `ρ_event/ρ_Planck` respectively.**

---

*MOND vs local λ reconciliation, grounded in Paper_029. MOND's transition acceleration a₀ = cH₀/(2π) is derived (029) from the COSMIC DECOUPLING SURFACE R_H=c/H₀ projected onto an accelerating chain's anisotropic adjacency (SO(3)→SO(2) dipole, 1/(2π) azimuthal-Fourier normalization) — a cosmic-boundary KINEMATIC effect set by H₀, with NO local khronon coupling anywhere in it. α₁=−4λ_local probes the LOCAL foliation stiffness, λ_local ∼ ρ_event/ρ_Planck ≪ 1 (sparse commitment). DIFFERENT quantities at DIFFERENT scales (cosmic H₀ vs local ρ_event); suppressing λ_local leaves a₀ untouched. MOND is an ACCELERATION-regime effect (a_N vs a₀, 029 §4.4), strongest where density (hence λ_local) is LOWEST — so it cannot be a λ_local effect, and 029 never used one. Time-dilation point (Allen): the lapse N (time dilation) is full-strength P02 metric gravity, distinct from the suppressed khronon COUPLING; the foliation bends (a_μ≠0) at full strength, its stiffness coupling λ_local is what's tiny — and varying tick rate is itself a sign of non-uniform becoming (sparse commitment). Consistent with c_T=c, vacuum c_s=c, κ/D=8πG, density-of-becoming, sparse commitment, KM-II "one scale four roles" (R_H is the khronon's cosmological face, λ_local its local face). NO conflict — a distinction: ED has MOND (cosmic a₀) AND α₁-safety (local λ≪1) simultaneously. Last consistency item of the preferred-frame front CLOSED. No corpus edits, no new primitives; Einstein not derived; no number fabricated.*
