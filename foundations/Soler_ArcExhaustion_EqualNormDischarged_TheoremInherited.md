# Solèr Arc Exhaustion: the Angle-Condition Equal-Norm Discharged Operationally (the Move-1 Parallel), and the QM Keystone Exhausts as Grounded-Hypotheses + Inherited-Theorem + Emergent-Metric

**Foundations — the third and last of AP's GUT structural arcs (Solèr / the QM-foundations keystone tail under the reconstructed Gleason inner-product keystone). Read to exhaustion from the standing corpus: `Paper_QuantumLogicKeystone_GleasonReconstruction` (the published reconstruction), `Gleason_Soler_LatticeVerification` (the per-axiom map), `Gleason_CircularityAudit_2026-07-10`. Crank-rail ON. Two moves: (1) discharge the one flagged-open, bounded, non-circular residual — the angle-condition *equal-norm* — operationally from P03 homogeneity, exactly as Move 1 discharged orthogonality; (2) show the arc exhausts into ED's standard pattern: the hypotheses grounded to primitives (ED's contribution), the Piron–Solèr theorem inherited (standard math, legitimately invoked not derived), the metric emergent (Born), and two distinctive interpretive claims at the falsifier frontier. Honest boundary held throughout: this does NOT close the Hilbert space as a theorem; it closes the last ED-side hypothesis-residual and classifies what remains.**

## 1. What was already standing (the reconstruction, before this note)
The keystone that stalled three prior attempts is a coherent reconstruction (`Paper_QuantumLogicKeystone`). Per Piron–Solèr axiom, grounded to ED primitives:
- **Channel orthogonality** `⟨K|L⟩=0` — **reduced** to channel-distinctness (Move 1: perfectly distinguishable ⟺ orthogonal; ED's channels are perfectly distinguishable by exclusion of commitment, P07+P11+Born), *given the representation*.
- **ℂ field** — **selected** from Solèr's `{ℝ,ℂ,ℍ}` (P09's scalar phase rules out ℝ by the central-scalar argument; V5/063 composites rule out ℍ). T14 upgraded.
- **Covering law** — **candidate-grounded on the channel basis** (P11 irreversibility *enforces* first-kind repeatability: a committed channel is locked, so re-measurement returns it with certainty).
- **Orthocomplementation** (P11 exclusivity), **irreducibility** (P10 sectors) — grounded.
- **Orthomodularity** — rides with the representation (automatic for any inner-product lattice; not independent).
- **Preferred basis** — resolved: **primitive einselection** (P11 commits only in the intrinsic channel basis; basis-democracy emergent).
- **Physical (Kochen–Specker) non-contextuality** — resolved (intrinsic `b_K` from P04 + one substrate commitment context + stochastic commitment).

Flagged as the residual: the **Solèr technical lattice rigor** (Piron's exchange-property/projective step, infinite-dim), plus two named ED-side gaps — the **angle-condition equal-norm circularity** and **atoms-join** — and the exact/emergent split (logic exact, metric Born-emergent).

## 2. The angle-condition equal-norm, DISCHARGED (the Move-1 parallel)
Solèr's angle condition needs an infinite orthogonal sequence `{e_i}` of **equal norm** `⟨e_i|e_i⟩ = ⟨e_j|e_j⟩`. The flagged problem (`Gleason_Soler_LatticeVerification` §angle; `Paper_QuantumLogicKeystone` §3): the ED route (P03 spatial homogeneity → infinitely many translation-equivalent channels, orthogonal by P07) has "equal norm" **presupposing the inner product** — circular, exactly the trap Move 1 removed for orthogonality, and "not yet derived."

Discharge it the same way — operationally, not metrically:
- **ED's sequence:** the translation-equivalent channels `{K_i}` across the homogeneous substrate — infinite (P03 unbounded homogeneity), pairwise orthogonal (P07 distinctness + Move 1).
- **The norm is a substrate quantity, not a free metric.** In ED's amplitude representation (Paper_001, `√b_K · e^{iπ_K}`), `⟨K|K⟩ = b_K`: the squared norm **is** the channel bandwidth (P04). So "equal norm" `⟺` "equal bandwidth", a substrate fact, not a metric assumption.
- **P03 homogeneity forces equal bandwidth.** A translation-equivalent channel is substrate-identical under P03 spatial homogeneity, so `b_{K_i} = b_{K_j}`. Therefore `⟨K_i|K_i⟩ = b_{K_i} = b_{K_j} = ⟨K_j|K_j⟩` — **equal norm, forced by P03 homogeneity via the Born reading of the norm as commitment-weight**, with no metric input.

> **The angle-condition equal-norm reduces to equal-bandwidth (P03 homogeneity), the same operational move that reduced orthogonality to distinguishability (Move 1).** It upgrades from "candidate, circularity-flagged" to **grounded operationally, given the representation.**

**Honest boundary (identical to Move 1).** This reduces equal-norm to a substrate fact *given* the amplitude representation `⟨K|K⟩ = b_K` (Paper_001). It does not construct the representation. So the flagged circularity is removed — equal-norm is no longer a metric assumption — while the representation's own existence (the Piron step) stays the inherited residual (§3). *(Atoms-join closes the same tier: the channel sub-lattice is the Boolean algebra of subsets of the channel index set, so every proposition is a join of channel-atoms by construction; for the full lattice atomicity rides with the representation, like orthomodularity. Not an independent gate.)*

## 3. What genuinely remains — and it is INHERITED standard math, not an ED gap
After §2, every ED-side hypothesis of Piron–Solèr is grounded, reduced, selected, or resolved to primitives. What "rides underneath the whole reconstruction" is the **Piron–Solèr theorem itself**: the projective-geometry / exchange-property step that turns the grounded axioms into "closed subspaces of a Hermitian space over `K`", plus the infinite-dimensional `*`-ring subtleties. **This is a standard mathematical theorem (Piron 1964 / Solèr 1995), which ED *invokes*, not something ED must *derive*** — exactly as ED inherits Clifford theory and Pauli's theorem for the spinor (T2), and the lattice-gauge→continuum dictionary for Maxwell. Inheriting the theorem is legitimate; the ED-side work is showing the substrate *satisfies its hypotheses*, which the reconstruction (now including equal-norm) does. So the residual is **inheritance, not a hole**: ED does not owe a re-derivation of Piron–Solèr any more than T2 owes a re-derivation of Pauli's theorem.

## 4. The exhaustion verdict
**The Solèr arc exhausts into ED's standard pattern:**
- **Hypotheses grounded to primitives (ED's contribution, the reframe):** orthogonality (operational distinguishability, Move 1), equal-norm (operational homogeneity, §2), covering law (arrow-enforced first-kind, channel basis), orthocomplementation (P11), irreducibility (P10), ℂ (selected by P09 + composites), preferred-basis einselection (P07+P11), Born non-contextuality (intrinsic `b_K` + one context + stochastic commit). This is the genuine ED content — the change of question from "are the vectors orthogonal/equal-length?" to "what does commitment operationally do, and what must represent it?"
- **The theorem inherited (standard math):** Piron–Solèr (the projective step + ∞-dim rigor) stitches the grounded axioms into a Hilbert space over `ℂ`. Invoked, not derived — a legitimate inheritance.
- **The metric emergent (Born):** the proposition *lattice* is substrate-exact (P07, P11); the inner-product *metric* is Born-statistical. The Hilbert space is **exact-as-logic, emergent-as-metric** — consistent with ED's two-layer philosophy (the metric is the layer-2 object).
- **Two distinctive interpretive claims at the falsifier frontier (not gaps, features):** einselection is *primitive* (the arrow selects the pointer basis, not decoherence-emergent), and QM's multi-context Gleason/Kochen–Specker structure is *emergent*. Both tied to the arrow; both carry a candidate (currently-unobservable) substrate-scale signature.

In the "ED-derived or ED-inherited" test: the QM-logic axioms are **ED-grounded** (to primitives, with the operational reframe); the stitching theorem is **ED-inherited** (standard math); the metric is **ED-emergent** (Born); the two distinctive claims are **ED-predictions** (falsifier frontier). Nothing is an un-classified open hole.

## 5. Net — and the three-arc status
**Solèr arc: exhausted.** The QM keystone is a reconstruction whose ED-side hypotheses are all grounded to primitives (the last one, equal-norm, discharged here operationally); the Piron–Solèr theorem that completes it is inherited standard math (legitimately, as everywhere in ED); the metric is Born-emergent; and the two distinctive claims (primitive einselection, emergent multi-context) are the falsifier frontier. This is "as closed as ED closes anything" — the reframe + grounding is ED's contribution, the theorem is inherited, the metric is emergent — the identical shape as gravity, gauge, T4, and the continuum arc.

**All three structural GUT arcs are now exhausted:** T4 (form-complete + value/structure-inherited + one channel-topology wall), continuum/curvature (geometry derived-native + smooth-continuum an emergent shadow), and Solèr (hypotheses grounded + theorem inherited + metric emergent). Each resolved into *ED-derived form / ED-inherited standard-math-and-values / a small honest residual or distinctive prediction*, not an open hole. Per AP's plan, the GUT report is now writable: every arc is classified. Crank-rail: discharged only the one genuinely-open bounded residual (equal-norm) with the same honest "given the representation" boundary as Move 1; did not upgrade the reconstruction to a closed theorem; classified the inherited theorem honestly as inheritance (as the reconstruction paper itself does), not as a closed ED derivation.
