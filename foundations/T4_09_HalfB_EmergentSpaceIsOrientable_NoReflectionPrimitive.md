# T4 · Step 9 — Half (b) Verdict: the Emergent 3-Space IS Orientable (ED Has No Reflection to Make It Otherwise; and Its Spinors Require It) — the Spontaneous Global Orientation Genuinely Exists

**Foundations — T4 step 9. Settles half (b) of gate (i) (`T4_08`): does a *single global* spatial orientation exist for AP's parity route, or is the emergent space non-orientable / a patchwork? Crank-rail ON. Verdict: **ORIENTABLE, by a clean argument from ED's parity-cleanness** — so the spontaneous global orientation AP's mechanism needs genuinely exists, and the entire remaining burden is gate (ii). This resolves positively a question the corpus's geometry arc left flagged-open.**

## 1. The question
Half (b): a *single global* orientation exists only if the emergent 3-space is **orientable** (admits a consistent global handedness — no orientation-reversing loop) and **connectedly propagated** from a seed (one choice, not a patchwork). The corpus treated this as part of the open emergent-topology bridge; it can in fact be decided.

## 2. The argument — ED cannot build a non-orientable space (it has no reflection)
Orientability is precisely the absence of a **reflection** in the frame bundle: a manifold is non-orientable iff some loop's frame-holonomy is a reflection (`det = −1`), i.e. a reflection is built into the atlas/gluing (the Möbius identification is the canonical example). So the question is: **can ED's emergent space acquire a reflection holonomy anywhere?**

- **P05 transport is a *proper* rotation.** P05 (the connection) is continuous and invertible — an isometry, invertible between commitments (Gauge_02). A continuous, invertible map built up from the identity lies in the **identity component** of the frame group — `SO(3)` (`det = +1`) — never a reflection. And a holonomy around any loop is a *product* of these proper transports, hence itself proper (`det = +1`).
- **No ED primitive is a reflection.** The substrate is parity-clean: P02–P13 contain no reflection/orientation-reversing operation (this is the same established parity-symmetry behind gate (i)-a). There is nowhere for a `det = −1` to enter.
- **All-proper frame relations ⟹ an SO(3)-structure ⟹ orientable.** A frame bundle whose transition/gluing relations are all in `SO(3)` (no reflections) is, by definition, an `SO(3)`-structure — and an `SO(3)`-reduction of the frame bundle *is* an orientation (standard differential geometry). So the emergent space is orientable.

> **ED literally cannot build a non-orientable space — it has no reflection in its toolkit.** The *same* parity-cleanness that makes any orientation *spontaneous* (gate (i)-a — no rule fixes a handedness) *also* forbids the reflection that would make the space non-orientable (here). One fact, both halves: no parity-odd primitive ⟹ orientation is spontaneous *and* the space is orientable.

## 3. Independent cross-check: ED's spinors require it
ED has spin-½ fermions grounded in the substrate (T4_03: tethered frames under `SO(3)` → the `SU(2)` double cover). **Globally-defined spinors require a spin structure, which requires orientability** (`w₁ = 0` is necessary). So the mere fact that ED carries spinors is *consistent only with* an orientable emergent space — a second, independent indication of the §2 conclusion. *(Mildly circular as a standalone — T4_03 already works in `SO(3)` — so it is a consistency check, not the primary argument; §2 is the derivation.)*

## 4. Connected propagation ⟹ a *single* orientation, not a patchwork
Orientability gives that a consistent global handedness *can* be chosen; connectedness gives that *one* choice covers everything. ED's space grows **connectedly from a seed** — every event causally descends from the initial commitment (the big-bang / SCBU origin) — so an orientation chosen at the seed propagates by continuity to the whole causal patch. This is structurally the same as R4's lock (a reference set once at first-arrival and globally enforced). *(The global topology *beyond* the horizon is undetermined and irrelevant: the observable/causal patch is orientable + connected, so it carries one orientation — all AP's mechanism needs.)*

## 5. Verdict — half (b) CONFIRMED; the parity-odd background genuinely exists
**The emergent 3-space is orientable, and grows connectedly from the seed, so it carries a *single global* orientation.** Combined with gate (i)-a (any orientation is *spontaneous*, confirmed), AP's mechanism now has a **fully-founded parity-odd background: ED's emergent space carries a single, spontaneously-chosen, globally-consistent handedness.** Both decidable gates of the route are passed:

| gate | question | verdict |
|---|---|---|
| (i)-a | is any orientation spontaneous, or law-fixed? | **spontaneous — CONFIRMED** (parity-clean rules can't fix one) |
| (i)-b / this note | does a single global orientation exist? | **YES — CONFIRMED** (no reflection primitive ⟹ orientable; connected growth ⟹ single) |
| (ii) | does the emergent field content *couple* to it (→ observable parity violation)? | **the whole remaining burden** — the channel-topology→rep-spectrum program |

**Net for the matter sector.** AP's orientation-SSB route is no longer "plausible-pending-topology" — its foundation is **confirmed**: the spontaneous global handedness is real. The last native-chirality question has passed from *"blocked at every route"* to *"a founded live mechanism whose only remaining burden is the coupling (gate ii)."* And gate (ii) is the same channel-topology→representation-spectrum program the entire matter sector funnels into — so **chirality and the rep-spectrum are now one target, entered from a confirmed parity-odd background rather than a wall.** Tier: a structural argument on standard geometry + established ED facts (P05-properness, parity-cleanness, spinors); solid, and it converts half (b) from open to closed-positive. The concrete next move is gate (ii): whether the emergent gauge/matter content references the now-established global orientation.
