# Phase-3 GR — Hunting a Protection for `c₁₄`: Exact-Zero Refuted, Natural-Smallness Established

**Foundations analysis — searches for an ED structural mechanism forcing `c₁₄ = 0` (`α₁ = 0` outright) or the `α = 2β` locus, and checks any candidate against ED's *own* established results. Not a corpus edit, not a new primitive. The hunt resolves cleanly in BOTH directions: (i) exact protection is REFUTED — ED's established `c_s = c` plus a propagating khronon *force* `c₁₄ ≠ 0`; (ii) but small `c₁₄` is shown to be TECHNICALLY NATURAL (not fine-tuned), and ED's metric-only matter coupling is exactly what protects it. Net: `α₁` is open, exact-zero excluded, natural-smallness available.**

The `c₁₄` note left the value undetermined with two soft arguments. This note does what I should have done first: test the candidate protections against consistency with `c_s = c`. That consistency check is decisive and cuts the soft arguments down to a firm result.

**Crank rail:** I check candidates against established results before trusting them (the discipline I skipped when I floated metric-slaving). Where a candidate is refuted, I say so; where a real protection (technical naturalness) holds, I claim only what it gives — *allowed*, not *derived*.

---

## 1. The two candidate protections

`α₁ = −4c₁₄` (with `β = 0` from `c_T = c`). It vanishes iff:

- **(A) metric-slaving:** `c₁₄ = 0` — the khronon's acceleration carries no cost beyond the metric;
- **(B) the viability locus** `α = 2β`: with `β = 0` this is *also* `c₁₄ = 0`.

Both collapse to the same requirement: **`c₁₄ = 0`.** So the protection hunt is one question: does ED force `c₁₄ = 0`?

## 2. Finding 1 — exact protection is REFUTED by `c_s = c`

Check `c₁₄ = 0` against ED's own established results. The verified scalar speed (β = 0):

> `c_s² = \dfrac{(2−α)λ}{α(2+3λ)}`,  `α = c₁₄`, `λ = c₂`.

Impose `c_s = c` (ED's `ε = 0` result): `(2−α)λ = α(2+3λ)`, i.e. `α = λ/(1+2λ)`. Now set `α = c₁₄ → 0`: the relation forces `λ → 0` as well. But `α = λ = β = 0` is **all couplings zero = General Relativity = no propagating khronon.**

> **So `c₁₄ = 0` is incompatible with ED having a *propagating* khronon at `c_s = c`. ED has exactly that (the khronometric result + the `ε = 0` derivation). Therefore `c₁₄ ≠ 0` is *forced*.**

Both protection candidates (A) and (B) collapse to the refuted `c₁₄ = 0`. **The clean "`α₁ = 0` by structure" escape is closed** — and it is closed by ED's *own* prized results (a physical khronon at light speed), not by an external assumption. The metric-slaving argument I floated earlier is refuted: the khronon's acceleration sector *cannot* be cost-free, because then the khronon would not propagate.

Concretely, `c₁₄ = α = λ/(1+2λ) ≈ λ` for small couplings, so `α₁ ≈ −4λ`: **the preferred-frame parameter is locked to the khronon's expansion coupling `λ = c₂`** by `c_s = c`. They rise and fall together.

## 3. Finding 2 — but small `c₁₄` is TECHNICALLY NATURAL (not fine-tuning)

The refutation sounds bad (no exact protection), but the second half of the hunt is the genuine good news, and it is ED-specific:

- **The `c_i → 0` limit is General Relativity**, which has *full* diffeomorphism invariance — *more* symmetry than the foliation-preserving symmetry of the khronometric theory. So small khronometric couplings are protected by an **approximate enhanced symmetry**: in 't Hooft's sense, **small `c₁₄` is technically natural** — it does not require fine-tuning, because the symmetry is restored as `c₁₄ → 0`.
- The usual worry that ruins this in Lorentz-violating gravity is *radiative*: if matter sees the Lorentz violation, loops regenerate large couplings. But **ED's matter coupling is metric-only (KM-I)** — the matter sector is Lorentz-invariant and does not source the khronon couplings radiatively. **So in ED the technical naturalness of small `c₁₄` is intact**, precisely because of the metric-only coupling that also saved lensing.

> **Therefore: ED being PPN-safe (small `c₁₄`) is a technically-natural possibility, not a fine-tuning. The metric-only coupling (KM-I) is exactly what protects it.** Safety costs no naturalness price; it only awaits a value.

## 4. The magnitude question, sharpened

`α₁ ≈ −4λ` (with `λ = c₂` the expansion coupling). Safety needs `|λ| ≲ 2.5×10⁻⁵`. For comparison, BBN bounds `λ` (via the cosmological `G`-renormalization, KM-II) only to `≲ 10⁻¹`. **So `α₁` is the binding constraint — `~10⁴×` tighter than BBN** — and ED must sit in the small-`λ` corner of khronometric gravity. That corner is *non-empty* (viable khronometric theories live there post-GW170817) and *technically natural* (§3), but ED supplies **no derived value** for `λ`; whether it lands `≲ 2.5×10⁻⁵` needs the coarse-graining.

## 5. Verdict — the cleanest honest statement the front admits

**The protection hunt resolves in both directions, and the result is firm rather than soft:**

- **Exact protection is refuted.** `c₁₄ = 0` (hence `α₁ = 0` outright, and both the metric-slaving and `α = 2β` routes) is *incompatible* with ED's established propagating khronon at `c_s = c`. `c₁₄ ≠ 0` is forced; `α₁` is genuinely nonzero. *(This kills the over-optimistic "structurally safe" hope, earned via ED's own results.)*
- **Natural-smallness is established.** Small `c₁₄` is technically natural — protected by the GR-limit diffeomorphism symmetry, with ED's metric-only matter coupling (KM-I) shielding it from radiative spoiling. *(This kills the over-pessimistic "fine-tuning required" worry.)*
- **Net:** `α₁ ≈ −4λ` is **open**, nonzero, locked to the khronon expansion coupling `λ`, **safe iff `λ ≲ 2.5×10⁻⁵` — a value that is technically natural to have but not derived.** The binding constraint is `α₁` itself (`~10⁴×` tighter than BBN).

So the honest standing, after genuinely hunting: **ED's preferred-frame safety is not protected to be exact, and not penalized as fine-tuning — it is a technically-natural, undetermined small number `λ`, with the answer awaiting the one coarse-graining computation.** This is the most precise and the most *stable* characterization the front has had: both the false hope and the false worry are removed, and what remains is a single technically-natural coupling to compute. No number is fabricated; the refutation and the naturalness are both earned.

## 6. Next

1. **Coarse-grain `λ = c₂`** (the khronon expansion coupling) from the substrate — now the single decisive number (`α₁ ≈ −4λ`), and technically natural at any size, so no prior either way.
2. **Bank with this verdict:** ED's gravity is khronometric; `α₁ ≈ −4λ` is open, nonzero (exact protection refuted), and safe iff the expansion coupling `λ ≲ 2.5×10⁻⁵` — technically natural (KM-I-protected), undetermined. A clean, honest, falsifiable resting point with the naturalness status settled.

---

*Protection hunt for `c₁₄`. Both candidate protections (metric-slaving `c₁₄=0`; the `α=2β` locus) collapse to `c₁₄=0`, which is REFUTED: ED's established `c_s=c` + a propagating khronon force `α=c₁₄=λ/(1+2λ)≠0` (`c₁₄=0 ⟹ λ=0 ⟹` GR, no khronon). So `α₁=−4c₁₄≈−4λ` is genuinely nonzero, locked to the expansion coupling `λ=c₂`. BUT small `c₁₄` is TECHNICALLY NATURAL — the `c_i→0` limit is GR (enhanced diffeo symmetry), and ED's metric-only matter coupling (KM-I) shields it from radiative spoiling — so safety is no fine-tuning. Net: `α₁` open, exact-zero excluded, natural-smallness available; safe iff `λ≲2.5×10⁻⁵` (the binding constraint, ~10⁴× tighter than BBN), technically natural but undetermined. Both the over-optimistic ("structurally safe") and over-pessimistic ("fine-tuned") readings removed — the stable honest equilibrium. Next: coarse-grain `λ`. No corpus edits, no new primitives; Einstein not derived; no number fabricated, refutation and naturalness both earned.*
