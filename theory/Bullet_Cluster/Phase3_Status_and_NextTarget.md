# Bullet_Arc Phase-3: Status, and the One Next Target

**Author:** Allen Proxmire
**Arc:** Bullet_Arc (ED-Bullet-01) — Phase-3 status / banking note
**Purpose:** Tie the Phase-3 documents together, state what is banked, name the single real next target, and record the honest fallback. Read this first when returning to the arc.

---

## The Phase-3 thread, in order

1. **Observational protocol** (`Paper_ED_Bullet_Phase3_ObservationalProtocol`, and the EDG standalone `Paper_OffsetVelocityLaw_MergingClusterTest` + PDF). The offset–velocity law made executable: the observable (mass–gas offset, not the SIDM DM–galaxy offset), the statistic (segmented-regression model comparison), the real dataset (Wittman ensemble + characterized dissociative mergers), and the feasibility finding (dissociative mergers are velocity-selected into the ceiling regime, so the diagnostic knee lives in the under-measured low-offset population; a Rubin/Euclid/eROSITA-era test; ceiling checkable now). **This is the live, grounding-independent test and it stands regardless of everything below.**

2. **Theory reduction** (`Paper_ED_Bullet_Phase3_PinningTheKnee`). The two Phase-2 open ingredients that floated the knee (v_crit ~ 15–1500 km/s) reduce to one: the dynamic exponent z is fixed at ~2 (Model A) from GR-III's relaxational bandwidth rule; the two-timescale core/dressing structure is resolved structurally (the defect is a pinned texture, no vacuum mass gap, so the two clocks are independent by construction); what remains is a single residual, the static exponent ν of the organizational transition.

3. **ν-scope** (`Paper_ED_Bullet_Phase3_NuScope`). ν cannot be measured on the certified substrate: it is orientation-blind (hard Σ invariant) and its orientation is inert and 2D, so there is no ordering transition to have a ν. Redirected to the prior question: does the arc's order parameter even exist in the primitives?

4. **Path-B first result** (`Paper_ED_Bullet_Phase3_PathB_FirstResult`, probe `nu_pathB_orderparameter_probe.py`). Reading derived directors off the certified substrate: the dynamical sector produces a director (commit-flow) that is **displaceable from the matter** (decoupled from ∇rho — the one arc-critical property, grounded), but only **short-range coherent** and with **no topological-defect capacity** (windings at random-field level). Structural cause: orientation-blindness means no director–director coupling, so no mechanism for long-range order or protected windings.

## What is banked (honest status)

- **The observational test is real, experiment-ready, and independent of the grounding questions.** It needs only that *some* all-or-nothing ordering transition occurs, not that we have derived it. Confirm/kill lives in the data.
- **The knee location reduces to one exponent ν** (z=2 grounded; two-timescale resolved).
- **The arc's order parameter is only partially grounded.** Displaceability is an *output* of the certified primitives; long-range **order** and **topological-defect capacity** are *inputs* — assumed at the coarse-grained level, resting on an ordering director coupling the certified orientation-blind rule does not contain. This is the arc's real, now-precisely-located gap. It is not a refutation (a 3D emergent coupling could arise); it is a sharpened statement of status.

## The one next target (do after banking)

**Does an ordering director coupling *emerge* when the orientation-blind substrate is coarse-grained, or must it be added by hand?** This is the emergent-free-energy question. If an effective stiffness for a derived director (e.g. commit-flow) appears at coarse scale, its symmetry (O(2)? O(3) on a 3D substrate) fixes the topology *and* ν in one stroke, grounding the whole mechanism. If it does not, the topological-defect order parameter has no substrate home and must be rebuilt on a different field.

- It is a **research program, not a probe.** Schedule it deliberately.
- It is **the same emergent-free-energy problem that sits under curvature emergence** (does an ordering/coupling structure emerge from coarse-graining the substrate). Attack them together; a method for one likely serves the other.
- **The trap to refuse:** insert an O(3) alignment term, run it in 3D, and "find" monopoles and ν≈0.70. That measures the inserted coupling, not ED. Do not build it and call it grounding.

## The fallback (always available)

**Path C:** carry ν ≈ 0.70 (3D O(3)/Heisenberg) as an explicit flagged assumption. With z=2 already fixed, this hands a working knee-location estimate immediately, tiered honestly as an inherited target, not a measurement. Cheap, partial, honest — the right move whenever a concrete knee number is wanted without waiting on the emergent-free-energy program.
