# The Dwell Question — RETRACTED (Sourcing Error)

**Status: RETRACTED, 2026-07-05.** This memo originally answered the dwell gate with "dwell is admissible under P04/P11, ED mass is groundable-in-principle." **That conclusion rests on a sourcing error and is withdrawn.** An independent second-session verification, confirmed here first-hand, showed the argument was built on a non-canonical, explicitly archived structure. No four-band/dwell simulator should be built on it. This doc is kept (not deleted) as a visible correction, in the same style as the E1 headline retraction.

> **BOTH DEFECTS NOW ADDRESSED, 2026-09-05 — and the route is still blocked, for a better reason.** **Defect 1 (four-band sourcing) is ANSWERED** on its own terms: AP adopted **Branch 3**, under which the bands are a **classification of the channel index set $\mathcal{K}(u)$** licensed by P05/P07/P10/P11 and merely *summed by* P04 — so the partition is **not in P04 and never needed to be**, the Internal class exists, and a commitment has somewhere to collapse into. `primitives/P04_bandwidth.md` (amended); gravity ledger #93. **Defect 2 (the `∇_adj` borrow) is REPAIRED AS A DIAGNOSIS, NOT AS A RESCUE.** It is a **naming collision**: P12's `∇_adj` is an *adjoint gradient*, a derivative operator on the adjacency graph; the Adjacency band is a *class of channels*. **And the reading it produced is contradicted, not merely unsourced** — `qm-kinematics/Paper_012_7_AdjacencyBandwidth_Galilean` has adjacency as the **boost-INVARIANT position sector** (*“the position-content does not shift kinetically”*), while the transforming, motion-carrying sector is **propagation**, whose band lives in `Paper_012_6`'s separate **`P-FourBand`** partition. **So the argument reached into one partition for a band that lives in another and glued them with an operator from a third primitive.** Finally, **canonical P11 is silent on locus** — it collapses multi-channel participation to single-channel participation and says nothing about `u` — so *commitment ⇒ advancement* is an **identification**, named as the candidate **`P-Commitment-Advancement`** and **deliberately not adopted and not censused.** **Net: the dwell route is no longer blocked by a sourcing error; it is blocked by an honestly-stated missing postulate, which is a better place to be blocked.** `ED Generative/physics-papers/foundations/Note_Dwell_SecondRepair_2026-09-05.md`; gravity ledger #94. **The retraction below stands; the mass sector remains unaffected** (`FourBand_Dependency_Check.md`).

## What was wrong

The whole argument hung on P04 carrying a **four-band partition (internal / adjacency / environmental / commitment-reserve)**, so that a commitment (P11) could collapse to an *internal* channel = a dwell. That partition is **not in the canonical primitive.**

- **Canonical P04** (`physics-papers/wedges/Paper_087_13Primitives.md`, the authoritative primitives paper): *"Each channel-locus participation carries a bandwidth b_K(u) ≥ 0. Bandwidth is: non-negative, additive under channel decomposition ... the substrate-level scalar carrier of 'amount of participation.'"* That is the entire definition. **No bands.** No internal/adjacency/environmental/commitment-reserve split.
- **The four-band structure** comes from `papers/Generative Papers/paper_M2_primitive_forcing_P04_four_band_partition.md`, which carries the banner: *"⚠️ ARCHIVED — 2026-05-13 ... Do not cite this paper as 'forcing' P04; do not build new work on its framework."* It is a retired M-series *forcing* paper.
- The "adjacency = movement direction" reading was borrowed from **P12** (the Σ-gradient/acceleration construct, `a_C = −∇_adj Σ_C`), a *different, unrelated* primitive, and misapplied to interpret a "P04 band" that isn't canonically there.

So the provenance chain was: a non-canonical position-paper summary line ("P04 §1.5") → an explicitly archived forcing paper's vocabulary → a borrowed sentence from an unrelated primitive. **None of it survives contact with what canonical P04 and P11 actually say.** P11 is indeed silent on spatial advancement, but that silence does not help: there is no canonical internal band for a commitment to collapse into in the first place.

**And I saw the warning sign and rationalized past it.** Earlier in the same session I read Paper_087's P04 and explicitly noted it "does NOT list the four-band partition," then trusted the position paper's "§1.5" pointer anyway. That is precisely the M-series forcing crank-tell the standing memory note exists to catch: treating an archived "forcing" paper's content as if the postulated-primitives framing had established it.

## Corrected state of the mass sector

Revert to where E1 left it:

- **H2 / σ_τ does not ground on the reference substrate** (E1, empirical — survives).
- **H1 is blocked** by the missing second Σ-visible field and by orientation being Σ-blind (survives — these are facts about the reference substrate, independent of the four-band error).
- **The reference update rule is ballistic-or-extinct** (survives — a fact about the code).
- **The mass sector stays OPEN / inherited.** `Paper_113` row 10 remains OPEN. The other session's Paper_113 citation is unaffected: it cited the E1 diagnostic (no amplitude-invariant field on the reference substrate), which survives; it did not cite the dwell claim.

> **✅ NOW LICENSED — `P-Commitment-Advancement` ADOPTED 2026-09-06 on AP's decision (gravity ledger #117).** Declared at `Paper_087` §P11 and entered in the postulate registry; **census 173 → 174.** **`P-Commitment-Advancement`:** *a commitment selecting a **propagation-carrying** channel advances the chain's locus; one selecting a **non-propagating** channel does not.* **Propagation-carrying is not a new class** — Branch 3 (#93) makes the **Adjacency** class exactly the P05-transporting channels, so the postulate reads: **commit into Adjacency and you move; commit into Internal and you stay.** **This memo's demand is met on its own terms** — it asked that the dwell be *“proposed honestly as a new candidate primitive refinement … not asserted as something the existing 13 primitives already admit”*, and it is now a **named, tiered, censused postulate** rather than an assumption. **And this memo's own objection is answered:** it argued P11's silence *“does not help: there is no canonical internal band for a commitment to collapse into in the first place”* — **Branch 3 supplied exactly that class.** **What is unblocked is the SOURCING, not the physics:** nothing derives `σ_τ`, the lepton masses, or any mass-sector result, and **the retraction below otherwise stands.** **The certified rule is unaffected** — *“ballistic-or-extinct, no dwell”* is the **special case** in which every commitment selects an Adjacency-class channel, so everything measured on it stands (#116). `ED Generative/physics-papers/foundations/Note_ChannelVsLocus_Selection_2026-09-06.md`.

**The dwell idea is not dead, but it is not licensed.** If pursued, it must be proposed honestly as a **new candidate primitive refinement** (an internal-band commitment channel added to the substrate), not asserted as something the existing 13 primitives already admit. The corpus's own M-series history is a direct warning to be very careful here: the "forcing" framing was retired program-wide precisely because it treated displaced postulates as established.

## Corpus hygiene flag (bigger than this memo)

Arc M's σ_τ mass functional sums over the same four bands (`σ_τ = ℏ√(Σ_X w^X ⟨(∂ ln b^X)²⟩)`, X ∈ {internal, adjacency, environmental, commitment}), and the Arc Q Higgs scoping references `b^env`, `b^adj`. **These live-corpus results depend on a four-band structure that the canonical primitive does not carry and that was archived.** This should be checked: does Arc M's four-band σ_τ stand as an independent Arc-M modeling choice, or does it inherit the retracted M-2 "forcing"? Flagged for the open-targets hygiene list — this is a real dependency question, not just my error.

## Lesson (banked to memory)

When the canonical primitives paper (`Paper_087`) and a position-paper elaboration disagree, **the canonical paper wins**, and the elaboration's provenance must be checked before building on it — especially anything labeled "§N.M forcing," "four-band," or "primitive forcing," which traces to the retired M-series. First reads that add structure to a primitive are the crank-tell; verify against the canonical text before recommending work on them.
