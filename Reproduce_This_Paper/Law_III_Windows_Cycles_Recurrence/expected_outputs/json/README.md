# JSON Outputs — Law III

This folder contains the canonical JSON reports for ED‑Arch‑24.

Expected files:

- `law_surface_results.json`  
- `decay_window_results.json`  
- `recurrence_window_results.json`  

These JSON files represent:

- recurrence‑window detection  
- ladder‑rung timing  
- window‑width scaling  
- mechanism classification  
- recurrence–resonance interaction  

They are generated automatically by:

```
python run_law_III.py
```
