# JSON Outputs — Law 20

This folder contains the canonical JSON reports for ED‑Arch‑20.

Expected files:

- `law_surface_results.json`  
- `decay_window_results.json`  
- `angular_rate_results.json`  

These JSON files represent:

- analytic law‑surface sampling  
- DECAY window boundaries  
- angular‑rate law validation  
- mechanism predictions  

They are generated automatically by:

```
python run_law_20.py
```
