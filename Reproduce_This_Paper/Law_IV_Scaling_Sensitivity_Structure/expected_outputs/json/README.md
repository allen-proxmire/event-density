# JSON Outputs — Law IV

This folder contains the canonical JSON reports for ED‑Arch‑25.

Expected files:

- `law_surface_results.json`  
- `decay_window_results.json`  
- `scaling_results.json`  

These JSON files represent:

- recurrence‑time scaling  
- window‑width scaling  
- DECAY‑spacing scaling  
- ladder‑density scaling  
- universal‑curve rescaling  

They are generated automatically by:

```
python run_law_IV.py
```
