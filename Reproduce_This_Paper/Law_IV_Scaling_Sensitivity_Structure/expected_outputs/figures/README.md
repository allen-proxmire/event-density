# Figures — Law IV

This folder contains the canonical figures for ED‑Arch‑25.

Expected files:

- recurrence‑time scaling plot  
- window‑width scaling plot  
- DECAY‑density scaling plot  
- ladder‑density scaling plot  
- universal‑curve overlays  

These figures visualize:

- \( T_r(N) \propto 1/N \)  
- \( W(N) \propto 1/N \)  
- DECAY spacing shrinking with \( N \)  
- ladder density growing with \( N \)  
- collapse‑surface convergence under rescaling  

Figures are generated automatically by:

```
python run_law_IV.py
```
