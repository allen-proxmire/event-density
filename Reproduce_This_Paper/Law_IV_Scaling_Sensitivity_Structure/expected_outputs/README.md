# Expected Outputs — Law IV (Scaling, Sensitivity, Structure)

This directory contains the **canonical outputs** for:

**ED‑Arch‑25 — Law IV: Scaling, Sensitivity, and Structure**

These outputs allow researchers to:

- verify N‑scaling of recurrence time  
- confirm window‑width scaling  
- validate DECAY‑spacing scaling  
- reproduce ladder‑density scaling  
- confirm universal collapse‑surface convergence  

Subfolders:

- `json/` — canonical JSON reports  
- `figures/` — canonical figures used in the paper  

All files in this directory are generated automatically by:

```
python run_law_IV.py
```
