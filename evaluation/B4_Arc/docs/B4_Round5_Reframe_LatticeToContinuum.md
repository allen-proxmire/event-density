# B4 Round 5 — Reframe: Lattice → Continuum (the layer correction)

**Reframe note. Does NOT rewrite B4, change any discrete result, add a primitive, change a rule, or claim a Maxwell derivation.**
B4 (recon + Steps 1–4) established a discrete topological charge skeleton and a negative result on the "metrical flesh." This note makes one surgical correction: **B4's negative half (Steps 3–4) was looking for a *continuum* object at the *discrete* layer.** Correcting that layer error preserves everything B4 found and converts its dead-end ("ED would have to break its invariants") into a well-posed, ED-native open problem.

---

## 1. What B4 got right (unchanged)

The positive half stands exactly as recorded:

- **Quantized winding.** Single-valued committed U(1) polarity (P09 + P11) gives an integer winding `w ∈ ℤ` (π₁(U(1))=ℤ), confirmed to machine precision.
- **Conserved, protected.** The winding is conserved and cannot slip without an "uncommitting" that P11 irreversibility forbids.
- **Integral Gauss law.** The circulation around any enclosing loop equals `2πw`, independent of loop size/shape — the topological/integral content of Gauss's law.
- **Discrete holonomy structure.** All of the above is the holonomy of a discrete U(1) connection (P05 transport / P09 angle) on the participation graph.

None of this is touched by what follows. This is the substrate-layer object, and it is genuine.

## 2. The category error in Steps 3–4

B4 Step 3 found the *local* field (the per-edge configuration) "gauge/sweep-dependent — undetermined," and Step 4 tried to **force a determined `1/r²` local field on the graph itself**, finding it required breaking *both* orientation-blindness *and* P11 irreversibility — at which point it is no longer ED. Step 4 concluded "the metrical flesh is outside ED by construction."

The correction: **a determined continuous local field is a coarse-grained object by construction, and Steps 3–4 were demanding it at the discrete (substrate) layer.** That is the same layer mistake as demanding a smooth metric on a single event, or a continuous wavefunction on a single commitment. ED is discrete; the continuum is what coarse-graining (DCGT) produces. So the *absence* of a determined continuum field at the substrate layer is **not a deficiency — it is correct.** Step 4's "you'd have to break ED" was an attempt to manufacture a continuum-layer object on the lattice by violating the lattice's own rules. Wrong layer.

## 3. The reframe: the holonomies are lattice gauge link variables

The precise statement. **B4's discrete U(1) holonomies are exactly the link variables of lattice gauge theory.** The winding, the Wilson loops, the integral Gauss law — that is the lattice layer of a U(1) gauge theory. The continuous electromagnetic field — the connection `A_μ`, the field strength `F_μν`, the Coulomb `1/r²` — is the **known continuum limit** of those link variables. So:

> B4 found the **lattice** structure of charge. The continuous Coulomb field is its **continuum (coarse-grained) limit** — it was always supposed to live at the DCGT layer, not on the graph.

This also explains Step 3's "undetermined local field" with no remaining mystery: **per-link configurations in lattice gauge theory are gauge-dependent** (individually unphysical); the physical, *determined* object is the gauge-invariant, action-selected configuration. B4's "sweep/gauge-dependence" is exactly that lattice gauge freedom. It is not a failure of ED — it is the expected redundancy at the discrete layer.

## 4. The coherence term is a latent Maxwell-like action

Why isn't the configuration determined *at the substrate*? Because the physical (continuum) field is selected by an **action**, and orientation-blind Σ supplies no action over the phase — Σ reads ρ and graph only, never the polarity (the hard invariant). So nothing at the micro layer selects the configuration. That is the whole content of Step 3's underdetermination.

But B4 Step 2 already identified the relevant action sitting in the substrate: the **coherence energy** `b_e = cos²(Δφ_e/2)`, equivalently the incoherence `sin²(Δφ_e/2)`, whose continuum form is the `∫(∇φ)²`-type (Maxwell/XY-like) action. **ED has the right action latent in P04's coherence structure; orientation-blind Σ simply does not minimize it at the micro layer.** This is the hinge: the determinacy that Step 4 tried to install by force is the determinacy an *action* supplies — and the action is already present, just not exercised by the micro selection rule.

## 5. The corrected open problem

The selection an action performs in lattice gauge theory happens at the **ensemble / coarse-grained** level: the determined (Coulomb) field is the gauge-invariant *expectation* of the link variables, dominated by low-action (coherent) configurations. So the real, ED-native question is:

> **Does ED's DCGT coarse-graining produce Maxwell/Coulomb as the continuum limit of the discrete holonomies — i.e., is the DCGT measure coherence-weighted, so that the coarse-grained expectation selects the coherence-minimizing (Maxwell-action) configuration?**

If the DCGT measure weights toward coherence, the continuum field emerges as a **coarse-grained statistical fact**, with **orientation-blindness untouched** — because the selection occurs at the ensemble layer, not in the micro rule. This *replaces* Step 4's "ED would have to break its invariants": the field is obtained by coarse-graining (the same machinery ED uses for the emergent metric and the wavefunction shadow), not by breaking ED.

This also softens B4's complementarity line. The correct statement is not "ED's nature forecloses a continuous field," but: **ED's *discrete layer* correctly carries only the skeleton; the field lives one layer up, in the coarse-grained limit.** The "foreclosure" was a foreclosure *at the wrong layer*.

## 6. What remains open (and what is not a deficiency)

- **Open (the right question):** whether ED's DCGT coarse-graining of the holonomies actually yields Maxwell/Coulomb — concretely, whether the DCGT measure is coherence-weighted enough to select the Maxwell-action configuration as the continuum expectation. **This is unproven.** This note makes **no claim that Maxwell is derived.**
- **Not a deficiency:** the absence of a determined continuum field at the discrete layer (Step 3), and the failure to force one there without breaking ED (Step 4). Both are *correct behavior* for a discrete substrate — the continuum field is not a substrate object.
- **Unchanged:** every discrete B4 result (Sections 1). The skeleton — winding, conservation, protection, integral Gauss law — is the genuine substrate-layer content and is not affected.
- **Connection to the program:** this is one instance of the single deepest open thread. The determined continuous structure — **field (here), metric (Phase-3 GR), boundary (A2/emergent horizons)** — is in every case the coarse-grained shadow, and the live question everywhere is the same: *does ED's coarse-graining supply it?* Charge is the cleanest instance because the lattice→continuum dictionary (lattice gauge theory) is already standard.

## Net

B4's discrete result is intact and correct. Its negative half is reinterpreted, not retracted: **Steps 3–4 located the Coulomb field at the discrete layer, where — correctly — it does not live.** Read as lattice gauge theory, B4 found the link-variable (holonomy) structure; the continuous field is its DCGT continuum limit; the latent Maxwell-like action is already present in the coherence term; and the one honest open question is whether ED's coarse-graining measure selects it. That is an ED-native, well-posed problem — and a strict improvement on "ED would have to break itself," which was a layer error.

---

*Round-5 reframe note only. Preserves all B4 discrete results; corrects the layer error in Steps 3–4 (continuum field sought at the discrete layer); reinterprets the holonomies as lattice gauge link variables with the Coulomb field as their DCGT continuum limit; identifies the coherence term as the latent Maxwell-like action; states the corrected open problem (does the DCGT measure yield Maxwell). No new primitives, no rule changes, no Maxwell derivation claimed, no rewrite of B4.*
