# Primes Arc — Finite-Substrate Ceiling Test

**Question:** What does a substrate ontology say about primes? — not what primes *are*, but **where the substrate's own edge is.** A finite-reach/finite-memory substrate (ED's class, by primitives #2–3) can reproduce the integers' **template** layer but provably not the **escape** layer (which open positions are prime = sign of μ). The integers, through the Factor Skyline, are a **ruler** that locates that ceiling — externally certified by Sarnak Möbius disjointness + the sieve parity barrier, not self-graded.

**Discipline:** STRUCTURAL/FORM parallel only. ED does not explain primes; FS does not explain physics. The shared object is the abstract class "finite-memory/zero-entropy dynamical system" and what information it can carry. The 1.700-bit template is the citable invariant; the ~0.26-bit escape is a scale-dependent peak, not a constant. This is a ceiling-LOCATION test — the expected outcome confirms known hardness.

## Status

| Item | File | Status |
|---|---|---|
| Ceiling test (Tests A/B/C + 1.700 anchor) | `primes_test.py` + `docs/Primes_CeilingTest_Results.md` | **Done** |

## Verdict (one line)

**Finite memory saturates the template and fails the escape — exactly the predicted ceiling.** Template info = **1.700 bits** (reproduced exactly); lpf-ladder exact; best finite-memory μ-correlator → 0 (Sarnak-in-class confirmed, ~√(M/N) decay, falsifier not seen); twin density needs the **2C₂ = 1.3203** singular series (escape correlation beyond the template). The integers locate ED's information ceiling where external mathematics places it.

Pairs with **A1** (`Bits/docs/A1_ChannelCapacity_Results.md`): A1 measured the finite-reach ceiling *inside* ED (capacity = 0); this measures the *same* ceiling against an external, math-certified ruler (μ-disjointness). Answers "can ED finite-memory produce an infinite-memory system like primes?" → template yes, escape no; the *no* is load-bearing.
