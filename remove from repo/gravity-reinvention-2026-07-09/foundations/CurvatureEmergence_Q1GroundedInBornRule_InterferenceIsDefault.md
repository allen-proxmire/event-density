# Q1 Grounded in the Born Rule: the Interfering Quadratic Strain Is the Default Observable, Newton Is Its Decohered Diagonal

**Foundations, gravity / curvature-emergence arc. Grounds P14's one load-bearing open commitment (Q1: gravitational strain is quadratic/interfering, `|Σ P|²`, not linear) in the QM keystone's Born rule. Probe: `evaluation/CurvatureEmergence/q1_bornrule_grounding_probe.py`. RESULT: the physical observable is `|P|²` (quadratic, phase-invariant), not `P` (linear, phase-covariant, not observable) — this is the Born/Gleason structural fact, and with ED's native `b = |P|²` (Paper_001) and the kinematic metric sourced by `b`, it forces gravitational strain to be quadratic in amplitude. On a shared bilocal channel (P14 single-carrier: one bandwidth P04 + one phase P09), the observable is `|Σ P|²` WITH interference, and — the key point — the Born rule INVERTS THE BURDEN: the coherent interfering form is the DEFAULT observable, while additive Newton is its DECOHERED special case (probe: averaging over random relative phases collapses `|Σ P|²` to the diagonal `Σ b_i` = Newton). So Q1's interfering form is not a free modeling choice; it is the fundamental Born observable, and the linear-vs-quadratic tension dissolves (Paper_026's Newton = the diagonal). HONEST: the quadratic form + interference-as-default are GROUNDED (inherited from Born + `b=|P|²` + shared channel, all established in ED); the RESIDUAL narrows to the horizon-coherence retention (the `σ` piece, account-tier, already de-risked and QM/gauge-consistent) plus the `a_0` value (inherited). This is a BOUNDED grounding of one form in one established rule, NOT a grand "gravity = QM" unification. P14 is SUBSTANTIALLY DISCHARGED. See [[project_p14_partial_reduction]], [[project_gleason_complementarity_reframe]].**

---

## 1. What Q1 is, and the tension it carried

Q1 is the single open, load-bearing commitment under the whole ED MOND line: gravitational bilocal strain is a *quadratic/interfering* functional of amplitude (`|Σ P|²`, Born-like), not a *linear* functional of source content. The step-3 catch was that the corpus (Paper_026) builds gravitational strain *linearly* (Newton's additive `Φ ∝ GM/R`), and linear field theory has no source-source interference. So the interfering form looked like a free modeling fork one had to simply choose. This note grounds it.

## 2. The Born rule forces quadratic-in-amplitude (phase-invariance)

ED's participation amplitude is `P = √b · e^{iπ}`, so `b = |P|²` (Paper_001): bandwidth *is* the squared amplitude. The QM keystone (the Gleason/Born reconstruction) supplies the structural fact: **the physical, observable quantity is `|P|²` (quadratic), not `P` (linear).** The reason is phase-invariance. Under a global phase `P → e^{iθ}P`, a linear-in-amplitude quantity (`Re P`, or a linear functional `⟨c|P⟩`) *changes* — the probe shows `Re P` running through `0.81, −0.17, −1.29, 0.24` as `θ` varies — so it is not phase-invariant and cannot be a physical observable. The quadratic `|P|²` is invariant (`1.7000` throughout). A linear-in-amplitude gravitational strain is therefore not even a valid observable; the physical strain that sources gravity *must* be quadratic in amplitude. Since ED's kinematic metric is sourced by `b = |P|²` (established), gravitational strain is quadratic by inheritance, not by fiat.

## 3. The linear-vs-quadratic tension dissolves

The tension was a confusion of levels. Paper_026's strain is *linear in mass/bandwidth* (`Σ b_i`), which is *quadratic in amplitude* at the diagonal (`Σ |P_i|²`). So there was never a linear-vs-quadratic conflict at the amplitude level: both are quadratic in amplitude. The real distinction is **diagonal-only (additive) vs the full quadratic form (with off-diagonal interference)**. The probe makes this explicit: `|Σ P|²` splits into the diagonal `Σ b_i` (`= 7.04`, the additive Newtonian source, linear in mass) plus the off-diagonal interference (`= 4.85`, the term Paper_026's linear build dropped). Newton *is* the diagonal of the Born-quadratic form. Paper_026 built the diagonal; the Born rule mandates the full form.

## 4. The key move: the Born rule inverts the burden of proof

Phase-invariance forces quadratic-in-amplitude, but it does not by itself force *interference* — that requires the sources to combine into *one* amplitude before squaring. In ED they do: the P14 single-carrier structure (a channel carries one bandwidth (P04) and one phase (P09), hence one amplitude) means sources sharing the bilocal gravitational channel superpose at the amplitude level, so the observable is `|Σ P|²` with the cross terms present. This inverts the usual burden:

- **The coherent interfering form `|Σ P|²` is the DEFAULT observable** (the fundamental Born quantity on the shared channel).
- **Additive Newton `Σ b_i` is the DECOHERED special case.** The probe confirms it: averaging `|Σ P|²` over random relative phases returns `7.03`, matching `Σ b_i = 7.04` to `0.1%` — the coherent off-diagonal averages to zero under decoherence.

So one does *not* have to argue "why is there interference?" (it is the default Born form); one has to argue "why does Newton hold?" — and the answer is *local decoherence* (the `σ²` suppression of local-local cross terms, from the recast). Interference is fundamental; Newton is emergent from decoherence. That reframing is the substance of the grounding: Q1's interfering form is no longer a free choice, it is what the Born rule gives, with additive Newton the derived limit.

## 5. Honest tiers and verdict

- **GROUNDED (inherited):** (i) strain is quadratic in amplitude — from the Born rule (observable `= |P|²`, phase-invariance) + `b = |P|²` (Paper_001) + kinematic metric sourced by `b`; (ii) on the shared bilocal channel (single-carrier, P04+P07+P09) the observable is `|Σ P|²` with interference; (iii) interference is the default, Newton the decohered diagonal (probe-confirmed).
- **DISSOLVED:** the linear-vs-quadratic tension (Newton = the diagonal of the quadratic form).
- **RESIDUAL (narrowed, not closed):** the open question is no longer "is gravitational strain quadratic/interfering?" (grounded) but "why does a residual *horizon* off-diagonal survive decoherence while local-local ones do not?" — the `σ`-coherence, which is account-tier but already characterized, de-risked, and shown QM/gauge-consistent (`CurvatureEmergence_SigmaAlignment_QMGaugeConsistent`). The MOND scale `a_0 = σ√(b_hor)` remains inherited.
- **NOT CLAIMED:** a grand "gravity = QM" unification. This is a bounded grounding of one specific form (`|Σ P|²`) in one established rule (Born), internal to ED.

**Verdict.** P14's load-bearing commitment Q1 is **substantially discharged**: the quadratic/interfering strain is not a free modeling fork but the **default Born observable** on ED's shared bilocal channel, forced by phase-invariance (`observable = |P|²`) together with ED's native `b = |P|²` and the kinematic metric sourced by `b`. The linear-vs-quadratic tension dissolves — Paper_026's Newton is the *decohered diagonal* of that Born observable — and the burden inverts: interference is fundamental, Newton is emergent from local decoherence. What remains open is no longer the *form* of the strain but a single physical retention question (does the horizon off-diagonal survive decoherence?) plus the `a_0` value: the `σ`-coherence, already account-tier-characterized, de-risked, and QM/gauge-consistent. So the entire MOND line now rests on one narrow, characterized, internally-consistent residual rather than on an un-argued modeling choice. Held to the crank-rail (MOND is a documented over-read trap): this is a grounding-and-reframing result, bounded and inherited, not a closed derivation of MOND and not a unification claim. Together with the μ(x), recast, and QM-consistency results, the P14/MOND arc is now grounded end to end except for the horizon-coherence retention and `a_0`.
