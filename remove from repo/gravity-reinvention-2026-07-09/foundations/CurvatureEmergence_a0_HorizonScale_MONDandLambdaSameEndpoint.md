# a₀ Is a Horizon Acceleration Scale (~c²/R_H ~ cH₀ ~ c²√Λ): MOND's Scale and Dark Energy Are the Same b→0 Cosmic Endpoint

**Foundations, gravity / curvature-emergence arc. Takes on the last inherited number of the ED MOND line: the MOND acceleration `a_0`. Probe: `evaluation/CurvatureEmergence/a0_horizon_scale_probe.py`. RESULT: in the quadratic-strain recast MOND *is* the local-vs-cosmic-horizon interference sector, so the MOND onset (`g_N ~ a_0`) is where the local acceleration falls to the horizon's own acceleration scale, and the only acceleration built from `c` and the horizon radius `R_H` is `c²/R_H = cH_0`. So `a_0 ~ cH_0` is FORCED by the mechanism, converting `a_0` from an inherited fundamental constant into a PREDICTED horizon scale and explaining the famous `a_0 ~ cH_0` coincidence (in ED it is not a coincidence, the horizon is the interference partner). SHARPER ED reading: ED's horizon is the `b→0` SCBU cosmic endpoint, already tied to the cosmological constant (`Θ_ED ≈ 10⁻¹²²`), so `a_0 ~ c²√(Λ/3)` and MOND's scale and dark energy are the SAME `b→0` endpoint scale. HONEST/crank-railed: `a_0 ~ cH_0` numerically is GENERIC to any horizon-MOND (not an ED discovery); the ED contribution is that the MECHANISM forces it and unifies it with Λ; the magnitude matches within an O(1) factor (`a_0/cH_0 = 0.18 ~ 1/5.5`, bracketed by `1/2π` and `1/6`); the exact O(1) factor is NOT derived (numerology risk, flagged). Falsifiable: `a_0` tracks a horizon. NOT a derivation of `a_0`'s number, a grounding of its SCALE and ORIGIN. See [[project_scbu_bridge]], [[project_p14_partial_reduction]].**

---

## 1. Why a₀ is a horizon scale in the recast

The quadratic-strain recast makes MOND the **local-vs-cosmic-horizon interference sector**: the off-diagonal cross-term between the local gravitational field and the horizon reference (`CurvatureEmergence_QuadraticStrainRecast_...`). Newton is the diagonal; MOND is the surviving local-horizon coherence. The MOND *onset* is where that cross-term becomes comparable to the local Newtonian diagonal, `g_N ~ a_0`. Structurally, that is where the local acceleration has fallen to the **horizon's own acceleration scale**: below it, the local field is weaker than the cosmic background it is interfering with, so it can no longer maintain Newtonian coherence and couples to the horizon (MOND). The horizon is ED's `b→0` boundary at radius `R_H`, and the only acceleration one can build from `c` and `R_H` is
$$ a_0 \sim \frac{c^2}{R_H} = c H_0 \qquad (R_H = c/H_0). $$
So `a_0` being a **horizon/cosmological** scale is *forced* by the mechanism, not fitted. This is the deep content of the observed `a_0 ~ cH_0` coincidence: in ED the horizon *is* the MOND interference partner, so the MOND scale is tied to the size of the universe by construction. This addresses the standard puzzle (why is a galaxy-dynamics constant equal to a cosmological one?) at the level of *origin*.

## 2. The numbers, and the honest separation

The probe (Planck `H_0 = 67.4`):

| quantity | value (m/s²) |
|---|---|
| `a_0` (observed) | `1.20×10⁻¹⁰` |
| `cH_0 = c²/R_H` | `6.55×10⁻¹⁰` |
| `c²√(Λ/3)` (de Sitter/Λ horizon) | `5.44×10⁻¹⁰` |

`a_0/cH_0 = 0.18 ~ 1/5.5`, bracketed by the folklore factors `1/2π = 0.159` and `1/6 = 0.167`; `a_0/[c²√(Λ/3)] = 0.22`. So `a_0 ~ cH_0 ~ c²√Λ` within an O(1) factor of a few. Crank-rail, stated plainly:

- **ED-specific (the contribution):** the recast's mechanism *forces* `a_0` to be the horizon acceleration scale (MOND = local-horizon interference). `a_0` cosmological is structural, not fitted.
- **Generic (NOT an ED discovery):** the numerical fact `a_0 ~ cH_0` itself. This is near-automatic for *any* horizon-based MOND, and many frameworks reach it. The ED novelty is the *mechanism* forcing it, not the number.
- **NOT derived (flagged):** the exact O(1) factor (`~1/2π` to `~1/6`). ED gives the *scale*, not the coefficient; claiming the `2π` would be numerology.

## 3. The sharper ED reading: a₀ and Λ are the same b→0 endpoint

ED's horizon is not an arbitrary choice of "which horizon." It is the **`b→0` SCBU cosmic endpoint** (the A1 capacity-zero surface at cosmic scale, `project_scbu_bridge`), which the program *already* ties to the cosmological constant: `Θ_ED ≈ 10⁻¹²²`, the Λ-in-Planck-units magnitude. So the same `b→0` structure that sets dark energy `Λ` sets the MOND horizon. That gives the de Sitter/Λ reading
$$ a_0 \sim c^2\sqrt{\Lambda/3}, $$
which (i) matches observation within the same O(1) factor (`0.22`), (ii) is `~const` (the future/de Sitter horizon does not evolve like the instantaneous Hubble horizon), consistent with the lack of strong observed `a_0` evolution, and (iii) **unifies MOND's acceleration scale and dark energy as one `b→0` cosmic-endpoint scale**. This is the non-generic ED content: `a_0` and `Λ` are not two independent cosmic numbers but the same endpoint, echoing Milgrom's `a_0 ~ c²√Λ` but *derived from* ED's SCBU structure rather than posited. Tier: an *identification* (the `b→0` boundary = the Λ/de Sitter horizon), grounded in ED's existing SCBU-Λ link (`Θ_ED ≈ 10⁻¹²²`), not a from-scratch computation of either number.

## 4. Falsifiable handle

Because `a_0` tracks a cosmic horizon, *which* horizon is a testable question. If it is the instantaneous Hubble horizon (`R_H = c/H`), `a_0(z)` evolves (larger at high redshift, smaller horizon), testable against high-`z` rotation curves / the radial-acceleration relation; standard MOND takes `a_0 = const`. If it is the de Sitter/Λ (future) horizon, `a_0 ~ const`. ED's SCBU reading favors the latter (`a_0 ~ c²√Λ`, `~const`), but either way **`a_0` tracks a horizon**, a distinguishing prediction from "`a_0` is a fundamental constant of nature." Pinning which horizon (hence the `a_0(z)` behavior) is open and is the sharp observational discriminator.

## 5. Honest tiers and verdict

- **GROUNDED (ED-specific):** `a_0` is cosmological/horizon-set, forced by the recast's MOND = horizon-interference mechanism. `a_0 ~ c²/R_H ~ cH_0` is structural. This *explains* why the MOND scale is a cosmic scale.
- **IDENTIFICATION:** ED's `b→0` horizon = the de Sitter/Λ endpoint (from the SCBU-Λ link, `Θ_ED ≈ 10⁻¹²²`), giving `a_0 ~ c²√(Λ/3)` and unifying `a_0` with dark energy `Λ` as one `b→0` scale.
- **ORDER-OF-MAGNITUDE (predicted, matches):** `a_0 ~ cH_0 ~ c²√Λ` within an O(1) factor (`~1/5`).
- **GENERIC (not an ED discovery):** the number `a_0 ~ cH_0` itself.
- **NOT DERIVED (flagged):** the exact O(1) factor (`~1/2π`..`1/6`). ED gives the scale, not the coefficient.
- **FALSIFIABLE:** `a_0` tracks a horizon (redshift dependence if Hubble-horizon; `~const` if Λ-horizon).

**Verdict.** `a_0` is converted from an **inherited fundamental constant** into a **predicted horizon acceleration scale**, because in ED MOND *is* the local-vs-cosmic-horizon interference sector, so its onset is set by the horizon's own acceleration `c²/R_H ~ cH_0 ~ c²√Λ`. The famous `a_0 ~ cH_0` coincidence is explained (structural, not fitted), the magnitude matches within an O(1) factor, and the sharper ED reading unifies `a_0` with dark energy `Λ` as the same `b→0` cosmic-endpoint scale (consistent with `Θ_ED ≈ 10⁻¹²²`). Held hard to the crank-rail: the number `a_0 ~ cH_0` is generic to horizon-MOND and is *not* claimed as an ED discovery; ED's contribution is the *mechanism* that forces it and the *unification* with Λ; and the exact O(1) coefficient is explicitly not derived (that would be numerology). This is a grounding of `a_0`'s **scale and origin**, not a derivation of its number. With this, the ED MOND line is grounded end to end: the interfering strain (Born rule), the recast (Newton+MOND coexistence), the interpolation `μ(x)`, the QM/gauge consistency, and now `a_0`'s cosmological origin, with only the exact O(1) factor and the `a_0(z)` horizon-identification left open. Connects [[project_scbu_bridge]], [[project_p14_partial_reduction]], [[project_curvature_emergence_arc]].
