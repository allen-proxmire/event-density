# Working the Bandwidth-Acceleration Bridge Falsifies the Interference Recast of MOND: σ Cannot Be Both MOND-Sized and Newton-Safe

**Foundations, gravity / curvature-emergence arc. Works the bandwidth↔acceleration bridge that the many-body Newton tension reduced to, and the result is a clean NEGATIVE: the source-horizon interference recast of MOND is falsified as formulated. Probe: `evaluation/CurvatureEmergence/bandwidth_acceleration_bridge_probe.py`. The coherence `σ` that gives MOND (each local mass `σ`-coherent with the horizon) unavoidably gives, by transitivity, an `O(σ²)` interference force between any two masses; and working the onset condition in ANY consistent bandwidth measure forces `σ` far too large for Newton. Both-mass or both-area counting gives `σ ~ 10⁻⁶`, at which a galaxy has `σ²·N ~ 0.3` (Newton fails); the field-energy/acceleration picture (onset at `g_N ~ a₀`) gives `σ ~ O(1)` (two-body Newton violated ~25%); the paper's own §6 gives `σ ~ 0.09`. The only Newton-safe value (`σ ~ 10⁻³⁷`) came from the inconsistent mix (galaxy by mass, horizon by area), which is both a `~10⁶²` units artifact AND breaks the single-carrier premise that produces interference at all. So there is no consistent version. Held to the reverse-crank bar. SURVIVING ROUTE: the companion khronometric SCALAR mode is a different mechanism (a dynamical field, not source-horizon phase coherence) and is not touched by this. See [[project_p14_partial_reduction]], [[feedback_dont_over_bank_falsifications]].**

---

## 1. The tension reduced to the bridge

The many-body Newton tension: coherence is transitive, so if each local mass is `σ`-coherent with the horizon (as the MOND cross-term requires), any two masses are `σ²`-coherent, giving an inter-mass Newton violation `~ σ²·N`. Whether that is safe depends entirely on the value of `σ`, which the MOND onset condition (`cross-term ~ diagonal`) fixes in terms of a bandwidth ratio. So everything reduces to the bandwidth↔acceleration bridge: what bandwidth does the horizon contribute to the cross-term `√(b_loc·b_hor)`, and does the resulting `σ` land Newton-safe?

## 2. Every consistent counting forces σ too large

The cross-term `√(b_loc b_hor)` lives on ONE shared bilocal channel (P14 single-carrier: one channel, one bandwidth), so `b_loc` and `b_hor` must be the *same kind* of degree of freedom. The probe works all the options:

| counting | `b_gal` | `b_hor` | `σ ~ √(b_loc/b_hor)` | galaxy `σ²·N` (`N=10¹¹`) |
|---|---|---|---|---|
| **(A) both by mass** (`M/m_Pl`) | `~10⁵⁰` | `~10⁶¹` | `1.6×10⁻⁶` | `0.27` → **Newton fails** |
| **(B) both by area** (`(R/ℓ_P)²`) | `~10¹¹²` | `~10¹²³` | `1.7×10⁻⁶` | `0.28` → **Newton fails** |
| (X) galaxy mass, horizon area | `~10⁵⁰` | `~10¹²³` | `1.6×10⁻³⁷` | `10⁻⁶³` (safe) — **but not a valid single carrier** |

Both consistent countings give `σ ~ 10⁻⁶`, and a galaxy at star-counting already fails Newton (`σ²·N ~ 0.3`, an order-unity anomalous force between stars). Independently, the field-energy / acceleration picture (strain `S ∝ g²`, onset at `g_N ~ a₀`) gives `cross/diagonal|onset = 2σ√(S_hor/S_loc) ~ 2σ` because `S_hor ~ a₀² ~ S_loc` at the onset (the onset *is* `g_N ~ a₀`, and `a₀` *is* the horizon's acceleration), so `σ ~ 0.5` and even the *two-body* Newton violation `~ σ² ~ 25%` is ruled out. The paper's §6 (`2σ = a₀/cH₀ ≈ 0.18`) gives `σ ~ 0.09`, the same order. Every consistent route puts `σ` between `~10⁻⁶` and `~0.5`, all fatal to Newton.

## 3. The only Newton-safe value is the inconsistent mix

`σ ~ 10⁻³⁷` arises solely from counting the galaxy by mass (`10⁵⁰`) and the horizon by area (`10¹²³`). Because a horizon's area count is the *square* of its mass count, that mix inflates `b_hor` by `~10⁶²` — the entire "headroom" of the retracted resolution, a units artifact, not physics. And a geometric mean of a bulk-mass dof and a boundary-area dof is not a single-carrier channel, so option (X) also breaks the single-carrier premise (`§3` of the paper) that produces interference in the first place. So (X) is incoherent either way: either no headroom (consistent counting) or no interference (inconsistent counting).

## 4. Honest tiers and verdict

- **The transitivity (MOND coherence forces an `O(σ²)` inter-mass violation) is exact.**
- **`σ` from the onset is `~10⁻⁶` (bandwidth, consistent) to `~O(1)` (acceleration); all fatal** — this is order-of-magnitude but robust, and it does not depend on the fuzzy details of the bridge, only on the *ratio* `b_loc/b_hor` being `~10⁻¹¹` in any consistent measure (both counts scale together).
- **The `σ ~ 10⁻³⁷` escape is a units artifact + a single-carrier violation**, not a physical option.
- **The one unexplored (speculative, unbuilt) escape:** the horizon carries many independent phase channels and different local masses couple to different ones, decorrelating local-local while keeping local-horizon. This would dilute MOND and is not demonstrated; barring it, the recast is dead.

**Verdict.** Working the bandwidth-acceleration bridge **falsifies the source-horizon interference recast of MOND as formulated.** The coherence that produces the MOND cross-term unavoidably produces an `O(σ²)` interference force between ordinary masses, and no consistent bandwidth measure makes `σ` simultaneously MOND-sized and Newton-safe: consistent counting gives `σ ~ 10⁻⁶` (galaxy Newton fails), the acceleration picture gives `σ ~ O(1)` (two-body Newton fails), and the only safe value is a `~10⁶²` units artifact that also breaks the single-carrier premise. This is a genuine negative result, held to the same bar as a positive (I attempted the save, it failed adversarial review, and working the bridge confirms the failure is structural, not incidental). **The interference recast is dead; ED's MOND, if viable, is the companion paper's khronometric SCALAR mode** (a dynamical field sourced by the substrate, not a source-horizon phase coherence), which this result does not touch. The μ(x)-limits intuition and a₀-as-a-horizon-scale intuition may survive in that framing, but the specific interference mechanism does not. Consequence: the MOND-from-interference paper (`Paper_MONDfromInterference_...`) must be reframed as a negative result or retracted; the P14 quadratic-strain program's application to MOND is falsified on the many-body Newton test.
