> **STATUS BANNER (2026-07-09): THE CENTRAL MECHANISM OF THIS PAPER IS FALSIFIED.** Working the bandwidth↔acceleration bridge (the many-body Newton tension, §4/§8) shows that the coherence `σ` giving MOND unavoidably gives an `O(σ²)` inter-mass Newton violation, and no consistent bandwidth counting makes `σ` both MOND-sized and Newton-safe (consistent counting → `σ ~ 10⁻⁶`, galaxy Newton fails; acceleration picture → `σ ~ O(1)`, two-body Newton fails; the Newton-safe `σ ~ 10⁻³⁷` was a units artifact that also breaks the single-carrier premise). See `event-density/foundations/CurvatureEmergence_InterferenceMOND_FalsifiedByTheBridge.md`. **The source-horizon interference recast of MOND is dead as formulated.** This paper must be reframed as a NEGATIVE result (a clean falsification of a natural idea) or retracted; the decision is pending. The surviving ED-MOND route is the companion khronometric SCALAR mode, a different mechanism this result does not touch. Read the body below as the falsified proposal.**

---

# The MOND Sector of ED Gravity: Interference-Sourced Off-Diagonal Strain on a Shared Channel, and a₀ as a Cosmic-Horizon Scale

**Allen Proxmire**

**July 2026**

**Series:** Event Density (ED) Generative Papers, gravity arc (curvature emergence)
**Status:** Publication draft (structural consolidation). Consolidates the MOND (low-acceleration) sector of the ED gravity arc: why gravitational strain is an interfering form (the interference from the shared bilocal channel; the quadratic-in-amplitude form definitional, confirmed by the Born rule), how Newton (diagonal) and MOND (off-diagonal, local-vs-horizon interference) are meant to coexist and the unresolved tension in doing so, the interpolation function that results, the consistency of the required phase alignment with the QM/gauge sector, and why the MOND scale a₀ is (as a scale) a cosmic-horizon acceleration, conjecturally the same endpoint as dark energy. Standalone; cold-reader accessible. Companion to the kinematics paper (Relational Gravity: the emergent metric g~1/b) and the khronometric-dynamics paper.
**Repository target:** `physics-papers/gravity/` (ED-Generative)

---

## Preamble: What This Paper Does NOT Claim *(written first per QC discipline; abstract reconciled against this)*

1. **This is not a derivation of MOND from nothing.** It does *not* derive MOND's phenomenology from a vacuum, and it does not derive a₀'s number. What it does: (i) the strain being *quadratic in amplitude* is definitional (`b = |P|²`), with the Born rule/phase-invariance only *confirming* that a linear-in-amplitude strain is inadmissible; (ii) the *interference* (the off-diagonal cross-term, the actual MOND content) follows from the **shared bilocal channel** (P14 single-carrier), an input, **not** from the Born rule; (iii) the *scale* of a₀ is a cosmic-horizon acceleration. The MOND arc is a documented over-read trap in this program (multiple past retractions); every claim here is tiered accordingly, and the interference is credited to the shared-channel input, not to the Born rule.

2. **The load-bearing residual is account-tier, and it carries an unresolved tension.** The whole MOND effect rests on one physical retention: a small universal phase alignment `σ` of local participation toward the cosmic-horizon reference, so that local-horizon interference (MOND) survives while local-local interference (which would break Newton) is suppressed. This `σ`-alignment is *motivated* (the horizon is the global boundary all local participation shares) but is *not derived*; it is account-tier. **Its sign/direction (horizon-ward, giving an attractive effect) is also part of the assumption.** And there is an unresolved tension (§4, §8): the local-local Newton violation scales as `σ²·(N−1)` in the number of separate sources `N`, so many-body Newton needs `σ` extremely small, while `σ` must also be large enough to produce the MOND term. Whether one `σ` does both is **open and potentially a showstopper**; it is not established here.

3. **a₀ ~ cH₀ numerically is generic, not an ED discovery.** The near-equality of the MOND acceleration and the cosmic horizon acceleration is reached by many horizon-based MOND frameworks and is close to automatic once MOND is tied to a horizon. The ED contribution is the *mechanism* that forces a₀ to be a horizon scale (MOND = local-vs-horizon interference), plus the *conjectured* identification of a₀ with dark energy Λ via the same `b→0` endpoint. The bare number is not claimed as novel.

4. **The exact O(1) factor in a₀ is not derived.** Observed `a₀/cH₀ ≈ 0.18`, an undetermined O(1) of order a few. ED gives a₀'s *scale* (`c²/R_H`), not its coefficient; deriving the coefficient (whether it is `1/2π`, `1/6`, or anything else) would be numerology and is explicitly avoided.

5. **a₀'s scale is dimensionful (`c²/R_H`); `σ` is at most a dimensionless coefficient.** The microscopic `σ√(b_hor)` expression is dimensionless (a channel count times a phase resultant) and *cannot* by itself be an acceleration; the dimensionful scale is the horizon acceleration `c²/R_H`. So `σ` (and the horizon bandwidth) can at most set the undetermined O(1) coefficient (item 4), not the scale, and the dimensional bridge is not worked out here.

6. **This is not a "gravity = QM" unification.** The Born rule is used only to confirm the (definitional) quadratic-in-amplitude form and to rule out a linear-in-amplitude observable; it is a bounded structural inheritance, not a claim that gravity and quantum mechanics are one theory.

7. **Conditional on the companion papers and on P14's single-carrier structure.** The gravity here is ED's *kinematic-metric* gravity (companion kinematics paper). The interference requires sources to share one bilocal channel (P14: one channel carries one bandwidth (P04) and one phase (P09), hence one amplitude); that single-carrier structure is corpus-native but is an *input* here and does the load-bearing work. **The relationship between this interference mechanism and the companion khronometric paper's "MOND lives in the scalar/khronon mode" is not established (§8); the two are not yet shown to be the same object.**

8. **No new primitive is introduced.** `σ` is a value of the existing phase (P09) relative to the existing horizon (the `b→0` SCBU boundary).

---

## Abstract

ED gravity reduces to Newton at high acceleration and to MOND at low acceleration (companion papers). This paper examines the *low-acceleration (MOND) sector*, which had rested on one un-argued modeling choice (that gravitational strain is quadratic/interfering rather than linear) and one inherited number (the MOND scale a₀). **Strain is quadratic in amplitude by definition** (`b = |P|²`, Paper_001); the Born rule/phase-invariance only *confirms* this and rules out a linear-in-amplitude observable (`P` is phase-covariant, not physical), which is a modest, near-definitional contribution. **The interference, the actual MOND content, comes from the shared bilocal channel** (P14 single-carrier: sources on one channel add their amplitudes before squaring, `|Σ P|²`), an input, not from the Born rule. The one genuine clarification is that this dissolves the "linear-vs-quadratic" confusion: Paper_026's Newton (linear in mass) *is* the diagonal `Σ b_i` of the quadratic form, and the off-diagonal `Σ_{i≠j}√(b_i b_j)cos Δπ` is the interference the linear build dropped. **Newton (diagonal) and MOND (off-diagonal, local-vs-horizon interference) then have to coexist.** The naive picture fails a real obstruction (one phase-randomness setting kills both Newton-safety and MOND), addressed by a coherence *asymmetry*: a small universal phase alignment `σ` toward the horizon reference gives local-local coherence `σ²` (Newton-breaking) while local-horizon coherence is `σ` (MOND), an exact probability identity. **A serious tension remains, disclosed here and not resolved:** the local-local Newton violation scales as `σ²·(N−1)` in the number of separate sources, so many-body Newton needs `σ` extremely small, while `σ` must be large enough to give MOND; whether one `σ` does both (via the large horizon bandwidth) is open and potentially a showstopper. **The `σ`-alignment is QM/gauge-consistent** (a source-horizon *relative*-phase coherence, gauge-invariant, invisible to co-local double-slit interference; local gauge invariance and the photon mass untouched). **The interpolation function**, with strain as field energy (`S ∝ g²`) and the coherence factor absorbed into a₀, is `μ_ED(x) = [√(1+4x²)−1]/(2x)`: monotone, with correct Newton (`μ→1`) and deep-MOND (`μ→x`) limits, near the data-favored "simple" and "standard" forms (exact shape model-dependent). **The scale a₀ is a cosmic-horizon acceleration.** Because MOND is the local-vs-horizon interference sector, its onset is where the local acceleration falls to the horizon's own scale, and the only acceleration built from `c` and the horizon radius is `c²/R_H = cH₀`; so a₀ ~ cH₀ is forced *as a scale* by the mechanism (converting a₀ from an inherited constant to a predicted horizon scale). The dimensionless O(1) coefficient (`a₀/cH₀ ≈ 0.18`) is *not* derived. ED's `b→0` horizon is the SCBU cosmic endpoint, tied to the cosmological constant (`Θ_ED ≈ 10⁻¹²²`), so ED *conjectures* a₀ ~ c²√(Λ/3), identifying MOND's scale and dark energy Λ as the same `b→0` endpoint. The honest verdict: ED's MOND sector is grounded in its *forms, scales, and origins* (the interference form from the shared channel; a valid μ(x); QM/gauge consistency; a₀'s cosmological scale) **except for one load-bearing account-tier assumption (the `σ`-retention), on which the entire effect rests and which carries an unresolved many-body-Newton tension**, plus the undetermined O(1) factor in a₀. The number a₀~cH₀ is generic to horizon-MOND, not an ED discovery. This is a candidate mechanism with a disclosed potential showstopper, not a proven theory.

---

## 1. Introduction: Can the MOND Sector Be Grounded, or Only Housed?

The companion papers place ED gravity in the khronometric class (a preferred-frame Lorentz-violating gravity) with a GR-like tensor sector and one extra scalar mode, and they identify that scalar as the home of galactic MOND: ED is GR at high acceleration, MOND at low. But "housing" MOND in a sector is not the same as *reproducing* it. Two things had to be granted: that gravitational bilocal strain is a *quadratic/interfering* functional of amplitude (so that a local source can interfere with the cosmic horizon, the MOND cross-term), and that the MOND scale a₀ is what it is. The first was an un-argued modeling fork; the second was an inherited number. This paper grounds both, to the extent honest tiering allows, and isolates precisely what remains open.

The result is a MOND sector grounded in *some* of its forms, scales, and origins, with the load-bearing work resting on two inputs: the interfering strain follows from the shared bilocal channel (§3, *not* from the Born rule, which only confirms the definitional quadratic-in-amplitude form); Newton and MOND coexist via a coherence asymmetry (§4) that is QM/gauge-consistent (§5) but carries an unresolved many-body tension; the interpolation function has the right limits, model-dependent shape (§6); and a₀'s *scale* is a cosmic-horizon acceleration (§7). Sections 8-10 are limitations, falsifiers, and the position statement. Throughout, the crank-rail is held hard: this is a grounding of some forms and scales, resting on one account-tier assumption with a disclosed potential showstopper, not a derivation of MOND's phenomenology or a₀'s number.

## 2. Load-Bearing Step Audit

| Step | Status | Source / justification |
|---|---|---|
| Participation amplitude `P = √b e^{iπ}`, so `b = |P|²` | P / established | Paper_001 |
| Strain is quadratic in amplitude (not linear-in-amplitude) | **definitional** (`b=|P|²`); Born only confirms | §3; phase-invariance rules out a linear-in-amplitude observable |
| The **interference** (off-diagonal, `= |Σ P|²`): sources add amplitudes before squaring | **input (shared channel), NOT from the Born rule** | §3; P14 single-carrier (P04+P07+P09); this does the load-bearing work |
| Dissolves the "linear-vs-quadratic" confusion (Newton = the diagonal) | **clarification** | §3; Paper_026's linear-in-mass Newton = the quadratic-form diagonal |
| Newton = diagonal `Σ b_i` (additive, linear in mass) | **definitional** | §3-4 |
| Naive recast fails (one phase setting kills both Newton-safety and MOND) | **demonstrated** | §4; probe |
| Resolution: local-local coherence `σ²`, local-horizon `σ` (asymmetry) | **exact identity** | §4; `⟨cos(π_i−π_j)⟩ = |⟨e^{iπ}⟩|² = σ²`, `⟨cos(π_i−0)⟩ = σ` |
| Many-body Newton safety (`σ²·(N−1)` must stay small while `σ` gives MOND) | **OPEN / potential showstopper** | §4, §8; not established; the central unresolved tension |
| `σ`-alignment QM/gauge-consistent | **demonstrated** | §5; gauge-invariant relative phase, double-slit unchanged |
| Interpolation `μ_ED(x) = [√(1+4x²)−1]/(2x)`, correct limits, monotone | **derived given 3 inputs** (`S∝g²`, isotropy-drop, `2σ` absorbed into a₀) | §6; exact shape model-dependent |
| a₀'s **scale** is the horizon acceleration `c²/R_H ~ cH₀` | **grounded (mechanism-forced), scale only** | §7; MOND = local-horizon interference |
| a₀'s dimensionless O(1) coefficient (`a₀/cH₀ ≈ 0.18`); the `σ√b_hor`→acceleration bridge | **NOT DERIVED** | §7, preamble 4-5; dimensional bridge not worked out |
| a₀ and Λ are the same `b→0` endpoint (`a₀ ~ c²√(Λ/3)`) | **conjecture / identification** | §7; SCBU-Λ link `Θ_ED ≈ 10⁻¹²²`, not used to compute a₀ |
| The `σ`-retention (why the horizon off-diagonal survives; its sign/direction) | **account-tier (open); the whole effect rests on it** | §4, §8; motivated, not derived |
| Identification of the interference sector with the companion khronometric scalar mode | **OPEN / unshown** | §8; two different MOND mechanisms, not yet bridged |
| a₀ ~ cH₀ as a *number* | **generic, not an ED discovery** | §7, preamble 3 |
| MOND derived from nothing / gravity=QM unification | **NOT CLAIMED** | preamble 1, 6 |

The load-bearing work is done by two inputs (the shared bilocal channel and the account-tier `σ`-retention), not by the Born rule; the Born rule's role is the near-definitional confirmation that strain is quadratic in amplitude. Two items are flagged OPEN / potential-showstopper (many-body Newton, the khronometric-scalar identification).

## 3. Where the Interfering Form Comes From (and Where It Does Not)

Q1, the load-bearing modeling commitment, is that gravitational strain is quadratic/interfering (`|Σ P|²`), not linear. It is important to attribute each part correctly, because the Born rule does *less* work here than it might appear.

**Quadratic-in-amplitude is definitional, and the Born rule only confirms it.** ED's participation amplitude is `P = √b · e^{iπ}` (Paper_001), so `b = |P|²`: bandwidth *is* the squared amplitude, by definition. The QM keystone's phase-invariance argument adds only that a *linear*-in-amplitude quantity cannot be a physical observable: under a global phase `P → e^{iθ}P`, `Re P` runs through `0.81, −0.17, −1.29, 0.24` (probe) while `|P|²` stays fixed. So a linear-in-amplitude gravitational strain is inadmissible. But this confirms the definition; it does not supply anything new, and it does not by itself force *interference*.

**The interference comes from the shared channel, not the Born rule.** Phase-invariance says the observable is built from `|amplitude|²`; it is *silent* on whether N sources add *coherently* (`|Σ P_i|²`, with cross terms) or *incoherently* (`Σ |P_i|²`, diagonal only). That is decided by whether the sources share one channel. In ED they do, by the P14 single-carrier structure (one channel carries one bandwidth (P04) and one phase (P09), hence one amplitude `Σ P_i`), so the sourcing quantity is `b_tot = |Σ P_i|²` *with* interference. **This shared-channel input is the load-bearing step, and it is an input, not a consequence of the Born rule.** The credit for the interference belongs to the single-carrier channel structure (corpus-native in P04+P07+P09), not to the QM keystone.

**The one genuine clarification.** What the quadratic-form view *does* settle is the "linear-vs-quadratic" confusion. Paper_026 builds gravitational strain linearly *in mass* (`Σ b_i`), which is *quadratic in amplitude at the diagonal*. So there was never a conflict at the amplitude level: `|Σ P|²` splits into the diagonal `Σ b_i` (`= 7.04` in the probe, additive Newton, linear in mass) plus the off-diagonal interference (`= 4.85`, the term Paper_026 dropped). Paper_026 built the diagonal; the shared channel adds the off-diagonal. Newton and MOND are the diagonal and off-diagonal of one form. (A probe also shows that averaging `|Σ P|²` over random relative phases returns the diagonal `Σ b_i` to `0.1%`, but this is the tautology that random phases wash out cross terms, shown for illustration, not a confirmation of anything.) The physics question is therefore *not* "why is there interference?" but "why is the off-diagonal negligible everywhere Newton holds, yet nonzero against the horizon?", which is the coherence structure of §4.

## 4. Newton (Diagonal) and MOND (Off-Diagonal) Coexist via a Coherence Asymmetry

Write the quadratic form for local sources plus the cosmic horizon:
$$ b_{\text{tot}} = \Big|\sum_i P_i\Big|^2 = \underbrace{\sum_i b_i}_{\text{diagonal = Newton}} + \underbrace{\sum_{i\neq j}\sqrt{b_i b_j}\,\cos(\pi_i-\pi_j)}_{\text{off-diagonal = interference}}. $$

**The obstruction (confronted).** The off-diagonal must vanish for local-local pairs (or the solar system shows anomalous source-source forces, unobserved to `~10⁻⁸`) yet survive for local-horizon pairs (to give MOND). A single "phases are random" setting kills both: random enough to wash out local-local is random against the horizon too, so MOND dies; full coherence breaks Newton. A probe confirms both failures. So the naive recast fails along the whole coherence axis.

**The partial resolution (an exact identity).** Give the local phases a small common bias `σ = |⟨e^{iπ}⟩|` toward the horizon reference (phase 0; the horizon-ward *direction* fixes the attractive sign of the effect and is itself part of the assumption). Then, for independent phases,
$$ \underbrace{\langle\cos(\pi_i-\pi_j)\rangle_{i\neq j} = |\langle e^{i\pi}\rangle|^2 = \sigma^2}_{\text{local-local (Newton-breaking)}}, \qquad \underbrace{\langle\cos(\pi_i-0)\rangle = \sigma}_{\text{local-horizon (MOND)}}. $$
The Newton-breaking local-local term is *quadratically* suppressed relative to the MOND-giving local-horizon term (probe-confirmed to three decimals across `σ ∈ [0.025, 0.81]`). This asymmetry is the mechanism, and it is exact.

**The unresolved tension, and why it is entangled with the dimensional bridge (disclosed, not solved).** Coherence is *transitive*, so the suppression is not by `σ²` alone: if each local mass is `σ`-coherent with the horizon (as MOND requires), any two are mutually `σ²`-coherent, and the local-local off-diagonal sums over all pairs, scaling as `σ²·(N−1)` in the number `N` of separate sources. Many-body Newton then requires `σ²·(N−1) ≪ 1`, while the MOND term `σ·√(b_hor/b)` must be O(1). The critical point is that **the value of `σ` is not free but is not consistently determined either**, and the two constraints pull it to numerically incompatible values:

- The paper's own §6 (deep-MOND normalization, `2σ = a₀_obs/a₀_horizon ≈ a₀/cH₀ ≈ 0.18`) needs **`σ ≈ 0.09`**, at which `σ²·N` fails for `N ≳ 100`.
- An attempted rescue that fixes `σ` from the onset (`σ² ~ b_onset/b_hor`) and appeals to a huge horizon bandwidth gives **`σ ~ 10⁻³⁷`** only by counting the horizon holographically (area, `(R_H/ℓ_P)² ~ 10¹²³`) while counting the local system by mass (`M/m_Pl ~ 10⁵⁰`); a horizon's area count is the *square* of its mass count, so that mismatch is a spurious `~10⁶²` inflation. Counted consistently (both by mass), `σ ~ 10⁻⁶` and a galaxy at star-counting already gives `σ²·N ~ 0.3`, i.e. Newton fails.

So the `σ` that would protect Newton and the `σ` that sets `a₀` differ by many orders, and which value is correct hinges on the **unworked bandwidth↔acceleration bridge** (§7, preamble 5). The many-body tension and that dimensional gap are therefore *one* problem, not two, and until the bridge is worked in a single consistent bandwidth measure (same for the local source and the horizon) that both reproduces the fixed-acceleration universal MOND onset and lands `σ` in a Newton-safe range, **Newton-safety is not established. This is the mechanism's central open problem and a genuine potential showstopper**, stated as such rather than buried. What *is* clean: the strong-field limit is safe regardless (when `g_N ≫ a₀` the cross-term is `≪` the diagonal, §6), and the within-particle case is safe (the two paths of one particle share its alignment, which cancels; §5), but neither addresses the between-mass many-body sum.

## 5. The σ-Alignment Is Consistent with the QM/Gauge Sector

A universal preferred phase could break QM or the gauge program. It does not, for a structural reason: **`σ` is a source-horizon *relative*-phase coherence.** A probe demonstrates three consistency facts.

- **Gauge-invariant.** Under a global rotation `π → π + θ`, source and horizon shift equally, so `σ`, the double-slit visibility, and the MOND cross-term are all invariant. The global phase stays unobservable; there is no global-U(1) breaking.
- **Invisible to local interference.** Two co-local double-slit paths share the same horizon bias, which cancels in their relative phase, so visibility is unchanged (`0.9606` across the full `σ` range, identical to the no-bias value; the contrast case with independent biases collapses, proving the cancellation is load-bearing). The `σ` cross-term is `~0` and `σ`-independent for co-local pairs, and tracks `σ` only for local-vs-horizon pairs. So the effect is confined to the cosmological-scale relative phase = MOND, inaccessible to laboratory physics.
- **Local gauge invariance untouched.** A global reference is not a local rotation, so the photon stays massless and the non-abelian gauge structure is unaffected. The `σ`-reference is consonant with ED's existing preferred-frame structure (the arrow, the SCBU horizon), not a new symmetry breaking.

One physical input, the local smoothness of the horizon bias, does both the QM-safety (co-local cancellation) and the Newton-safety (`σ²` suppression) jobs. Residual (flagged): a possible tiny preferred-phase effect on a photon propagating against the global reference, argued not a local-gauge breaking and `σ`-suppressed, not fully worked out.

## 6. The Interpolation Function

The exact interpolation shape rests on *three* model inputs, stated up front: (M1) strain as field energy (`S ∝ g²`, so the local strain is `g_N²` and the horizon strain scale is `a₀²`); (M2) the isotropy-drop of the horizon self-term (its diagonal sources no net force, so only the cross-term survives); and (M3) absorbing the coherence factor `2σ` into the definition of a₀ (the deep-MOND normalization, which is where a σ-dependence hides inside a₀; §7). Given these, the cross-term is the geometric mean `√(g_N² a₀²) = g_N a₀`, exactly the deep-MOND relation, and diagonal dominance gives Newton. Inverting `g² = g_N² + g_N a₀` (`μ(x) = g_N/g`, `x = g/a₀`) gives
$$ \mu_{\text{ED}}(x) = \frac{\sqrt{1+4x^2}-1}{2x}, $$
monotone, bounded in `(0,1)`, with `μ→x` (deep MOND) and `μ→1` (Newton) both exact (probe-verified). It sits near the data-favored "simple" `x/(1+x)` (within `0.12`) and "standard" `x/√(1+x²)` (within `0.11`) forms, both acceptable for rotation curves. The sign-indefinite phase `cos Δπ` that Paper E flagged is **regime-separated**: a probe with `cos Δπ` genuinely negative at high acceleration still gives `μ→1` there (the diagonal dominates), so the phase is only sign-indefinite where it is irrelevant, and where MOND lives it is coherent `≈ +1`. Honest tier: only the two limits and monotonicity are robust (forced by the cross-term = geometric mean of squared accelerations); the exact transition shape is model-dependent (a second strain mapping `S ∝ g` gives the same limits, a different mid-curve), so `μ_ED` is one member of a family, not a pinned prediction.

## 7. a₀'s Scale Is a Cosmic-Horizon Acceleration (Conjecturally the Dark-Energy Endpoint)

The last inherited number, and the one where the honest result is *scale only*. Because ED's MOND *is* the local-vs-cosmic-horizon interference sector (§4), the MOND onset (`g_N ~ a₀`) is where the local acceleration falls to the **horizon's own acceleration scale**. The horizon is ED's `b→0` boundary at `R_H`, and the only acceleration one can build from `c` and `R_H` is `c²/R_H = cH₀`. So
$$ a_0 \sim \frac{c^2}{R_H} = cH_0 \qquad (\text{scale, forced by the mechanism}). $$
a₀ is a horizon scale, not a new local constant. This converts a₀ from an inherited constant into a predicted horizon *scale*.

**A dimensional caveat, load-bearing.** The microscopic expression from the recast, `a₀ ~ σ√(b_hor)`, is *dimensionless* (a phase resultant times a channel count) and cannot be an acceleration; the dimensionful scale is `c²/R_H`. So `σ` (and `b_hor`) can at most enter the **dimensionless O(1) coefficient** `a₀/cH₀`, not the scale, and the bridge from the microscopic `σ√(b_hor)` to the macroscopic `c²/R_H` is *not worked out here*. This is the same `σ` that must be tiny for many-body Newton (§4), so the value of the coefficient and its consistency with the Newton bound are part of the unresolved tension, not a clean prediction. Numerically the coefficient is `a₀/cH₀ ≈ 0.18`, an undetermined O(1) of order a few; ED gives the scale, not this coefficient.

**A conjectured sharper reading: a₀ and Λ as one endpoint.** ED's `b→0` horizon is the SCBU cosmic endpoint, which the program elsewhere ties to the cosmological constant (`Θ_ED ≈ 10⁻¹²²`, inherited and not used to compute a₀ here). If that identification holds, the de Sitter/Λ reading `a₀ ~ c²√(Λ/3)` (`= 5.4×10⁻¹⁰`, coefficient `0.22`) applies and is `~const` (the future horizon does not evolve like the instantaneous Hubble horizon), consistent with the lack of strong observed a₀ evolution. ED then *conjectures* that MOND's acceleration scale and dark energy Λ are the same `b→0` cosmic-endpoint scale, echoing the known `a₀ ~ c²√Λ` relation but from ED's SCBU structure. This is an identification/conjecture (tier), not a computed link.

Crank-rail, stated plainly: the number a₀~cH₀ is *generic* to any horizon-MOND and is **not** an ED discovery; the ED contribution is only that the mechanism forces a₀ to be a horizon *scale* (and the conjectured Λ-identification). The dimensionless coefficient is not derived, and deriving it (whether `1/2π`, `1/6`, or otherwise) would be numerology and is avoided.

## 8. Limitations (honest)

- **Many-body Newton safety is the central open problem (potential showstopper), and it is one problem with the dimensional bridge.** By coherence transitivity the local-local Newton violation scales as `σ²·(N−1)` (§4). The value of `σ` that would make this safe (`≪ 1/√N`) is in direct numerical conflict with the `σ` the paper's §6 needs to set a₀ (`≈ 0.09`), and an attempted rescue giving `σ ~ 10⁻³⁷` was found to be a units artifact (horizon counted by area, galaxy by mass; consistent mass-counting gives `σ ~ 10⁻⁶`, at which a galaxy fails). So `σ` is undetermined across a many-order range spanning "safe" to "fatal," fixed only by the **unworked bandwidth↔acceleration bridge** (§7). The many-body tension and that bridge are the same open problem, and until it is worked in a single consistent bandwidth measure that both reproduces the fixed-acceleration universal MOND onset and lands `σ` Newton-safe, the Newton+MOND coexistence is **not established** for realistic many-body systems. This is the most likely place the whole mechanism fails.
- **The `σ`-retention is account-tier and load-bearing.** The whole MOND effect rests on a small universal horizon-aligned phase bias `σ` (magnitude *and* horizon-ward direction/sign), motivated (the horizon as the global common reference) but not derived from the primitives. Everything downstream is conditional on it. It has been de-risked (QM/gauge-consistent, §5) but not grounded.
- **The relationship to the companion khronometric MOND is unshown.** The khronometric paper houses MOND in a dynamical scalar (khronon) mode; this paper houses it in the off-diagonal interference term of a kinematic-metric strain. Whether these are the same object (is the cross-term the scalar field? is `σ` the khronon configuration? is a kinematic-metric read-out even compatible with a dynamical tensor+scalar split?) is not shown. The two MOND mechanisms are presently bolted together under "conditional on the companions," which is too thin a hedge for what may be an inconsistency; the identification is **open**.
- **a₀ is a scale, not a number.** The dimensionless O(1) coefficient is not derived, the `σ√b_hor → c²/R_H` bridge is not worked out, and the a₀~cH₀ magnitude is generic, not novel (§7).
- **The exact μ(x) shape is model-dependent** (three inputs, §6); only the limits and monotonicity are robust.
- **Conditional on the framework:** ED's kinematic-metric gravity (companion papers) and the P14 single-carrier shared channel, which does the load-bearing work for the interference. The strong-field regime (where the kinematic-metric picture may break) is separately speculative.
- **Residual EM-sector consistency** (a photon against the global phase reference, §5) is flagged, not resolved.
- **This is a candidate mechanism with a disclosed potential showstopper, not a proven theory:** grounded in some forms, scales, and origins, but resting on the account-tier `σ`-retention (with the unresolved many-body tension) and the un-bridged relation to the companion khronometric scalar.

## 9. Falsification Criteria

### 9.1 Gravitational strain is not interfering

**Falsifier sentence:** *A demonstration that ED's gravitational strain is not a quadratic/interfering observable (that a linear-in-amplitude gravitational strain is admissible, contrary to phase-invariance, or that the sources do not share a bilocal channel so no cross term exists) would remove the interference and with it the entire MOND sector (§3-4).*

### 9.2 The coherence asymmetry does not hold

**Falsifier sentence:** *A demonstration that no single `σ` can be simultaneously small enough to hold the many-body Newton violation `σ²·(N−1)` below observational bounds and large enough to source the MOND term would falsify the Newton+MOND coexistence (this is the mechanism's central open tension, §4, and its most likely failure mode). Equally, a detected source-source interference correction to Newton at the `σ²·(N−1)` level, or a demonstration that the σ²-vs-σ per-pair asymmetry cannot survive the sum over sources, would falsify it.*

### 9.3 The σ-alignment breaks QM/gauge

**Falsifier sentence:** *A detected effect of the horizon phase reference on local (co-local) interference or on gauge observables (a σ-dependent double-slit visibility, an observable global phase, a photon mass) would falsify the QM/gauge consistency (§5) and kill the mechanism.*

### 9.4 The interpolation function is wrong

**Falsifier sentence:** *Galactic rotation curves requiring an interpolation function outside the family bracketed by the simple and standard forms, or requiring particle dark matter rather than the low-acceleration scalar sector, would falsify the μ(x) result (§6).*

### 9.5 a₀ does not track a horizon

**Falsifier sentence:** *A measured a₀ that neither matches the cosmic-horizon acceleration scale (`~cH₀ ~ c²√Λ`) within an O(1) factor nor tracks a horizon (constant if the de Sitter/Λ horizon; redshift-evolving if the Hubble horizon) would falsify the a₀-as-horizon-scale result (§7). In particular, high-redshift rotation-curve / radial-acceleration data pinning a₀(z) discriminates the Hubble-horizon and Λ-horizon readings.*

## 10. Position Statement

**ED's MOND sector is the off-diagonal (interference) part of a quadratic gravitational strain on a shared bilocal channel, with Newton the diagonal, and its acceleration scale a₀ is a cosmic-horizon acceleration.** Gravitational strain is quadratic in amplitude by definition (`b = |P|²`), which the Born rule/phase-invariance only confirms; the *interference* (the MOND content) comes from the shared-channel (single-carrier) input, not from the Born rule. Newton (diagonal) and MOND (off-diagonal) then have to coexist, addressed by a σ²-vs-σ coherence asymmetry (a small horizon-aligned phase bias suppresses local-local relative to local-horizon), which is QM/gauge-consistent because σ is a gauge-invariant source-horizon relative phase invisible to local interference. But the coexistence carries an unresolved many-body tension (the local-local term scales as `σ²·(N−1)`), which is the central open problem. The resulting interpolation function has the correct Newton and deep-MOND limits (exact shape model-dependent), and a₀'s *scale* is the cosmic-horizon acceleration `c²/R_H ~ cH₀`, forced by the mechanism, with the dimensionless coefficient undetermined and the identification of a₀ with dark energy Λ a conjecture.

**What this paper claims.** The interference form (from the shared-channel input, not the Born rule; the Born rule only rules out a linear-in-amplitude observable), the dissolution of the linear-vs-quadratic confusion (Newton = the diagonal), the σ²-vs-σ coherence asymmetry (an exact identity), the QM/gauge consistency of σ (demonstrated), the interpolation function's correct limits (monotone; shape model-dependent), and a₀'s cosmological *scale* (mechanism-forced). Grounded in some forms, scales, and origins, with the load-bearing work resting on two inputs (the shared channel and the σ-retention).

**What this paper does not claim.** MOND derived from nothing; the interference grounded *in the Born rule* (it is grounded in the shared-channel input); "grounded end to end" (the whole effect rests on the account-tier σ-retention, which additionally carries an unresolved many-body-Newton tension and a dimensional gap); a₀'s number or its O(1) coefficient (not derived; a₀~cH₀ is generic to horizon-MOND); the a₀-Λ identification as established (a conjecture); a "gravity = QM" unification; or that the interference sector is the same object as the companion khronometric scalar mode (unshown). It is a candidate mechanism with a disclosed potential showstopper, not a proven theory.

**Series context.** The MOND-sector companion to the kinematics paper (Relational Gravity: the emergent metric g~1/b, and the identification of the nonlinearity as MOND-resembling) and the khronometric-dynamics paper (which houses MOND in the scalar/khronon sector). Where those establish the geometry and the dynamical class, this proposes the MOND sector's mechanism (local-vs-horizon interference on a shared channel), its interpolation, and its scale (the horizon), while flagging that the identification of this interference sector with the khronometric scalar mode is not yet shown. It draws on the SCBU boundary (the `b→0` cosmic endpoint), conjecturally tying the galactic (MOND) and cosmological (Λ) sectors to one horizon.

---

## Appendix: Glossary and Reader Map

### Glossary

- **Quadratic / interfering strain.** Gravitational strain as `|Σ P|²`, not a linear sum. Diagonal `Σ b_i` = Newton; off-diagonal interference = MOND. The quadratic-in-amplitude form is definitional (`b=|P|²`); the interference (the off-diagonal) comes from the shared channel, not the Born rule.
- **Born rule (here).** The physical observable is `|amplitude|²` (quadratic, phase-invariant), not the amplitude (linear, not observable). Applied to ED's `b = |P|²` this only confirms the definitional quadratic form; it does **not** force the *interference*, which comes from the shared bilocal channel (single-carrier).
- **σ (horizon-aligned phase bias).** A small universal alignment of local participation phases toward the cosmic-horizon reference. Gives local-local coherence `σ²` (Newton) and local-horizon coherence `σ` (MOND). Account-tier; sets at most the dimensionless O(1) coefficient of a₀ (not a₀'s scale, §7), and is bounded by the many-body-Newton tension (§4).
- **Coherence asymmetry.** The exact identity that local-local coherence (`σ²`) is quadratically smaller than local-horizon coherence (`σ`). It lets Newton and MOND coexist per-pair, but the local-local term sums as `σ²·(N−1)` over sources, the unresolved many-body tension (§4).
- **μ_ED(x).** The interpolation function `[√(1+4x²)−1]/(2x)`; correct Newton and deep-MOND limits; near (not identical to) the simple and standard forms; exact shape model-dependent.
- **a₀ as a horizon scale.** The MOND acceleration *scale* `~ c²/R_H ~ cH₀`, the cosmic horizon's own acceleration, forced (as a scale) because MOND is the local-vs-horizon interference sector; the dimensionless O(1) coefficient is not derived. Conjecturally the same `b→0` endpoint as dark energy Λ (`a₀ ~ c²√Λ`).

### Reader map

*Intuition.* In ED, what you can physically measure is always an amplitude *squared* (that is the Born rule), and gravity is sourced by such a squared amplitude, the bandwidth. When a local mass and the whole cosmic horizon both feed *the same channel* (this shared-channel step, not the squaring, is what actually produces interference), the squared total has a cross-term between the local field and the horizon. That cross-term is MOND. Ordinary Newtonian gravity is the part without the cross-term. For this to work, the cross-term must vanish between ordinary local masses (or the solar system would show phantom forces) yet survive against the horizon; whether one small phase alignment can do both for a system of many bodies is the mechanism's main unsolved problem. Where it does survive, it survives only when gravity is weak, below the horizon's own tiny acceleration `cH₀`, which is why the MOND scale a₀ is a cosmic number, and conjecturally the same cosmic endpoint that sets dark energy. So MOND, in ED, is gravity interfering with the edge of the universe, with its scale the edge's own pull, and its open question whether that interference can be kept away from everyday gravity.

*The distinctive claim.* MOND is the interference (off-diagonal) sector of a quadratic gravitational strain on a shared channel; Newton is the diagonal; a₀'s scale is the cosmic-horizon acceleration, conjecturally the same `b→0` endpoint as dark energy Λ. Falsifiable through the interpolation function, the source-source Newton correction (which is also the mechanism's main internal risk), and whether a₀ tracks a horizon.

**Where to look next.**
- *The emergent metric and the MOND-resembling nonlinearity:* the kinematics paper (Relational Gravity).
- *The khronometric class that houses the MOND scalar:* the khronometric-dynamics paper.
- *The Born rule / QM keystone:* the quantum-logic reconstruction.
- *The `b→0` cosmic endpoint and Λ:* the SCBU boundary work.

**Open work** (declared): the many-body-Newton tension (whether one `σ` is small enough for many-body Newton yet large enough for MOND), the central open problem and a potential showstopper; grounding the account-tier `σ`-retention beyond the motivated argument; the identification of this interference sector with the companion khronometric scalar mode (unshown); the O(1) coefficient in a₀ and the `σ√b_hor → c²/R_H` dimensional bridge (not derived); which horizon a₀ tracks (Hubble vs de Sitter/Λ), hence a₀(z); and the residual EM-sector phase consistency. The interference *form* (given the shared channel) and a₀'s *scale* are grounded; the coexistence for many bodies, the retention, and the coefficient are open, and the whole effect rests on the account-tier `σ`-retention.

---

**End of Paper (MOND from Interference).**

*Gravity arc, MOND-sector companion. Gravitational strain is quadratic in amplitude by definition (`b=|P|²`; the Born rule only confirms this and rules out a linear-in-amplitude observable); the interference (`|Σ P|²`) comes from the shared bilocal channel (P14 single-carrier), an input, not from the Born rule. Newton (diagonal) and MOND (off-diagonal, local-vs-horizon interference) coexist via a σ²-vs-σ coherence asymmetry (a small horizon-aligned phase bias), which is QM/gauge-consistent (σ is a gauge-invariant source-horizon relative phase, invisible to co-local interference) but carries an unresolved many-body-Newton tension (`σ²·(N−1)`), the central open problem / potential showstopper. The interpolation `μ_ED(x)=[√(1+4x²)−1]/(2x)` has correct limits (shape model-dependent). a₀'s scale is the horizon acceleration `c²/R_H ~ cH₀`, forced by the mechanism; the O(1) coefficient is not derived, and a₀ ~ c²√Λ (MOND scale = dark energy endpoint) is a conjecture. Crank-railed: a₀~cH₀ is generic (not an ED discovery), the load-bearing work is the shared-channel + account-tier σ-retention (not the Born rule), and the interference↔khronometric-scalar identification is unshown; a candidate mechanism with a disclosed potential showstopper, not a proven theory. Companion to the kinematics and khronometric papers.*
