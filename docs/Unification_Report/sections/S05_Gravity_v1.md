# §5 — Gravity: the khronon is the arrow made dynamical

*Draft v1, 2026-07-10. Register: peer-facing. Tier: **closed** (the GR quartet is Derived; the dark-matter/dark-energy line is Grounded, with named open debts). Point-toward, not re-derive. Primary sources (read-first, tiers taken from the gravity folder README + each paper's audit): `Paper_GR-I..IV`, `Paper_KM-I/II`, `Paper_OneField_Letter`, `Papers_025–038` (G, a₀, combination rule, BTFR, MOND field equation, Λ), and this session's `Khronometric_Lambda_HealthyBranch_FromStandingKeystone`. This is the arrow's second §6-relevant job; §6 will identify it with §4's.*

---

**The arrow's job here.** In §4 the arrow selected the quantum pointer basis. Here it does something a relativist is trained to say is forbidden: it supplies gravity with a **preferred time**. General relativity insists no such thing exists, that no foliation of spacetime is physically singled out. ED's gravity has one, and it is not smuggled in, it is the arrow of §3 made dynamical. The field that carries ED's gravity, the **khronon**, *is* the substrate's direction of commitment, promoted to a degree of freedom. Gravity, in ED, is what the arrow looks like at large scale.

This is the sector where ED is simultaneously most complete and most distinctive. It reproduces general relativity where GR is tested, and it departs from GR exactly where the arrow leaves a fingerprint.

## The weak field is Einstein's

The lapse of the emergent metric is set by the substrate bandwidth, `N² ~ b`, and carrying that through with the correct factor of two yields the **weak-field Einstein metric** and the three classical tests (perihelion precession, light bending, Shapiro delay) at the standard values (GR-I, Derived). Newton's constant comes out as `G = c³ℓ_P²/ℏ` once the substrate grain is identified with the Planck length (Paper_027; `ℓ_P` inherited, the relation derived). So at the level everyone actually measures, ED gravity *is* the textbook metric. Nothing exotic is on offer in the regime where GR is confirmed, which is the correct behavior for a theory that has to survive the Solar System.

## The class is forced, and it is khronometric

What kind of gravity is it, structurally? Lovelock's theorem plus a mode count fixes the answer: ED gravity is in the **khronometric class**, two tensor (graviton) modes plus one scalar khronon (GR-II, Derived/Grounded). The tensor sector propagates at the speed of light, `c_T = c`, *structurally* rather than by fine-tuning, so ED is clean against the GW170817 bound that killed a swathe of modified-gravity models. The Lorentz violation the khronon carries is universal and confined to the safe sector.

The dynamical rule for the khronon field is `ḃ = D∇²b − κρ`, built and run as a simulation (GR-III, Derived/Measured): it has the Newtonian fixed point, gives `r_s ∝ M`, produces a frozen `b → 0` horizon with an area law, and fixes the khronon speed `c_s = c`. And the preferred-frame parameters, the usual place a preferred-time theory dies, are safe: **`α₂ = 0` exactly**, and `α₁ = −4λ_local` with `λ_local ~ ρ_event/ρ_Planck`, safe by at least seventy orders of magnitude because becoming is sparse (GR-IV). The exact `α₂ = 0` is not a fitted escape; it is a structural prediction, and a sharp one.

## One field: gravity, dark matter, dark energy

The reason to care about this sector is not that it re-derives GR. It is that the *same* khronon does three jobs that mainstream physics assigns to three unrelated things.

- In its **high-acceleration** face, it is Newton and the Einstein metric (above).
- In its **deep field**, it is the dark-matter phenomenology. KM-I embeds MOND relativistically as the khronon's low-acceleration behavior, forced given the ED combination rule `a = √(a_N·a₀)` (Paper_030), with the transition scale `a₀ = cH₀/(2π)` coming from the cosmic decoupling surface (Paper_029), the slope-4 Baryonic Tully–Fisher relation `v⁴ = GMa₀` with zero intrinsic scatter (Paper_031), and the AQUAL field equation (Paper_036). The lensing, ghost, and PPN kill-checks pass (KM-I, Grounded/conditional). ED does not add dark matter; it reads the rotation curves off the same field that gives Newton.
- In its **cosmological** face, it is the dark-fluid and a single cosmological constant Λ (KM-II; the Λ *magnitude* is inherited, targets #10).

That is the payoff line of the whole sector, and it is worth stating without hedging: **gravity, dark matter, and dark energy are one field in ED, and that field is the arrow.** (Paper_OneField_Letter is the capstone.)

## The new bound: ED is on the healthy branch, not GR

One quantitative piece was tightened this session. The khronometric scalar's kinetic structure has the form `K_ijK^ij − λK² = shear² + (1/3 − λ)K²`. Ghost-freedom requires the low branch, `λ < 1/3`. General relativity is the special point `λ = 1`, and GR escapes the conformal-mode ghost using full four-dimensional diffeomorphism invariance. **ED cannot use that escape**, because the arrow makes the khronon (the expansion/conformal mode) genuinely propagate, breaking the 4-diffeomorphism symmetry GR leans on. So health *forces* ED onto the low branch: `λ < 1/3`, and in particular `λ ≠ 1`, ED is not GR (form-forced bound; the exact value of λ in `[0, 1/3)` is F-dependent and inherited). This is a distinctive, falsifiable structural claim: ED lives on the Hořava *healthy* branch, and its scalar sector is not general relativity's.

## Where ED departs from GR (the falsifiable edge)

Collecting the fingerprints, because these are the sector's weapons, not caveats:
- **ED gravity is MOND, not Einstein, in the deep field**, and the emergent metric is kinematic rather than a dynamical Einstein field (the nonlinear completion is the interference cross-term, not the Einstein–Hilbert term). This is a genuine departure, and it is the source of the dark-matter phenomenology rather than an add-on.
- **`α₂ = 0` exactly** — a hard preferred-frame prediction.
- **`λ < 1/3`, `λ ≠ 1`** — the healthy-branch bound.
- **Weak-lensing depends on substrate activity** (Paper_038.6), and the **merging-cluster offset–velocity "knee"** is experiment-ready (Paper_OffsetVelocityLaw). These are on the falsifier frontier (§14).

## Honest debts

Stated plainly, because a closed sector still has edges:
- **Clusters and the CMB are owed.** KM-II carries a clustering regulator dial that reproduces the phenomenology but is not yet pinned from first principles; the cluster-scale and CMB fits are the standing open debt of the line.
- **Λ's magnitude is inherited**, not derived (the structure, sign, and scaling are grounded; the number is the `Θ_ED ≈ 10⁻¹²²` inherited value).
- **The exact λ is inherited** (the bound is derived; the point in `[0, 1/3)` is not).
- **GW polarization content** beyond `c_T = c` is viable but its exact couplings are not fully derived, the one live tension, not a failure.

None of these demote the sector. The GR quartet is closed and Derived; the one-field unification of dark matter and dark energy is grounded and distinctive; the departures from GR are sharp enough to kill the theory if nature disagrees.

## What this buys §6

The load-bearing fact to carry forward is the identification made at the top: **gravity's preferred time is the arrow.** The khronon is not a new field standing next to the arrow; it is the arrow, dynamical. Hold that against §4's result, *the arrow selects the quantum pointer basis*, and §6 has both halves it needs. The pointer basis and the preferred foliation are jobs of one primitive. That is the unification, and gravity is where its second half lives.

---

*Draft notes for finalization:*
- *Tiers taken from the gravity README (authoritative): GR-I..IV Derived (α₂ sparsity Measured); KM-I Grounded/conditional; KM-II Grounded with clusters/CMB Open; a₀/Λ value Inherited. Do not upgrade KM-I to "Derived" or call Λ's magnitude derived. Do not down-flag the GR quartet — it is closed.*
- *The λ bound is this session's result (Khronometric_Lambda note); state as form-forced bound / inherited value, matching §5 of the ledger. Verify the `(1/3 − λ)` decomposition against GR-II before locking.*
- *Length ~1350 words. This sector earns a bit more room than §4 because it carries three phenomena (gravity/DM/DE) and the §6 half; trim the "departs from GR" list first if tightening.*
- *Register OK: khronometric, Lovelock, α₁/α₂, AQUAL, Hořava healthy branch, GW170817 named flat-out.*
- *§6 dependency: the khronon = arrow identification must read identically here and in §6. Keep the phrasing "the arrow, dynamical."*
