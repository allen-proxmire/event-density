# ED Research Targets — Live Open/Closed Map

*The current state of the open and closed work. Snapshot maintained by hand; update as arcs close. Pairs with `NEW_SESSION_ONBOARDING.md`. The one-line read is at the bottom. For the **strategic finish-line overlay** (these targets organized against what unifying physics requires: two structural gates, one goal-weapon, the rest inherited or walled) see **`ED_Road_To_Unification.md`**.*

*Full whole-repo reorientation 2026-06-23 (six parallel arc-audits across `event-density/` + `ED Generative/`). Prior snapshot was 2026-06-14 and had gone stale on the gravity keystone (Route B done; α₁ moved) and SCBU (Route A4 closed). Two genuinely-new open items surfaced: the chiral-gauge/parity gap (#2b) and the α₁ internal tension (#1). Verdict tiers and crank-rail honesty preserved throughout.*

---

## A. The big open research targets (real work, not tightening)

1. **Phase-3 GR → Einstein closure — the "big E." — ESSENTIALLY CLOSED (2026-06).** **The structural arc is COMPLETE and the preferred-frame kill-switch (α₁/α₂/PPN) is CLOSED by GR-IV (`Paper_GR-IV_ArrowsAlibi`): α₂=0 exactly (verified vs Will/Blas–Sibiryakov), α₁=−4λ_local suppressed ≥70–88 orders below bound (mechanism derived, c₂=sparsity measured, the two strands reconciled). What literally remains is bookkeeping (λ_J factor-of-2 normalization; a fuller 10-param PPN table if wanted) + the acknowledged-irrelevant absolute ρ_event magnitude — NOT real work, NOT safety-critical.** *(Detail below kept for the audit trail; read it as "closed," not "open.")*

   **What is done (and solid).** Rounds R1–R12 + the dynamical-bandwidth builds close every *structural* question: field-equation form is **khronometric** (Lovelock + mode count: 2 tensor + 1 scalar, the arrow breaks the time gauge); weak-field Schwarzschild metric derived (`∇²b~ρ`, `N²~b`); factor-of-two light bending (sim 2.09, Nordström excluded); redshift, perihelion precession (timelike geodesic closed at eikonal level — limit-forced, residual is one simulable massive-front step); `α=1` **forced** (Einstein branch; reserve is a monotone-draining P04 band, Nordström needs forbidden replenishment); both gravitational cones **luminal** (`c_T=c` from the single P05 cone; `c_s=c` because `ε=0` — the reserve is dissipative not kinetic); the **dynamical rule `ḃ = D∇²b − κρ` is built and run** (Newtonian fixed point corr 0.999; `r_s∝M` emergent; finite-radius `b→0` horizon = frozen A2 cut = metric horizon, realizing R4§6 by build-and-run). Horizon thermo delivered: **`S∝A` measured** on the emergent horizon (perimeter `r_h^0.96`, not bulk) and **Hawking `T∝1/r_h` derived** (`κ=½∂_r b` on `b=1−r_s/r` + GR-I + measured `r_s`; the prior "flat κ" was a measurement error, now corrected).

   **The one open piece — `α₁`/`α₂` (the preferred-frame falsification number) — and its internal tension.** The whole front reduces to one coupling (`α₁ = −4c₁₄`, equivalently `−4λ_J`, equivalently `−4λ`). Two strands in the corpus do not yet fully agree:
   - **The "safe via sparsity" strand** (`LambdaOfRho_Derivation`, `Alpha1_Magnitude`, `SparseCommitment_Constitutive`): the coupling is **density-screened** — `λ = (k₁₁/s₀₂)·ρ_event/ρ_Planck`, because metric stiffness is *always-on* P02 sharing (density-independent) while khronon stiffness is *sparse* P11 commitment-pinning (density-dependent). On the **forced sparse branch** (dense commitment = quantum-Zeno freeze = no QM; ED has QM ⟹ sparse — this is *constitutive*, the same fact behind coherence and time dilation), `α₁ ≲ 4×10⁻⁹³` in the Solar System — safe by ≥70 orders. This strand says "closed cleanly."
   - **The "undetermined coupling" strand** (`ComputingC14`, `ProtectionHunt`, `DirectedFluxSector`, `MovingBinarySim`): as a *bare* PPN coupling, `c₁₄` is genuinely undetermined; **`c₁₄=0` exact is REFUTED** (a propagating khronon at `c_s=c` forces `c₁₄≠0`); small `c₁₄` is *technically natural* (GR-limit symmetry, metric-only matter coupling) but its value rests on an **O(1) inherited band-fraction** `k₁₁/s₀₂` and an **assumed scale** (`ρ_event^vac` sub-Planck, or the near-matter dissipative range `Γ ~ ℓ_P`). And **`α₂` + the full PPN suite (LLR, pulsar) are asserted-by-extension, not computed.**

   **RECONCILED + α₂ COMPUTED 2026-06-23** (`foundations/Phase3_GR_Alpha2_Vanishes_Alpha1_Reconciled.md`): the two strands were never in conflict. (i) **`α₂ = 0` identically** on ED's luminal family — computed from the cited khronometric formula; the same `c_s=c` condition that fixes the kinetic coupling nulls the `α₂` numerator (`−λ+α+2αλ → 0` under `λ=α/(1−2α)`), so the *tighter* preferred-frame bound (`10⁻⁷`–`10⁻⁹`) is satisfied for free and the front loses half its content. (ii) **The strands reconcile** via the `c_s=c` family relation `c₁₄ = c₂/(1+2c₂) ≈ c₂`: LambdaOfRho *derives* `c₂ = sparsity`, so `c₁₄ ≈ sparsity` — nonzero (khronon propagates; ProtectionHunt's exact-zero refutation holds) yet tiny (`≈10⁻⁹³`). `α₁ = −4c₁₄ ≈ −4·sparsity`, safe by ≥70 orders on the forced-sparse branch, against the *looser* LLR bound. (iii) **The conservative sector is safe on its own** — Route-A's "only escape is the dissipative near-field" pessimism dissolves; dissipation is now extra margin, not load-bearing. **So the keystone residual is much smaller than the prior snapshot implied:** the front is one number (`α₁`) against the looser bound with a ≥70-order derived clearance, plus `α₂=0`. **What's still genuinely owed:** ~~an independent re-derivation of the α₂ formula~~ **DONE 2026-06-23** (formulas verified against published arXiv:1105.5149 eq. 31 + Blas–Sibiryakov 1412.4828 eq. 27, + numerical check — `α₂=0` is not a transcription artifact but a *general* feature: any luminal-cone khronometric theory has α₂=0, α₁=−4c₁₄); **Findings 1 & 2 RECTIFIED 2026-06-23** (`Phase3_GR_Alpha2_Vanishes_Alpha1_Reconciled.md §9`, anchored on the verified Will PPN `g_{0i}` form, + correction banners on both source docs). **F2 (the bug):** Route-B §7's matching (conservative `α₁=−8c₁₄`, `α₂=−2c₁₄`) was wrong — it sourced `g_{0i}` from the *scalar* current, which DirectedFlux measured curl-free → pure-gauge; the physical cross-term is the vector sector `g_{0i}=λ_J w_iU` (`w_iU` only) ⟹ `α₂=0` (gauge-invariant, = verified) and `α₁=−4c₁₄𝒮(Γ)`; §7 superseded. **F1 (the tension):** `c_s=c` forces `c₁₄=c₂/(1+2c₂)`; with LambdaOfRho's `c₂=sparsity`, `λ_J=c₁₄≈sparsity` is intrinsically tiny ⟹ DirectedFlux's "O(1)-saved-by-screening" framing superseded, `𝒮(Γ)` redundant. The two strands/routes now tell one consistent story. **What's left is precision/consistency, not safety:** the band-partition *confirmation* of `c₂=sparsity` from the four-band dynamics — **now scoped turnkey** (`Phase3_GR_BandPartition_Scoping.md`, 2026-06-23): build the explicit P04 4-band partition (adjacency=always-on→`M_P²`; commitment-reserve=sparse→`f²`,`Γ`), measure `M_P²`/`f²`/`λ_J=κ_J/κ` as *outputs* (not inputs), sweep `ρ_event` to confirm `c₂∝ρ_event/ρ_Planck`, and run the decisive consistency check `c₂ =? λ_J/(1−2λ_J)` (the `c_s=c` family relation); honest boundary stated first — the lattice gives the O(1) band-fractions + the scaling + the consistency, NOT the absolute `ρ_event^vac` (Planck-scale, multiscale-EFT, ~10⁴⁴ off-lattice), and doesn't need to since the scaling already gives ≥70-order safety; turnkey diffs specified for `band_level_ppn.py`/`directed_flux.py` + a new `band_partition_check.py`; the `λ_J=c₁₄` vs Will-form `2c₁₄` factor-of-2 normalization; the robust-but-unpinned `ρ_event^vac` magnitude + the inherited `O(1)` band-fraction; and a literal LLR/pulsar comparison (trivial given the margin, not yet run). **This is the (now-narrowed) keystone residual.** **BAND-PARTITION TARGET A DONE 2026-06-26** (`foundations/Phase3_GR_BandPartition_TargetA_Findings.md` + `evaluation/DynamicalBandwidth/band_partition_check.py`): built the explicit bands and measured `c₂=f²/M_P²` as an OUTPUT. **(A1) `M_P²` (always-on adjacency) is density-independent** (3.1% spread across ρ_event, even with bandwidth-draining commitments — band decoupling confirmed). **(A2) `f²` (sparse commitment-reserve) is linear in ρ_event** (c₂/ρ const ~74 through the dilute regime 1e-5..1e-2), **saturating above ρ_event~0.03** (= per-locus bandwidth-capacity knee = the validity edge). **So `c₂` inherits the sparsity scaling `c₂ ∝ ρ_event/ρ_Planck` — "c₂ = sparsity" MEASURED from the band dynamics, upgrading the α₁ safety premise from asserted-screening to measured-scaling+O(1)-coefficients** (physical sparse commitment sits far inside the dilute regime, so the linear scaling holds robustly). Honest scope: relative units, absolute magnitude stays EFT (off-lattice, by construction); leading dilute scaling = analytic expectation, the genuine tests passed = M_P² density-independence + the saturation knee; single-seed Poisson noise. **NEXT (decisive, deliberately not rushed): Target B `λ_J=κ_J/κ`** (the directional response of a *moving* commitment — which IS the α₁ physics question; a careless model would smuggle the answer) **+ the consistency check `c₂ =? λ_J/(1−2λ_J)`** (§4, the heart). **TARGET B DONE 2026-06-26 — honest "no" on the measurement** (`foundations/Phase3_GR_BandPartition_TargetB_Finding.md`): **λ_J is NOT independently lattice-measurable.** Three confirmed reasons: (i) the metric-band non-covariance wake is O(1) (Δ/w≈125, dipolar — ran `boost_noncovariance.py`) but **pure gauge** (`directed_flux` curl-free) → not the physical λ_J; (ii) the physical λ_J (vector sector, ∝sparsity~10⁻⁹³) is **swamped by the lattice's own O(1) boost-breaking artifact** (same kind of object, inseparable); (iii) **modeling the directional response IS the boost-covariance choice** that determines λ_J (smuggles value AND scaling). **So the consistency check is an ANALYTIC IDENTITY** (λ_J=c₂/(1+2c₂) was *derived from* c₂ via c_s=c) — not an independent lattice test; **this corrects the scoping doc's §3/§4 optimism.** **Consolidated: the keystone α₁ rests on [α₂=0 verified] + [c_s=c→family relation, analytic] + [c₂∝sparsity MEASURED (Target A)] + [α₁≠0 confirmed]; magnitude EFT/off-lattice by construction.** **CORRECTED 2026-06-26: c_s=c was already done** (derived in `Phase3_GR_DerivingEpsilon_KhrononSpeed.md` from ε=0; sim-confirmed in `hyperbolic_modes.py` — the reserve damps/overdamps but doesn't move the cone; GW170817-consistent). So my "remaining lattice item = confirm c_s=c" was redundant. **There is NO load-bearing remaining lattice item for α₁:** the safety chain is complete — [α₂=0 verified] + [c_s=c derived+sim-confirmed] + [c₂∝sparsity MEASURED (Target A)] + [α₁≠0 confirmed] — λ_J=c₂/(1+2c₂)≈sparsity follows, α₁≈tiny, safe ≥70 orders (scaling). Only the absolute `ρ_event^vac` magnitude is off-lattice EFT by construction (and not needed; the scaling gives the safety). **The α₁ front is complete to the scaling level on the substrate's own terms.** No λ_J faked.
   - Minor structural residuals (not falsification-critical): the **3D build** (the `1−r_s/r` profile at the horizon; sims are mostly 2D/log), a **fully-direct `κ∝1/r_h` sim** (re-run 2026-06-26 `dynamical_bandwidth_3d.py`: gives **flat κ ~ r_h^0.09**, and the diagnosis is **structural not resolution** — the minimal rule is ELLIPTIC, the fixed sharing length D sets an r_h-INDEPENDENT transition width; a direct dynamical κ∝1/r_h needs a **HYPERBOLIC strong-field rule, not built**. The *physical* κ∝1/r_h holds via the corrected formula κ=½db/dr=1/(2r_s) on the harmonic profile + measured r_s∝M — `strongfield_surface_gravity.py`; r_s∝M clean, the 3D b=1−r_s/r profile only approximate, slope −1.68), the **strain↔bandwidth** substrate identification (Paper_033 posited it; whether `b=b(strain,flow)` is unproven but not a contradiction, `MetricCoincidence_Analysis §4.1`), and the **`κ,D` coefficients + reserve profile** (inherited/postulated, can't flip the branch). **HYPERBOLIC RULE RESOLVED 2026-06-28** (`evaluation/DynamicalBandwidth/hyperbolic_strongfield.py` + `Hyperbolic_StrongField_Finding.md`): built the wave rule `b'' = c²∇²b − κρ − γb'` and ran it to completion at S=144. The κ∝1/r_h "failure" was a **measurement artifact** — properly measured (inner-transition-width κ_w on a near-point source), the ELLIPTIC rule already **scales** (κ_w ~ r_h^−0.50, tracking analytic 1/2r_s); the hyperbolic rule does **not** beat it (−0.33); neither reaches clean −1 (finite-resolution, r_h≠r_s at 3–9-cell horizons). So the hyperbolic strong-field rule is **built but NOT needed** for the Hawking scaling — the premise dissolves, κ≈1/(2r_s) corroborated in value. Could-say-no fired (removed a supposed gap). *(NR-grade config parked for dedicated compute if a clean −1 power law is ever wanted.)*

   Wall 1, **closed in practice** (GR-IV). Still the keystone several B-column items inherit from (now unblocking them, e.g. #4). **The "highest-leverage single computation" framing is RETIRED — α₂=0 computed, PPN preferred-frame comparison done in GR-IV §6, screening scale measured (band-partition Target A). Only bookkeeping left.**

2. **Charge → Maxwell — the hard unique-signature target (#2).** **Topological skeleton CLOSED (M3); the continuum field is the open edge.** `Paper_ChargeAsTopology_B4` delivers integer winding `w∈ℤ` (quantized, conserved, irreversibility-protected, integral Gauss law `2πw` loop-independent) — measured, graph-first, exact. What it does **not** give is the determined local Coulomb `1/r²` field (per-edge config is gauge-dependent; forcing `1/r²` needs breaking both orientation-blindness and P11 → "not ED"). The reframe (§7): holonomies = lattice gauge link variables, Coulomb = their DCGT continuum limit, the Maxwell action is *latent* in the P04 coherence term `cos²(Δφ/2) ≈ 1 − ¼(∇φ)²`. **The one open question: does ED's DCGT coarse-graining select the Maxwell-action configuration as the continuum limit?** No Maxwell derivation claimed. Wall 1. **ANSWERED 2026-06-26 — honest NO** (`evaluation/B4_Arc/maxwell_continuum_test.py` + `B4_Maxwell_Continuum_Finding.md`): ensemble-averaged the gauge-invariant deficit over ED-as-built commit configs (orientation-blind, **P11 KEPT**, random orders) vs the Mod-B XY target (P11 broken = Maxwell minimizer, deficit·r²≈0.126=Coulomb). **Isotropy EMERGES** (iso cv 1.87→0.28 as N grows — the angular seam is gauge, washes out, confirming the lattice-gauge reframe's "per-edge config = gauge freedom") **BUT deficit·r² GROWS with r** (0.22→6.72 vs flat 0.126, 10-50× above the minimizer) — dominated by residual **trapped incoherence**. So **ED's committal coarse-graining does NOT select Maxwell**: the gauge (angular) part washes out, the committal/trapping (radial) part doesn't. Maxwell = the thermal/action-minimizing limit (= removing P11), NOT ED's dynamics — **the same wall as #3 (diffusion) + #5c (Gaussianity)**: the standard continuum object lives in the thermal limit ED's arrow-bearing committal substrate doesn't sample. Also informs MS-I's open "YM action via DCGT" item (committal coarse-graining doesn't select the F² action either). Could-say-no honored. **FLIPPED 2026-06-26 — AP's reframe vindicated** (`maxwell_coherent_decomp.py`): the "no" was a **field-vs-entropy measurement error.** AP's point — coarse-graining *is* losing the arrow, which **relocates to entropy** (it doesn't block the field). Tested by separating the coarse field into **coherent** (⟨e^{iφ}⟩) + **incoherent**: the **coherent part → ~Coulomb** (deficit·r² → ~0.17 trending to the 0.126 Maxwell value, vs ~6.7 for the un-separated total) and the **incoherence is concentrated near the source, falling off far away = the entropy**. **So CGing ED gives Maxwell (coherent) + entropy (incoherent) — the inverted stack working correctly, NOT a wall.** **Bigger-box confirmation (L=121, N=384):** the coherent field deficit·r² **tracks the Mod-B Maxwell reference** (matching at the far edge, 0.153 vs 0.151), cleaned up smoothly as N grew; the incoherence (entropy) is **sharply localized at the source** (0.92→0.14); coherence **plateaus at ~0.67** (a definite ~33% entropy fraction, not noise). Nailed: **coherent field = the Maxwell field; incoherence = localized entropy.** (Caveat: finite box → both ~0.15 not the asymptotic 0.126 far-field; the *equivalence* is the claim and holds.) **BIG implication:** likely reframes **#3 (diffusion) and #5c (Gaussianity)** the same way (the "no"s may all be signal+disorder summed) — a strong HYPOTHESIS, not yet re-checked. **#2: the coherent continuum field emerges; the arrow becomes entropy. The earlier ThreeWalls "ED doesn't reach the continuum" framing is corrected** (`foundations/ThreeWalls_OneWay_vs_TwoWay_Continuum.md` banner).

2b. **Chiral gauge structure / Standard-Model matter sector — NEW, unmapped until 2026-06-23.** **The sharpest unmet challenge to the discreteness commitment, and it was not on this map.** ED's substrate is **chirality-symmetric**: the gauge paper (T17) is self-described as a *rewrite* of fiber-bundle vocabulary, not a derivation; non-abelian structure is "A→analogy"; and baryogenesis shows even *binary* chirality is an **undischarged postulate** (P-BinaryAdmission; reduction routes R1/R2/R3 all failed). The RQM bridge classifies Weyl/Dirac reps (Cl(3,1) M1, Dirac M2, Lorentz reps M3, massless-slot M3) but **imports** chirality from standard theory and never derives *why* nature couples the weak force to one handedness only. **Producing parity-violating chiral gauge couplings from a parity-symmetric substrate is not derived, not analyzed, not mapped.** This is the physics behind Nielsen–Ninomiya (verified 2026-06-23: N–N is nowhere in the corpus; ED's non-lattice structure means N–N's literal premises don't apply, but that's necessary-not-sufficient — ED hasn't shown chiral couplings emerge from the graph). **Wall-status undetermined**: likely Wall-1 emergence (parity violation inherited at the value layer), but possibly a genuine obstruction if the chiral anomaly is more general than the lattice formulation. Sub-question #1: which wall is it? Blocks any full Standard-Model emergence work. **Highest-leverage open item in the matter sector.** **SCOPED 2026-06-23** (`foundations/ChiralGauge_Parity_Gap_Scoping.md`): the gap is *not* "no fermions/gauge" — ED has Cl(3,1) chiral *room* (γ⁵ exists, T2/T4) and a fundamental *orientation* (the arrow, retarded-only V1 per T18) — it's that the substrate is **oriented in time but ambidextrous in space**: parity-symmetric P09 polarity, no native chirality ℤ₂ (baryogenesis R1/R2/R3 all failed), parity-symmetric gauge sector (T17 rewrite). **Provisional wall verdict: Wall 1 (extension-away), not Wall 2** — Nielsen–Ninomiya's premises (regular lattice, *hermiticity*) don't bind ED (non-lattice, and the arrow makes the dynamics retarded/non-hermitian — exactly what N–N forbids you to have), so there's no known impossibility proof; but the current primitives demonstrably don't produce a chiral coupling. **First move (turnkey, analysis — no lattice run): SQ1, the N–N binding analysis** — write ED's discrete Dirac operator from T4+V1, check each N–N premise, test for a non-hermitian/graph generalization. Three outcomes: binds → Wall-2 jeopardy (a real result either way); doesn't-bind + the foliation-Clifford route (SQ2 — does the same preferred foliation that gives the khronon induce a *spatial* handedness via the timelike γ⁰?) looks viable → open the front; doesn't-bind + no route → mapped-but-open. SQ3 (anomaly cancellation) is the hardest piece, deferred. The promising thread: chirality could be the arrow's *fourth* job (after the khronon, α₁-safety, and time dilation). **SQ1 DONE 2026-06-23** (`foundations/ChiralGauge_SQ1_NielsenNinomiya.md`): the literal **Nielsen–Ninomiya theorem does NOT bind ED** — its engine is the compact Brillouin-torus topology + hermiticity, and ED has *no Brillouin torus* (relational graph, not a periodic lattice) and is *non-hermitian* (retarded-only V1, the arrow, T18), with locality intact (finite V1 width — not a non-locality loophole). Confirms **not Wall 2**, and is the principled basis for the "discrete but not a lattice" reply to the Tong objection. **Necessary-not-sufficient, the honest caveat:** the doubling question doesn't vanish — it *migrates* to a now-sharp open computation, **the net chirality of ED's DCGT continuum-limit Dirac sector** (the substrate-V1→Dirac derivation T4 §3.7 leaves OPEN; new target = chiral vs doubled); and the real target, a chiral *gauge* theory + **anomaly cancellation (SQ3)**, is unsolved even where the free-fermion no-go is beaten (Ginsparg-Wilson/overlap handle vector-like, not chiral gauge; electroweak-on-lattice is itself open). **Front OPEN, not walled.** Positive thread (hypothesis, not proven): non-hermiticity is a known route to *unpaired* chiral modes (point-gap topology, non-hermitian skin effect), so the arrow may be the *mechanism* that produces net chirality, not just the escape from the no-go. **Next:** (i) compute the continuum-limit net chirality; (ii) SQ3 anomalies; (iii) check for a non-hermitian doubling no-go (residual Wall-2 risk). **Continuum toy-model DONE 2026-06-23** (`foundations/ChiralGauge_SQ1b_ContinuumChirality_ToyModel.md` + `evaluation/ChiralGauge/chiral_winding.py`) — tested step (i)'s mechanism, **partial NO.** The arrow's retarded transport is structurally a *free Wilson term*: it **lifts the N–N doublers** (clean win — ED escapes the pairing, undoubled), but Wilson-style lifting is generically **vector-like (non-chiral)**. The only chiral candidate is the retarded transport's nonzero *point-gap winding* (±1, confirmed numerically), but that's a distinct non-hermitian invariant whose relativistic γ⁵ descent is **unproven**. So **net chirality is NOT delivered**; the natural reading is undoubled-but-vector-like; the "arrow → chirality" hypothesis is **not supported** (not strictly killed — the point-gap route survives logically). **#2b reinforced as a genuine hard open problem; the arrow is necessary-not-sufficient — it undoubles, it doesn't hand-pick handedness.** The real open computation is now the **relativistic bridge**: does the point-gap topology survive Lorentz-covariantization to a γ⁵ index (the open T4 coarse-graining; *default is vector-like*, overturning it requires the point-gap winding to survive)? — then SQ3 anomalies; plus an **IC-selected / spontaneous parity-breaking** route (as ED's baryogenesis already had to invoke) as the alternative if the transport is vector-like. **CORRECTED 2026-06-23 — SQ1c** (`foundations/ChiralGauge_SQ1c_PointGapChirality.md` + `evaluation/ChiralGauge/chiral_skin.py`): the SQ1b "vector-like" verdict was **too hasty** — it imported the *hermitian*-Wilson outcome into a non-hermitian setting. The correct non-hermitian computation shows the retarded (arrow) transport carries a **net spectral flow ±1 (a chiral-anomaly signature = ONE net chirality)**, a **non-Hermitian skin effect** (all modes pick one direction: COM 77–79 vs hermitian 39.5), and point-gap winding ±1 — all zero hermitially, all nonzero with the arrow, i.e. exactly the chirality N–N forbids on hermitian lattices. So the arrow is a genuine **candidate mechanism** for parity violation, not just an escape; **AP's standing "only one pattern allowed" intuition = the single net chirality, vindicated.** #2b upgraded from "default vector-like, hard" to **"open and promising."** Still NOT shown (1+1D toy, not the SM): (1) the **relativistic γ⁵ bridge** — discrete net chirality → Lorentz-covariant Weyl gauge coupling (needs the open T4 substrate→Dirac chain); (2) **unitarity** — non-hermitian arrow → unitary emergent QFT via sparse commitment (mechanism named, not shown); (3) **anomaly cancellation (SQ3)** — the hardest piece. **SQ1d 2026-06-23** (`foundations/ChiralGauge_SQ1d_TopologicalRobustness.md` + `evaluation/ChiralGauge/chiral_sparsity.py`) — the sparsity/unitarity bridge, first cut, favorable: the net chirality (point-gap winding) is **topological — quantized ±1 for every nonzero commitment however sparse, 0 only at exactly zero commitment.** So **sparse commitment preserves the FULL chirality** (unlike non-topological α₁ ∼ ρ_event/ρ_Planck, which sparsity suppresses) → **maximal parity violation is compatible with sparse commitment, no fine-tuning.** Same sparse-commitment fact, opposite fates: α₁ tiny (suppressed), chirality maximal (quantized). Suggestive (tiered) bonus: this could explain the SM's *unexplained* **maximality** of parity violation — a topological charge can't be partial — matching the real pattern (maximal P-violation + tiny preferred-frame). Honest limits: **1+1D; "sparse" modeled as "weak" ε (proxy, not a faithful rare-discrete-commitment model — the next refinement); relativistic γ⁵ descent + anomalies still open.** Net after SQ1→SQ1c→SQ1d: not walled, arrow carries net chirality, chirality is topological & sparsity-robust & plausibly maximal — coherent and promising; remaining hard work = the **relativistic bridge** (the open T4 substrate→Dirac chain), a faithful sparse-commitment model, and SQ3 anomalies. **SQ1e 2026-06-23** (`foundations/ChiralGauge_SQ1e_RelativisticBridge.md` + `evaluation/ChiralGauge/chiral_3p1d.py`) — relativistic bridge, assessment + one clean check: the arrow's **undoubling survives to 3+1D (16 doublers → 1, computed)** — necessary, met — **but 1 survivor is consistent with BOTH a chiral Weyl point AND a vector-like Dirac point** (Wilson's default is vector-like), and the count can't distinguish them. The 1+1D point-gap=anomaly coincidence is *dimension-special*; the chiral-vs-vector-like relativistic core is **open and NOT toy-settleable.** AP's reframing → the more ED-native route: handedness is *per-commitment* (a count, hence topological), so build chirality from **handed commitment events + first-arrival alignment**, which **unifies with baryogenesis** (parity violation + matter/antimatter asymmetry possibly ONE handed-commitment lock-in). **#2b has reached the research frontier** — coherent/promising at the 1+1D/structural level (not walled; arrow → net, topological, plausibly-maximal chirality; 3+1D undoubling confirmed), but the relativistic realization needs the real **substrate→Dirac derivation** (T4) or the **per-commitment/baryogenesis-unified construction**, not more toys. **The "same sparse-commitment fact → tiny α₁ (non-topological) AND maximal parity violation (topological), different kinds of thing" result is flagged for the eventual chiral-gauge paper.** **SQ1f 2026-06-23** (`foundations/ChiralGauge_SQ1f_PerCommitment_BaryogenesisUnification.md` + `evaluation/ChiralGauge/chiral_percommit.py`) — attacked the open core directly. **(1) The 3+1D transport survivor is VECTOR-LIKE** (near p=0, `D≈iΣγ^μ p_μ` = full 4-component Dirac point; retarded = Wilson class = vector-like): the 1+1D chirality was **dimension-special** (there the global point-gap winding coincides with chirality; in 3+1D they decouple). **A real partial-no for the transport route.** **(2) AP's per-commitment route works & unifies:** handedness = winding of the P09 phase along the commitment sequence; toy shows net winding survives **iff the phase-advance sign is globally LOCKED** (robust to P11 noise: drift∼N beats noise∼√N), else vector-like — and the lock is **first-arrival, the same mechanism baryogenesis already needs.** **UNIFICATION HYPOTHESIS: parity violation + matter/antimatter asymmetry = ONE handed-commitment lock-in** (one choice, two faces; both maximal because topological). **Net: #2b folds into the baryogenesis arc — ONE open problem, not two: derive the first-arrival handed-commitment lock + its phase-helix→γ⁵ realization.** Open core (shared w/ baryogenesis): the lock is undischarged (`P-BinaryAdmission`, M2; no native ℤ₂, R1/R2/R3 failed); phase-helix→γ⁵ unproven; SQ3 anomalies untouched. **#2b sprint endpoint: reframed from "unmapped possible-wall" to "specific ED-native baryogenesis-unified open derivation."** **Unification note written** (`foundations/HandedCommitment_Unification.md`, M-conjectural): parity violation + matter/antimatter asymmetry = ONE first-arrival handed-commitment lock-in, both maximal because topological; two open problems → one; correlation prediction (gauge handedness = cosmological matter-sign). **SQ1g 2026-06-23** (`foundations/ChiralGauge_SQ1g_PhaseScrew_Gamma5_Scoping.md`) — step 1 of the real T4-with-chirality derivation: pins the phase-screw→γ⁵ step to **ONE computable discriminator** — under P05 transport along the arrow, **advance(L) − advance(R) of the P09 phase** (zero ⇒ vector/EM-like/parity-conserving/screw-route-fails; nonzero ⇒ chiral/γ⁵/parity-violation). Hinges on one primitive property: **is P05's phase-transport spin–arrow-coupled (chiral) or spin-blind (vector, the default)?** Kills two red herrings (timelike-phase alone = temporal VECTOR; transport-doubling already settled SQ1f). Honest prior: default vector, real chiral channel via *structured* P05/P07 polarity transport — ~even odds, well-defined. **NEXT (the real derivation, ours): build the emergent helicity (T4 spinor), transport each helicity under P05, compute advance(L)−advance(R), check maximality (SQ1d predicts pure V−A).** Parallel high-leverage target: **derive the lock-in** (discharge `P-BinaryAdmission` M2 — closes BOTH #2b and baryogenesis). **SQ1h 2026-06-23 — CORRECTS the above by grounding in the real R4 baryogenesis memos** (`foundations/ChiralGauge_SQ1h_R4_BaryogenesisBridge.md`; memos on disk dated 2026-05-17): **(1) the lock-in is NOT open — it's BUILT** via R4 (continuous P09 χ_C ∈ S¹ broken at first-arrival; Outcome-C / M3-conditional; caveats: functional schematic, first-arrival IC is IC-INHERITED, M3 upgrade memo-specified not yet applied — paper still M2 on disk). So "derive the lock-in" is largely DONE for the C-face. **(2) "One lock-in, two faces" OVERCLAIMED:** baryogenesis χ_C = matter/antimatter (C-type, "orthogonal to motion"); gauge helicity = parity (P-type, "along motion") — DIFFERENT symmetries → **two structurally-analogous first-arrival P09 locks (C-lock built, P-lock open); same-event(CP)-vs-two = open** (SM partial CP ⇒ related-not-identical). **(3) GIFT/redirect:** R4 determined **V1 transport is chirality-AGNOSTIC** (∂_χC𝒦_V1=0 → vector; independently CONFIRMS SQ1f's vector-like survivor from the cosmological side) **and V5 cross-chain is chirality-SENSITIVE.** So the gauge chiral coupling, if any, is **V5-MEDIATED, not V1/P05-transport** → **SQ1g's discriminator is re-posed: does V5 cross-chain coupling supply an axial (γ⁵) coupling?** Cleaner unification thread: **V5 is the chirality-bearing kernel for BOTH faces; V1 is blind to both.** **CORRECTED NEXT TARGETS: (a) does V5 supply the axial γ⁵ coupling? (b) are the C-lock and P-lock the same first-arrival event? — NOT the V1/P05 transport discriminator (answered: vector).** **SQ1i 2026-06-23 — target (a), real structural result** (`foundations/ChiralGauge_SQ1i_V5_AxialCoupling.md`): read Paper_090 (V5 structure) vs R4. V5 is (i) the cross-chain "field-propagator analog" carrying the P09 U(1) connection (gauge-compatible §4.3) AND (ii) chirality-sensitive (R4). (i)+(ii) ⇒ **V5 is structurally a CHIRAL (parity-violating) U(1) gauge coupling, not vector.** **Clean correspondence: V1 (single-chain, chirality-blind) = VECTOR = EM-like; V5 (cross-chain, chirality-sensitive) = CHIRAL = weak-like — ED's two kernels = the SM's two coupling types** (substrate reason nature has both; retroactively explains SQ1f: transport=V1=vector kernel). Established for C-type (χ_C); residuals = P-type (helicity/γ⁵), non-abelian SU(2) (T17), anomalies (SQ3). HONESTY: V5-as-gauge-coupling is a synthesis of Paper_090 + R4, stated in neither, pending the emergent-fermion-action derivation. **SQ1j 2026-06-23 — sprint capstone** (`foundations/ChiralGauge_SQ1j_CP_SameEvent_ConvergesOnT4.md`): the C/P-same-event question reduces to **"are U(1) P09 polarity and emergent Cl(3,1) spin related?"** (χ_C = P09-vs-arrow, helicity = spin-vs-arrow; if related ⇒ one first-arrival event, two projections ⇒ C-lock=P-lock ⇒ (a) closes; SM separate-C/P + partial-CP = signature of two projections of one selection). **CONVERGENCE: all three residuals (SQ1g discriminator, SQ1i P-type V5 coupling, SQ1j C/P) need the SAME thing — the emergent spinor + P09↔spin relation = T4/Paper_106 §3.7 (already OPEN).** **#2b reduces to the pre-existing matter-sector spinor gate; adds NO new wall.** Endpoint: #2b mapped from "sharpest unmapped objection / possible wall" → "structurally in place, reduced to T4 §3.7." **REAL NEXT MOVE = T4 itself** (build emergent spinor + how P09 sits in Cl(3,1) spin) — now revealed as the single gate behind chirality + parity + C/P unification + baryogenesis. A genuine derivation, not a toy. **T4 STARTED 2026-06-23** (`foundations/T4_01_P09_is_Vector_SpinIsClifford.md`, `T4_02_SpinIsChannelTopology_BottomsOutOnGaugeProgram.md`): read T2 (Paper_103, Cl(3,1) 4-spinor, M1) + T4 (Paper_106, Dirac form, M2) + the channel concept (P07). **Step 1:** the spin-½/double-cover/chirality is **Cl(3,1)-based, NOT P09** (a U(1) winds integer; spin-½ needs the SL(2,ℂ) double cover). So "P09↔spin" sharpens to "is P09 vector or axial?" → structural argument: **P09 is a VECTOR U(1) (e^{iθ}, chirality-blind) = electromagnetism** (its advance is tied to the arrow/γ⁰ = temporal vector coupling; matches V1=vector=EM). **Step 2:** the spinor's spin/chirality is realized by **channel TOPOLOGY** (P07 §7.4: "U(1)/SU(2)/SU(3) arise as channel-topology classes," a Phase-2/3 target); P09 is the phase *along* a channel (vector dressing). So **spinor = channel-topology (spin, SU(2) double cover, γ⁵) + P09 phase (vector/EM)**; **γ⁵ = a channel-topology class**; C-lock χ_C = the **η thread** (P07 §7.7, channel survival by polarity vs ∇ρ); C/P link reopens at the channel level (polarity & topology are two attributes of the same channels). **T4's open §3.7 BOTTOMS OUT on the channel-topology→gauge derivation (P07 §7.4)** — the gap isn't the Dirac-operator step (T2+T4 fine), it's that the substrate object carrying the spinor (channel topology) is underived. **#2b reduces to: channel-topology→gauge program (§7.4) + η thread (§7.7) — both deep pre-existing Phase-2/3 targets.** Decision point: closing T4 = taking on the channel-topology→gauge derivation, a major program. **GAUGE PROGRAM STARTED 2026-06-23** (`foundations/Gauge_01_SUN_from_ChannelMultiplicity.md`, `Gauge_02_P05_Transport_Is_a_LatticeGaugeConnection.md`). Goes beyond T17 (a fiber-bundle vocab REWRITE: postulates U(1), non-abelian "by analogy"; verified via corpus map). **Step 1:** gauge group = structure group of the channel bundle; **N indistinguishable same-rule-type channels (P08 multiplicity) + bandwidth conservation (P04) → U(N) = SU(N)×U(1)** — non-abelian SU(N) from multiplicity (the explicit treatment P08 §7.5 flagged; beyond T17's analogy). **Abelian/non-abelian = single-channel/cross-channel = V1/V5 = EM/weak** (unifies with #2b kernel thread). SM U(1)×SU(2)×SU(3) ↔ multiplicities {1,2,3} (singlets/doublets/triplets = SM reps). **Step 2:** P05-transport of N channels (bandwidth-conserving, invertible between commitments) = **a U(N) lattice gauge link variable** → the symmetry is GAUGED (gauge field = per-edge generator; field strength = plaquette holonomy). **ED's channel substrate = a non-hermitian lattice gauge theory on the RELATIONAL graph** (no Brillouin torus + retarded arrow = the #2b N–N escape); gauge sector (U(N) from multiplicity) + fermion sector (spinor=channel-topology, T4_02) = ONE lattice gauge theory; unitary between commitments, commitments = projections (sparse-commitment). Grounds T17's postulated "P05=connection" as derived. **REMAINING PROGRAM:** (3) Yang–Mills action via DCGT (lattice→continuum, why F²); (4) **uniqueness {1,2,3}** (the deep open Q — why these multiplicities, unsolved in standard physics too; candidate: channel stability + D=3+1 bound); (5) single hypercharge U(1) + electroweak mixing; (6) spin-SU(2) frame bundle (separate); (7) chirality tie-in (weak SU(2) chiral via V5 cross-channel — the #2b payoff). **Step 3 — UNIQUENESS {1,2,3}, candidate found 2026-06-23** (`foundations/Gauge_03_Uniqueness_123_SpatialDimensionBound.md`): the framework gives SU(N) for any N; {1,2,3} needs a multiplicity bound. **Candidate (ED-native, falsifiable): N ≤ D_spatial = 3** — a channel is a spatial direction (P07: "a direction a chain can keep going in," forms along ∇ρ); time is the arrow, not a channel (→ bound 3 not 4); independent same-rule-type channels occupy independent spatial-frame directions → ≤3. **Payoff: SM U(1)×SU(2)×SU(3) = the COMPLETE set {SU(1),SU(2),SU(3)} of multiplicity gauge groups allowed in 3 spatial dimensions — "three forces because three spatial dimensions"; forbids stable SU(N≥4) (falsifiable).** **Three honest gaps (NOT closed):** (a) bound mechanism (why independent directions cap at 3 vs many channels per ∇ρ direction); (b) **COLOR IS NOT SPATIAL** — SU(3) is internal/rotation-invariant; 3-of-color vs 3-of-space = profound or numerology (the central risk; partial reconcile: count spatially bounded, SU(3) acts on internal channel-index, evades Coleman-Mandula); (c) why all of {1,2,3} realized. Complementary route + path to close: an **N-channel STABILITY calculation** (channel.md "usually small") forbidding N>3 and tying the bound to D=3. **Verdict: a genuine, falsifiable CANDIDATE — the most distinctive thing the program can say — explicitly NOT a closed derivation.** **Step 4 — WALKED BACK 2026-06-23** (`foundations/Gauge_04_Color_vs_Space_Resolution.md`): ran the color/space make-or-break; the spatial-dimension bound is a **CATEGORY ERROR.** Refuted twice: (1) 3 color channels = 3 spatial directions ⇒ spatial rotation mixes them ⇒ color rotation-variant, but observed color is exactly rotation-invariant; (2) 3 REAL spatial axes → SO(3) (dim 3), but the gauge group is SU(3) (complex, dim 8) — SU(3)≠SO(3). The SU(N) channels are a COMPLEX internal triplet ℂ^N (bandwidth conservation on complex ψ, step 1), structurally ≠ the REAL spatial frame ℝ³→SO(3); nothing forbids many complex channels in real 3D. N_c=3 pinned by anomalies/π⁰→γγ/R-ratio (no D reference); **3=3 most likely COINCIDENCE.** Steps 1–2 untouched (never depended on N's value). **Redirect: the multiplicity bound is INTERNAL (channel-family stability, "usually small"), value OPEN, NOT spatial-dimension.** Slim unbuilt rescue: a 3D-special TOPOLOGICAL bound (braiding/linking is 3D-special) could re-link complex-count to 3D without identifying channels with axes. **Net: the most distinctive claim was a category error; uniqueness {1,2,3} remains genuinely OPEN, redirected to the internal stability calculation.** **§7.1 framing note (2026-06-26, from ED-I-06 "Fields and Forces"):** the multiplicity question is best posed not as an abstract "channel count" but as a **stability question about directional participation-patterns** — *"which stable directional participation-patterns does the relational participation graph support, and is that number bounded?"* (ED-I-06: fields = stable participation structures; the gauge field is the *directional* class; ED is a relational graph, not a rigid lattice). This is more concrete than counting channels — it says study the **stability dynamics of directional participation structures**. Calculable form of the same question: a **Gram-matrix / equiangular-lines** coherence limit on mutually-stable complex channel-vectors (internal, NOT spatial — do not re-run the retracted spatial-dimension/category-error reflex). Caveat: complex-geometry maxima (e.g. SIC ~ d²) usually aren't a clean "3", so the likely outcome is *a* bound, not necessarily 3. **Step 5 — CAPSTONE / #2b payoff 2026-06-23** (`foundations/Gauge_05_ChiralityTieIn_NonAbelianIsWhereParityLives.md`): does the weak SU(2) come out chiral? Real result: **parity violation can only live in the non-abelian (V5/cross-channel) sector — the abelian (V1/single-channel) coupling is chirality-blind → vector → CANNOT violate parity.** Falsifiable (chiral abelian U(1) is allowed in general gauge theory; ED forbids it); matches low-energy EM (vector) / weak (chiral) = the V1/V5 split. **NOT a derivation that weak IS chiral**, two honest gaps: (1) not every non-abelian force is chiral — QCD SU(3) is vector; chirality = matter handedness-ASSIGNMENT (weak L-only, strong L+R), not the group; (2) V5 established sensitive to C-type χ_C (R4), weak chirality is P-type helicity — rests on the open C/P-link. **Both gaps → ONE shared open core: the first-arrival handedness selection of matter channels (η-thread, #2b's bottom). LOOP CLOSES — the gauge program and #2b are one problem at their frontier.** Remaining matter sector (unified): first-arrival selection + C/P-link (shared core); YM action; electroweak/Higgs (beyond the V1/V5 split — chiral hypercharge not captured); anomalies (SQ3). **FIRST-ARRIVAL KEYSTONE 2026-06-23** (`foundations/FirstArrival_HandednessImprint.md`) — attacked the shared open core; substantially advanced (an ACCOUNT, tiered, not a closed proof). **All chirality in ED traces to the arrow (P11) at the first commitment.** A commitment is a handed event carrying (i) a P09 phase and (ii) a channel-topology screw-orientation (SQ1c screw: time-arrow↔spatial twist; T4_02 helicity=channel-topology). The FIRST commitment imprints BOTH global references at once: P09 phase → χ_C (matter/antimatter, C); screw → helicity (parity, P). So **C-lock and P-lock = the SAME event (correlated), DIFFERENT attributes (distinct → C,P separate, CP partial)** — resolves the C/P-link AS AN ACCOUNT, grounds SQ1j "one event, two projections." Both topological (SQ1d) → **maximal** (complete matter selection + maximal V−A from one lock). **UNIFICATION: parity violation + matter/antimatter asymmetry = two faces of the arrow's first-commitment imprint; the arrow = source of all chirality** (khronon, α₁, time dilation, + handedness). Residuals: (1) *which* handedness = contingent IC (standard SSB, not a weakness); (2) why *weak specifically* = matter-assignment, candidate (weak = commitment-coupled/flavor-changing → inherits arrow handedness → chiral; strong = transport-coupled/binding → vector; phenomenological hook, riskiest step, NOT derived). **Verdict: a coherent ED-native ACCOUNT of chirality's origin — the matter-sector paper's centerpiece — NOT a closed weak-force derivation.** **→ PAPER NOW WRITABLE** as the strong unified version (structural results Gauge_01/02/05 + the tiered chirality-origin account + honest open frontier). **T4 STEP 3 — SPIN DOUBLE COVER GROUNDED 2026-06-28** (`foundations/T4_03_SpinDoubleCover_FromChannelTethering.md`): the deferred spin-SU(2) frame bundle (vs the internal U(N) from multiplicity) is grounded — an ED object is **tethered** to the graph by its channels (P07), a tethered frame under SO(3) carries the ℤ₂ of π₁(SO(3)) (orientation-entanglement / belt trick), so **spin-½ is forced** and is the **signature of relationality** ("two more fundamental than one"). Completes the *kinematic* "spinor = channel topology" picture (T4 §3.7): spin (tether-topology) + chirality γ⁵ (screw) + internal gauge U(N) (multiplicity) + P09 (EM phase). Structural tier (the topology is standard; the substrate grounding is new). **UNIQUENESS CALC RUN 2026-06-28** (`Gauge_08_MultiplicityUniqueness_StabilityBound.md` + `evaluation/ChiralGauge/channel_stability_uniqueness.py`): the never-run channel-family stability calc (Gauge_03 scoped, Gauge_04 redirected). Result — stability does NOT derive 3 (a d-dependent bound, honest no like the spatial route) BUT **reduces uniqueness {1,2,3} to ONE number: the internal channel-amplitude dimension d=3** (max mutually-orthogonal/independent channels in ℂ^d is exactly d; SU(N≥4) needs ≥4 orthogonal in ℂ³ = impossible). Forbidding corollary: **if d=3, no stable SU(N≥4)** (falsifiable). **NEW OPEN THREAD: why is internal d=3?** — and is it secretly the spatial 3? Naive "spatial-3 = gauge-3" = the Gauge_04 category error (internal complex vs real spatial); the **one live bridge = 3D-special topology (braiding/knotting is 3D-only)** — IF channel structure is braiding-based, spatial-3 fixes internal-3 (the "slim rescue," unbuilt). AP's lattice-connectivity spitball (node has 6/18/26 neighbors, not 3; ED isn't a lattice anyway) and the existing 3+1 derivation both point here. **BRAIDING PROBE 1 DONE 2026-06-28** (`foundations/Braiding_3D_CommitmentOrder_Hypothesis.md` + `evaluation/Braiding/linking_3d_vs_4d_probe.py`): quantitatively confirmed a link is **HELD in 3D, ERASABLE in 4D** (3D unlink forces collision, min-dist→0.000; 4D slides past, min-dist 0.600) — AP's 4D-instability intuition, proof-by-elimination (2D can't link, 4D unravels, 3D only). **Crank-rail catch:** corrected the mechanism — worldline-braiding is 2-spatial-D (anyons); the 3-spatial-D carrier is **SPATIAL LINKING of 1-D chains (codim-2)**. **NEXT PROBE (open): does ED hold its commitment-order as a spatial linking of its chains?** — the ED-specific test that would turn "3D is where links hold" into "ED *forces* 3D." The dimensional half is can't-be-otherwise; the ED premise is what remains. **Still-open hard core: the full topology→spin-rep classification, why-the-weak-force-is-chiral (η-thread), anomalies (SQ3), and now "why internal d=3 / the braiding bridge."** *(2026-07-01: a same-day simulation attempt at the chains-as-links question was retracted — hand-built proxy rather than the certified rule, and it assumed a pre-given 3D space to test whether 3D space is reached for, circular. Reframed via a scoping note* (`foundations/ChainsAsLinks_Scoping.md`) *into a sharper, non-circular, non-simulation question: does ED's channel-composition graph contain an intrinsically-linked substructure — a Petersen-family minor — a real existing graph-theory property (Robertson–Seymour–Thomas) that forces linking under ANY 3D embedding without assuming one first. **Checked (`ChainsAsLinks_SingleChainNegative.md`): a single chain's own composition rules (sequential/branch/merge, per P07's channel documentation) form a series-parallel graph, which is always planar and therefore CANNOT be intrinsically linked — a clean, correct, narrow negative, no simulation needed.** N-N-style caution applied on purpose: this closes the door only on the single-chain toy diagram, NOT on ED's full multi-chain participation graph (cross-chain V5 correlations, uncharacterized), the same discipline that kept N-N itself from wrongly binding ED. **EXTENDED 2026-07-01** (`ChainsAsLinks_MultichainV5_Reachable.md` + `evaluation/Braiding/multichain_v5_minor_check.py`): built the multi-chain graph directly from ED's own two rules (single-chain composition + V5 pairwise cross-links, no continuum/embedding assumed) and searched for a K6 (Petersen-family) minor — validated the search itself first against controls (caught and fixed a bug that failed even a literal-K6 sanity check). **Result: a genuine, constructive K6-minor witness appears at modest V5 coupling density (~20-35% of chain-pairs correlated, avg degree ~4-5.5)** — a real proof this graph is intrinsically linked at that density, not a maybe. Honest limits, all load-bearing: (i) the coupling density is a free swept parameter, not a derived ED value — no substrate-level number for "how richly V5 correlates chains" exists anywhere in the corpus; (ii) the coupling was uniform-random, not the geometrically-local proximity structure V5 actually has, which likely needs higher density for the same threshold; (iii) even an intrinsically-linked graph hasn't been shown to be the mechanism ED's order-holding actually uses (the operational "can you undo an order without collision" test is still untested). **#2b's fourth item upgraded: structurally reachable and concretely demonstrated in a simplified model, not yet shown to be ED's actual regime.** **Checked (a) 2026-07-01: no substrate-level estimate exists (searched — τ_V5/ℓ_V5 always symbolic, "regime-dependent, inherited" everywhere in the corpus; one weak indirect analogy via polymer-entanglement density in the soft-matter regime, flagged as thin — see reply log). Checked (b) 2026-07-01** (`ChainsAsLinks_LocalVsRandom_Correction.md`): built a local/proximity coupling model (small-world, no embedding assumed) and swept it against pure randomness across 40-200 chains, 2-20% reach — **locality made no difference at any scale tested; density alone sets the threshold, correcting the earlier caveat that local coupling would need much higher density (it doesn't, at least at these scales).** The reachability result is therefore more robust than first stated. **Attempted (c) 2026-07-01** (`ChainsAsLinks_OperationalTest_Status.md`): a real, three-round attempt at the operational undo-an-order test. Fixed a genuine bug (branch sets aren't loops — imposed geometric closures on scattered points don't test the real hypothesis); fixed it properly (used real graph cycles via K6's opposite-triangle structure + verified cross-edges + BFS paths, not imposed point-orderings); found initially-promising near-integer linking numbers (+0.98, +0.94, +0.92) that did NOT survive a resolution-convergence check (densifying the same real paths drove all ten splits toward small/zero values — the near-integers were themselves sampling artifacts, same failure mode caught and corrected mid-arc). **Net: the abstract graph-theoretic K6-minor result stands solid and untouched (Robertson-Seymour-Thomas guarantees SOME embedding shows the linking); no trustworthy geometric witness of that guaranteed linking has yet been constructed, so the actual operational test (does undoing an order force a collision) remains genuinely untested after a real, honest attempt.** Three real methodology fixes in a row, each catching a distinct flaw, is itself a signal the next attempt should use an established linkless-embedding-detection method rather than a fourth ad hoc construction. Still untested: whether the local/random coupling equivalence holds at much larger scale than the 200 chains checked here.)* **CONSOLIDATED 3D REVIEW 2026-07-03** (`foundations/ThreeDimensions_ConsolidatedReview.md`): single cross-session verdict, correcting session-local over-hedging. **The 3D case is a coherent structural bridge with real positive support, one named premise, one shared open bridge — NOT 'unresolved.'** Solid: linking topology (rigorous); internal d=3 reduction (structural + falsifiable, no SU(N≥4)); multi-chain graph's reachable linked structure (measured); locality-blind-topology / locality-bound-geometry complementarity (measured, `AreaLaw_FromStraddlingEdges`). The one genuine open premise (narrow): that ED's dynamics HOLD committed order by the linking its structure demonstrably supports (prior operational-probe 'failures' were tooling, not substrate negatives). The shared open bridge (NOT 3D-specific): curvature-emergence (metric from length-less graph), which sits under the whole geometric arc equally. Read this as the calibration correction: rate 3D at the same tier as the rest of ED's geometry, not as the shaky one. **UPDATE 2026-07-10 — CHIRALITY CASTING RESOLVED AS INHERITED (Wall-1 confirmed by computation; the native-chirality sub-arc closes).** The whole native-chirality push (`foundations/T4_07..T4_13`, `RepSpectrum_*`, `evaluation/ChiralGauge/rep_spectrum_casting_winding.py`) ran to a clean line. ED's own machinery builds the entire **stage**: γ⁵ = arrow × spontaneous orientation grounded (T4_10); the orientation is provably *spontaneous* (parity-clean rules cannot fix it, gate i-a) and the emergent 3-space provably *orientable* (no reflection primitive, T4_09); AP's highway-lane idea supplies the *capability* split at the operator level (the S-odd coupling sector: empty for N=1 → forced vector, nonempty for N≥2; T4_13). **Keystone computation:** parity-cleanness FORCES the point-gap winding of the commitment map to be **identically 0 for all N** (theorem: `det H(k)` even in `k` ⟹ the path retraces ⟹ winding 0) → the clean substrate is **VECTOR for every channel-count** → **parity violation is *necessarily* spontaneous (SSB), now a theorem, not a posture.** What ED does NOT derive is the *assignment* (which channel-count is chiral): the SM's *non-monotone* {vector, chiral, vector} matches neither the clean (0) nor a natural broken (monotone-N) winding — it is the **representation-theoretic** fact (SU(2) fundamental pseudoreal vs SU(N≥3) complex, `T4_12`), **inherited**, exactly as {1,2,3} is (Gauge_09) and every constant. **Two independent ED routes — geometry (T4_13) and arrow-stability (the winding) — both deliver capability and both miss the selection**, strong convergent evidence the casting is genuinely not an ED dynamical output. **Wall verdict: Wall-1 (extension-away / value-inherited), CONFIRMED** (the 2026-06-23 "provisional Wall-1" is now settled). Distinctive prediction stands, firmer: ED parity violation is a spontaneous, contingent, per-universe orientation correlated with the baryon sign, vs the SM's law-level fixed handedness. **Matter-sector chirality is no longer "highest-leverage open" — it is resolved-as-inherited; the residual open piece is anomaly cancellation (untouched, MS-II §8).** **UPDATE 2026-07-10 — T4 (substrate→Dirac) WORKED TO CLOSURE in ED's standard sense (`foundations/T4_14_Closure_*`).** The reduction is now **form-complete + computed**, not the gaping arc it read as: (1) the Dirac operator form is force-forced (KG factorization, Paper_106); (2) the substrate-V1→Dirac *continuum limit* that Paper_106 §3.7 flagged "asserted via inheritance, OPEN" is the canonical Clifford contraction `γ^μ∇_μ` (from T2's Cl(3,1) + P03 direction-indexing + P05/V1 retarded transport), whose continuum limit is `iγ^μ∂_μ` (computed, SQ1f) and whose arrow-retardation is the Wilson term that **undoubles the Nielsen–Ninomiya doublers 16→1 (re-verified 2026-07-10, `chiral_3p1d.py`)**. Paper_106 §3.7/row-15 updated with a dated pointer (OPEN → form-identified + continuum-limit-computed). The apparent "vector-like survivor" is NOT an obstruction — vector-like IS the Dirac fermion; its chirality/casting is the separately-closed (inherited) weak-force question. **Inherited (value/structure, by ED's own logic):** the Cl(3,1) spinor structure (T2/M1, via the acoustic metric = the continuum arc), the metric signature, the mass. **Residual:** non-hermitian→unitary emergence (→ QM keystone / ED-10) + the fully-covariant canonical-V1 proof (→ continuum arc). **The one genuine opening:** the graph-native spinor (build the 4-spinor from P07 channel d.o.f.) — blocked, canonical P07 has no native topology (T4_04 fork a) = the **same channel-topology wall as the rep-spectrum**; major, uncertain. **NET: T4 exhausts into form-forced + value/structure-inherited + one located wall — "closed to ED" in the operative sense (no from-nothing derivation is claimed anywhere in ED). The matter sector now reads like gravity and the gauge sector: form delivered, values inherited, one wall.** Of the three structural arcs, T4 is now the *most* resolved; live rigor tails = Solèr (QM) + continuum/curvature (which T4's spinor also leans on for the acoustic metric).

    **HARDENED 2026-07-08, audited 2026-07-10 — #2b has RESOLVED into derived-structure + two walls; the June "arrow → net chirality, promising" framing above is SUPERSEDED** (`foundations/T4_04_EmergentSpinor_Attack_Scoping.md`, `Paper_ParityWall_ChiralityVerdict`, `Gauge_08/09/10/11`, and the consolidated audit `foundations/ChiralGauge_Gate2_StateAudit_2026-07-10.md`). Three parts: **(A) Gauge + spinor STRUCTURE = substantially DERIVED (structural):** `SU(N)` from channel multiplicity (Gauge_01), P05 = `U(N)` lattice connection (Gauge_02), gauging generically non-abelian (Gauge_10, `F≠0` 2000/2000; the `SU(N)` mixer is **P05 not V5** — retires the "V5=mixer" analogy), YM action `−¼Tr F²` from the plaquette coherence-deficit (Gauge_06, numerically verified), gap ⟺ non-commuting channels (Gauge_07, mechanism), single hypercharge `U(1)_Y` = the one global P09/arrow phase (Gauge_11) → `∏SU(N_i)×U(1)_Y = SU(3)×SU(2)×U(1)_Y`, spin-½ double cover from tethering (T4_03). **(B) UNIQUENESS {1,2,3} = OPEN WALL:** both routes down (spatial = category error Gauge_04; stability refuted Gauge_09, symmetric multiplet stable for all N); reduces to "why internal channel-amplitude `d=3`" (Gauge_08); one live unbuilt lead = 3D-special linking/braiding. **(C) Weak CHIRALITY = WALL, inherited not derived:** T4_04 triple-confirmed the emergent fermion is vector-like by default — the SQ1g discriminator reduces to the *screw pitch* `n_z` (frame-rotation about the arrow), which the canonical time-arrow does not force; canonical P07 is combinatorial (no topology/handedness); first-arrival imprints **C (matter/antimatter, parity-even P09 circle) but NOT P (no parity-odd order parameter)** — this **retires the FirstArrival keystone's C/P-unification P-half** (banner applied; C-half/baryogenesis stands). The June optimism was 1+1D dimension-special. **Corridor audit (2026-07-10):** ParityWall flags two open corridors; **corridor 2 (P09 anomaly) COLLAPSES** (a chiral anomaly or `θ F∧F` needs a prior parity-odd input the parity-symmetric substrate can't supply — self-defeating), **corridor 1 (irreducible/full-4-spinor generator) = the standing §3.7 gate** (the full channel-topology→gauge program, major, unbuilt, no guarantee of handedness). **Net: gauge structure banked; chirality is inherited-by-current-evidence (open only through the one unbuilt gate, four times confirmed absent elsewhere — a well-founded but not *proven* wall); uniqueness {1,2,3} is an open wall.** **Documentation debt flagged same day:** the current headline paper `Paper_MS-II` (and superseded `Paper_MS-I`) still overclaim the retired P-imprint ("first commitment imprints both C and P; V5 is where parity lives") — they predate the 2026-07-08 flip and need a correction pass. **GUT positioning consolidated 2026-07-10** (`foundations/GUT_ThreeForces_StateOfPlay_2026-07-10.md`): ED unifies the three forces at the *mechanism* level (channel multiplicity — deeper than SU(5)), derives their *form* (non-abelian Yang–Mills), forbids `SU(N≥4)`, and gives topological charge quantization (B4, integer winding — but NOT the fractional spectrum); the one open wall is "why `{1,2,3}` = why internal `d=3`" (spatial route = category error, dead; the **linking bridge IS built** as a structural argument in `Paper_MS-II §7` — the two threes meet at linking, category-error-free — resting on one half-settled premise [order is geometry-held, certified sim] + one quantitative gap [3D-linking → *exactly* d=3 is an identification, not a derivation]). **Sharpest forward lead = ANOMALY cancellation** (standard physics pins `N_c=3` partly via anomalies; ED has the anomaly-free side in B4's exact Gauss law; does ED's anomaly-freedom + matter content force `d=3`? — gated on the T4 gate + SQ3, both open).

3. **Diffusion / continuum, the one untested route.** Does a *different* ED model (the Q-C / UDM diffusion, posited at the continuum) actually produce diffusion? The certified Σ-substrate provably can't (the CoarseGrain trilogy: contraction not mixing, kinetic lattice-gas not a PDE). Caveat baked in: a different model may be diffusive *by construction* (consistent, not deep). Honest framing: "diffusion lives a model/layer up, at the named kinetic→hydrodynamic rung." **DECOMPOSITION RE-CHECK 2026-06-26 — a third, distinct outcome** (`evaluation/CoarseGrain_Arc/diffusion_coherent_decomp.py` + `Diffusion_CoherentMean_Finding.md`): after #2 flipped (coherent field = Maxwell) and #5c did not (no Gaussian signal), tested whether the **ensemble-mean** density (signal; ballistic-worldline disorder averaged out) obeys diffusion. **Result: no flip to diffusion — but a clean continuum law DOES emerge, and it is transport/eikonal, not diffusion.** Ensemble-averaging cleaned the dynamics dramatically (step IC: regression R² 0.10 single → **0.60 mean**), revealing a coherent continuum equation — but **eikonal/transport (|∇ρ|, R²=0.60) beats diffusion (∇²ρ, R²=0.26) decisively.** The mean spreads ballistically (∝t), not diffusively (∝√t), because ED's worldlines are straight, not random-walk. **So ED's coarse density continuum is ballistic transport, not the diffusion PDE — diffusion was the wrong target** (the "many CGs" point: Newton vs GR; ED gives the transport law). Confirms the CoarseGrain trilogy's kinetic-lattice-gas verdict at the ensemble-mean level. **THE THREE-OUTCOME ARC (the real result): #2 Maxwell = coherent part IS the textbook object (window); #3 diffusion = coherent part is a clean continuum law but a DIFFERENT object (transport, not diffusion); #5c Gaussianity = NO coherent object (genuinely non-Gaussian, a wall). The discriminator — is there a coherent signal under the disorder, and is it the object you asked for? — is the content.** Crank-rail: my prior (mean stays ballistic) was right this time — did NOT force a flip. Notebook only; no external prediction (per AP). **DIFFUSION ARC 2026-06-26 — the tracer route + the UDM connection** (`evaluation/CoarseGrain_Arc/{tracer_diffusion_test,mobility_recovery_test,crowding_capacity_test}.py` + `Diffusion_Arc_Finding.md`): prompted by AP's "Brownian tracer" idea. **(1) ED DIFFUSES** — a single worldline in a disordered medium has MSD∝t^1.18 with VACF→0 (velocity decorrelates), and a front packet spreads R∝t^0.51 = Fickian — *robustly* (flipped my "no-diffusion" prior). Reconciles #3: **walkers disperse diffusively (Lagrangian); the deposited density-field evolves by transport (Eulerian)** — eikonal in smooth media, diffusive in disorder. **(2) NOT the UDM degenerate mobility** — D_eff is flat in density (β-fit=−0.1) and the packet exponent stays 0.5, amplitude-independent, as the core density climbs to ~3.0. **Sharp reason: the certified rule has NO capacity** (ρ unbounded, fronts never trapped). So **UDM = linear diffusion + a capacity ρ_max; the substrate supplies the diffusion bottom-up, NOT the capacity.** The capacity (jamming/max-packing) is AP's empirically-validated constitutive law (M∝(ρ_max−ρ)^β, 11 materials R²>0.986) — the physical heart of the UDM, absent from the bare worldline rule (but = the P04 bandwidth-capacity b→0 from the GR keystone, an ED ingredient not in this minimal rule). **"UDM ED-CGed?" — axiomatically YES (AP's derivation, data-validated, untouched); bottom-up = diffusion yes + capacity-as-added-(validated)-ingredient.** Upside-only test; UDM stands on its data regardless. Notebook only; no external prediction (per AP).

3b. **The coarse-graining decomposition program — "which floor casts which shadow," + the debt ED owes.** (Framing + open tests, from the #2/#3/#5c arc + AP's "it's all CGed" synthesis; writeup in `Desktop/Book 2/Everything is CGed.txt`.) **The organizing question:** every continuum law is a shadow cast by some coarse-graining floor — ask of each, *which floor casts it, and does ED's averaging climb high enough to cast it?* Run three times so far: **#2 Maxwell** (cast at this floor → window), **#3 diffusion** (ED casts *transport* here; diffusion is a floor up → different object), **#5c Gaussianity** (not reached — the bell curve needs the independence the committal substrate may never manufacture). **The evidential claim, honestly tiered:** one substrate casting many *independent* known shadows from *unretuned* primitives = **consilience** = the right kind of evidence for a substrate theory (how atomism was won — one hypothesis explaining Brownian motion + chemical ratios + gas laws + black-body). **BUT we are not there yet:** (i) we have **one** genuinely independent recovery (Maxwell); (ii) #3 transport = ED *confirming its own known ballistic character*, not an independent needle; (iii) even Maxwell is **partly expected-by-construction** (lattice gauge theory → Maxwell continuum, MS-I) — the right *bones* doing what bones do, not lightning from nowhere. So: **evidence-that-accumulates, NOT proof; strength scales with the number of independent non-trivial shadows cast before a wrong one** — and the wrong shadows are the most informative (a substrate that never casts a wrong one isn't being tested hard enough). **THE DEBT (standing obligation — ED owes the shadows where they are measured):** "it's all CG" does NOT free ED from matching the CG world — lossy ≠ wrong; ED still owes the observed continuum objects *at the layer they're measured.* The CMB **is** Gaussian (measured, ~0.1%); a non-Gaussian substrate is fine ONLY IF ED shows Gaussianity reappears at the observed scale. **Unpaid bills: (i) Gaussianity at the observed layer; (ii) diffusion at the hydrodynamic floor above transport; (iii) the asymptotic Coulomb tail (#2's finite-box caveat).** **OPEN TESTS — the three other ways to CG toward Gaussianity (AP's "go back to attempts 1–3"):** **(1) coarse-grain past the correlation length** — the proper CLT test (we only did small R_cg; measure the correlation length: finite → CLT should fire at large scale → Gaussian; scale-invariant → never; the anti-CLT result *leans* scale-invariant but MEASURE it); **(2) log-density** (log-normal hypothesis — densities often are); **(3) Fourier-mode amplitudes** (the actual GRF definition — a field can be lumpy in real space yet Gaussian in mode space; the most *correct* test, not yet run). Routes 2 & 3 are the promising ones (route 1 has the anti-CLT headwind); all cheap. **Pursuing these builds the consilience case AND pays the Gaussianity debt at once.** Notebook/program-level; no external prediction shipped (per AP).

3c. **The Layers Program — *The Arrow Sorts the Continuum* (3b, matured).** **[2026-06-27/28]** 3b grew into a full arc with a capstone paper (`ED Generative/physics-papers/substrate-evaluation/Paper_TheArrowSortsTheContinuum.md` + PDF; supersedes the 3 retired CG papers; working files in `ED Generative/layers/`). **Thesis: one primitive — commitment (P11, the arrow) — sorts every continuum law into three classes by what it does with the arrow's *memory* (correlations/phase):** (a) **committal structure-MAKING** (keeps memory) = layer-1-native — transport/eikonal (measured, #3), shocks, coherent Maxwell (#2), aggregation; (b) **dissipative structure-ERASING** (forgets memory) = layer-2 + **one forced operator** (the unique 2nd-order isotropic-local-conservative gradient-flux Laplacian ∇·(M∇φ); Gaussianity = its heat-kernel; capacity = its degenerate mobility) **OR one constraint** (incompressibility/conservation); (c) **reversible structure-PRESERVING** (needs the arrow absent) = an **EDGE** — the **soliton test is a measured NEGATIVE** (ED disperses; defocusing + dissipative), commitment forbids the reversible balance. **Operator spine:** the heat (∂ₜ) and wave (∂ₜ²) operators share the Laplacian; the arrow = the time-derivative order. **Bridge (measured):** the geometric corner **IS** GR-III's bandwidth rule (ḃ=D∇²b−κρ) — layer-2 Ricci-smoothing + layer-1 commitment-concentration/horizon → the layers map and the gravity/info arc are **two faces of one commitment**. **Derived:** YM action (`Gauge_06`, coherence-deficit→Wilson→F²) + gap mechanism (`Gauge_07`, gap = non-abelian-channel signature). Full sweep of the 14 AD-Atlas PDEs. Honest tiers throughout; the soliton no + primality are the proven edges; "field" = emergent shadow not primitive (ontology rail). **Status: capstone paper drafted + breaker-passed.** Open: a voice pass before release; eventual fold-back of the YM/gravity results into their home arcs.

**PREDICTION INVENTORY — the north-star meta-target.** **[flagged 2026-06-28, AP]** Survey the **whole corpus** for every **falsifiable, experiment-facing NOVEL prediction** — the *failable* ones, NOT postdiction/consilience (per the "reproducing known laws is hard to fail at" point: a known law is both easy to fudge toward and cheap to match). Output = a prediction map: what's predicted, how falsifiable, what experiment tests it, which are already-bounded vs latent/underexploited. **Why it's the priority:** AP's north star is the *indisputable confirmed novel prediction* — the only kind that ends arguments — so ED needs the inventory of where one can come from. Candidate seeds: **preferred-frame α₂=0-exact / α₁-tiny-but-nonzero** (#1, a structural prediction); **a₀/MOND/BTFR** (#5/#5b); the **synchronized H₀ co-variation** of SCBU's six projections (#5); the **gauge-handedness ↔ cosmic-matter-sign correlation** (#2b); the **entropic-time** family (Barontini); **stable SU(N≥4) forbidden** (Gauge_03, if salvaged). NOT yet a doc — this is the build. Highest-leverage for the program's *goal* (being undeniable), distinct from the research targets that feed it.

## B. Open consequences of the big targets (mostly numbers waiting on #1)

4. **ED-10 horizons + ED-Information.** Structure and scalings done **EFE-free**; the two emergent-horizon thermo numbers are now **delivered** (area-law `S∝A` measured, Hawking `T∝1/r_h` derived — see #1). Residual: **unitary completeness is open** (Page-curve *structure* reproduced; full unitarity not), plus the quantitative missing-info coefficient (the `S=A/4` factor — **attempt logged 2026-06-28** `BH_EntropyCoefficient_FromEventCounting.md`: ED owns half the 1/4 [κ=1/(2r_s) geometric, derived] + inherits half [the thermal 2π]; AP's geometric route reframes the 4 as the sphere/great-circle ratio [A/4 = the horizon cross-section = the r_s/2 sphere]; **decisive count run**: N_surf/(4π r_s²)≈0.78 = full-sphere tiling, so the cross-section route is exact geometry but not how the substrate tiles → the 1/4 is the thermal factor. **AND the thermal 2π is now DERIVED** `BH_Thermal2Pi_FromNearHorizonRindler.md`: ED's b=1−r_s/r has the **Rindler near-horizon form** (b→κ²ρ², confirmed), smoothness forces κτ period 2π → T=κ/(2π) → **S=A/4 fully structural** (½ from κ + 2π from the b-profile shape, both ED-derived; on par with GR, which also uses the Euclidean continuation). Remaining frontier: the continuation-free, commitment-statistics derivation of the 2π) and `EDInformation_Round1 §8`'s five open questions (A1 shared-past mutual info, re-expression fidelity P04/P11 lossless?, complementarity/firewall sharpness). Round-2 waits on #1's number. **AREA-LAW-AS-STRADDLING-EDGES 2026-07-03** (`foundations/AreaLaw_FromStraddlingEdges.md` + `evaluation/AreaLaw_Arc/straddle_area_law_probe.py`): tested AP's `LengthInED_FreeWrite` idea that a surface's entropy = the count of V5 cross-chain (entanglement) edges piercing it. **Short-range V5 → clean AREA law (straddle count ~ r^2.02, reach-independent); could-say-no fired (fully long-range V5 → ~r^2.7, volume-ward).** Sparse long-range tail (the ER=EPR wormhole edges) COEXISTS with the area law (2-10% tail stays r^2.1-2.3), degrades only when long-range dominates — a measured reconciliation of the "entanglement is any-distance vs area-law needs locality" tension. Unifies the ER=EPR echo (Paper_071, V5 = shared object) with the ED-10 area law into ONE graph-native object (the straddling-edge count) — the holographic area law in ED's own language. Complementarity with chains-as-links: locality is IRRELEVANT to entanglement's topology (K6 linking) but DECISIVE for its geometry (area law). Honest scope: assumes the emergent 3D geometry (does NOT derive it — the graph→metric bridge / curvature-emergence stays the open piece), reproduces a KNOWN result (consilience, not a novel prediction); the new content is the identification + reconciliation. Flags a candidate S=A/4-coefficient handle: if entropy = edge count, the 1/4 is edge-density-per-area, not purely thermal.

5. **SCBU + scale correspondence.** **M3-closed; Route A4 now supplies a substrate-parameter-INHERITED foundation (post-2026-05-17 — this is what the prior snapshot missed).** The six-projection four-regime model (one boundary `R_H=c/H₀` → BH horizon, cosmic decoupling, `ξ_canonical`, `a₀`, `M_crit`, `Q≈3.5`) is form-forced; `H₀` is form-derived from `Θ_ED` via Route A4. **The one live tightening = per-projection scaling exponents** (the ED-10 R3 scaling method is the tool); closing them **upgrades all six projections M3→M2/M1 simultaneously** and sharpens the headline falsifier (synchronized `H₀` co-variation of the six projections — testable-in-principle in the Hubble-tension context, sharpened from "family of falsifiers" to "specific covariation law" once exponents are derived). No sim/rerun baseline needed. **Internal cleanup owed:** `Paper_EDSC_SaddleClassification` still frames `r*` as an S1 invariant, but `r*` was audited and retired (pooled R²≈−1.88, filter-dependent) → it's S2 by their own scheme; add the caveat (cosmetic, no verdict change). **DONE 2026-06-26:** the `scale invariance/Paper_EDSC_SaddleClassification.md` copy already carried the correction (Preamble item 7, 2026-06-23); the un-corrected duplicate `scale correspondence/Paper_ED_SC_4_9_SaddleClassification.md` is now brought in line (added Preamble item 7 + fixed the Rewrite-Note cross-ref: r\* retired from invariant status, filter/motif-dependent, **not S1**, S2-vs-S3 unsettled). No verdict change. CLOSED.

   **SCOPING CORRECTION 2026-07-02** (agent audit, cross-checked against `SCBU_SubstrateEvaluation_Bridge.md` §5, `Update_ED_SC_4x_Arc_M3.md`, `Memo_ED_RouteA_A4_Construct.md` §7.2, and `Paper_ED_SC_4_2_xi_canonical_H0_derivation.md`): the "ED-10 R3 scaling method is the tool" / "no sim/rerun baseline needed" framing above **overstates readiness**. ED-10 R3 (`foundations/ED10_Horizons_Round3_QuantitativeRates.md`) is a horizon-thermodynamics rate-equation *pattern* (write the substrate rate equation, extract the exponent, leave the coefficient inherited), not reusable machinery — the "it's the tool" line traces to one proposal sentence in the bridge note, never actually executed. Applying it to SCBU means writing new substrate mechanisms **from scratch per projection**, not running an existing tool. Per-projection state is uneven, not six symmetric small gaps: **a₀** already has a real derived exponent (γ=1, Paper_029 §5.1, mechanism-backed) — nothing to do. **R_H** is trivial by definition (`R_H=c/H₀`) — nothing to derive. **ξ_canonical** has real scaffolding but its own paper (`Paper_ED_SC_4_2`) explicitly **failed to close**: only Route A closed, and only at the weaker substrate-parameter-*inherited* level (not form-derived); Routes B (`β(K)` functional form) and C (`K_boundary` at `R_H`) remain fully open and untouched since. **M_crit and Q≈3.5 have no proposed mechanism connecting them to H₀ at all**, in either source paper (`Paper_ED_SC_4_4`, `Paper_ED_SC_4_5` — the latter states `Q≈3.5` is "canon-internal... not derived from primitives," full stop) — this is invent-a-mechanism-from-zero work, not gap-closing, the same kind of move Paper_029 had to make for a₀. Also: the "M3→M2/M1 simultaneously" upgrade claim traces to `Memo_ED_RouteA_A4_Construct.md` §7.2 (2026-05-16), which was **walked back the very next day** by `Update_ED_SC_4x_Arc_M3.md` (2026-05-17) — verdict stays M3, exponent work explicitly demoted to non-load-bearing "pending." **Honest reframe: this is real, uneven-difficulty, mostly-from-scratch derivation work, not a scoped-and-ready tightening pass.** Does not retract the CLOSED status of the saddle-classification cleanup above — only the "easy tool exists" framing of the exponent tightening itself. If picking this up: ξ_canonical Route B is the best-scaffolded entry point; M_crit/Q need a mechanism invented before any exponent question is even well-posed.

5b. **Khronon-MOND clusters/CMB regulator dial — the MOND arc's highest-leverage open.** The KM arc is structurally complete and internally consistent (MOND embedding derived, stability passes, orthogonality theorem isolates statics from the regulator, one-Λ thesis at D-via-I). The **clusters/CMB debt is owed, not discharged**: no regulator member of the `𝒲(A²,Θ)` family is chosen, no CMB spectrum computed. By the orthogonality theorem this dial is **provably invisible to the galactic tests that already pass**, so it can be set observationally with no retrofit risk — "one function, five independent filters, the universe as judge." Also open (deliberately guarded against reverse-engineering): the primitives-level origin of the deep-IR `√(a_N a₀)` branch; and the ab-initio V1 boundary integral for Λ's *magnitude* (structure/sign/scaling derived; magnitude = CC-smallness, see #10). **Λ's equation-of-state FORM `w=−1` — RESOLVED-AS-FORCED 2026-07-16** (distinct from the magnitude above; AP's a₀-vs-Λ horizon question): Λ is constant (`w=−1`, as Cos_05 states) and this is *forced by viability*, not chosen. Both a₀ and Λ tie to the one Hubble horizon (`R_H=c/H`, Paper_028, explicitly *not* the GR event horizon per §3.5), but a₀ rides it *live* → `a₀(z)=cH(z)/(2π)` evolves, whereas a live-horizon Λ is `ρ_Λ∝H² ⟹ w=0` (no acceleration — the Hubble-cutoff-holographic failure; 038_5 §3.4's own `M_P²H²` form is exactly this if evaluated live), so Λ must be the *frozen* saturation-floor value. a₀ is a scale (rides live H harmlessly), Λ is a vacuum energy (cannot). **New OPEN (small, sharp):** does the substrate *cleanly force* the frozen-floor value or *stipulate* it? Cos_05's saturation regime supplies 'frozen', but the horizon integral alone doesn't, and there's a bootstrap knot (acceleration is needed to reach the de Sitter state that freezes H; ED's actual driver = matter-dilution, Cos_05 §3.3). Stale flag fixed same day: Paper_038_5 §3.5 open→resolved. **CMB-dust debt SHARPENED 2026-07-17 (first-pass derivation, unverified — checkable):** the debt is NOT a regulator-shape choice. Proof: KM-II's cosmological slice `𝒲(0,Θ)` is a local function of `H` (`Θ=H/H₀`), so `ρ_𝒲=ρ_𝒲(H)`; but `w=0` dust (`ρ∝a⁻³`, `ρ̇=−3Hρ`) forces `ρ'/ρ=−3H/Ḣ`, which is era-dependent (`ρ∝H²` in matter domination vs `ρ∝H^{3/2}` in radiation) — no single `ρ_𝒲(H)` can be dust across the rad→matter transition the CMB peaks probe. Dust needs *volume-memory* (an `a⁻³` integration constant), i.e. a `Ḣ`-dependent / constraint structure, NOT a shape of `𝒲`. So the CMB dust requires a **structural addition = the mimetic constraint** (unit-gradient dust mode; mimetic = constrained khronon per KM-II; imperfect-mimetic `(□φ)²` gives the small `c_s` for caustics). This makes KM-II's "non-projectable branch doesn't automatically contain dust" precise: the local `𝒲(0,Θ)` is dark-*energy*-like (Λ + G-renorm + maybe EDE `w(H)`); the *dust* is a separate mimetic-constraint mode. **New crux (was "pick a regulator member"):** is the mimetic dust mode substrate-justified (a conserved, pressureless, arrow-aligned, `a⁻³`-diluting quantity in the commitment/participation structure — candidate: comoving-conserved substrate-grain/commitment-locus density)? Substrate-justification pass DONE same session (first-pass): the cold-dust STRUCTURE is **substrate-native, not an add-on** — GR-II §3.1–3.2 forces the conserved bandwidth current (`∇_μJ^μ=0`, P05+P04) to a pressureless dust `T^{μν}=ρu^μu^ν` (→`ρ∝a⁻³`, `w=0` on FRW; Dyn_02: slow chains=dust). So the "mimetic constraint = structural addition" worry dissolves. **Crux relocates to two open pieces:** (1) dark–baryonic split (a photon-decoupled dust component ~5× baryons; abundance inherited, existence needs a substrate reason — don't lean on contested four-band P04); (2) **THE REAL CRUX = galactic double-counting** — native cold dust that clusters would spoil the khronon-MOND rotation curves; resolution = one field, two regimes (AeST-style: dust cosmologically + MOND galactically, no double-count), which GR-II supports (dust source = bandwidth = khronon). Precise target: show the ONE bandwidth field is `a⁻³` dust at recombination AND MOND (no clustering) in galaxies. Tractable, well-posed. **Reconciled vs OneField capstone + adversarially downgraded (same session):** the capstone/KM-II already assert one-field-two-regimes (dial=dust, sequestered by Orthogonality), but they **conflate `c_s≈0` clustering with `a⁻³` abundance** — the no-go shows no local dial member supplies the abundance, so "dust-capable" = "clustering-capable" only. And GR-II's native dust is *matter generically* (baryons ARE it); a **photon-decoupled ~5×-baryon DARK component is NOT established** (not in corpus; KM says "no DM"). Honest net: debt precisely-mapped, NOT resolved. Two open knots: (1) does a photon-decoupled ~5× dark component exist, or just baryons? (2) galactic double-counting (Orthogonality covers the dial, not the dust abundance). **First-pass chain — verify adversarially before banking or touching the report.** Detail: memory `project_cmb_dust_mimetic_debt`. **KNOT-1 PASS 2026-07-19 — a CROSS-ARC CONTRADICTION found, then reconciled (supersedes the "two open knots" framing just above):** grepping the cosmology arc revealed that **Cos_03 (CMB) and Cos_04 (structure) already RUN ON cold dark matter the gravity arc (KM-I) denies** — they use `Omega_m h^2 ~ 0.143` vs `Omega_b h^2 ~ 0.0224` -> `Omega_c h^2 ~ 0.12` (~5.4x baryons), the exact Planck LCDM values, labeled "IC-INHERITED from CMB cross-consistency" (Cos_04 names Omega_m explicitly + uses it in the growth eqn; Cos_03 hides it inside inherited CAMB/CLASS peak heights). "IC-inherited" hid a *component* (a species) behind a *value* label. **Fix to the task-1 no-go:** I had treated the khronon as a foliation FUNCTION W(0,Theta)=rho(H) (local in H, no volume memory -> correctly can't be dust). But the khronon is a dynamical FIELD; its **mimetic dust mode rho=C/a^3** is a function of *a* (history), not H, so rho-dot=-3H*rho holds in every era automatically — the a^-3 fixed by an **integration constant** of the field EOM, which IS the volume memory the no-go said was absent. The no-go kills the function route, NOT the field/mimetic route. **RESOLUTION (identification):** Cos_03/04's Omega_c = **the khronon's mimetic dust mode**, NOT a foreign CDM particle. "No dark matter" (KM-I) = no dark matter *particle*; there IS a photon-decoupled gravitational dust = the foliation clock's own C/a^3 mode, abundance C inherited (an IC, same status as Omega_c in LCDM), the cosmological face of the SAME field that does MOND in galaxies. **Knot-2 (double-count) escape:** solve ONE field ONCE per regime — cosmologically (expanding slice)->a^-3 dust; galaxy (static/high-A slice)->MOND; the Orthogonality Theorem = the two slices are disjoint = the no-double-count guarantee (AeST/Skordis-Zlosnik 2021 PROVES it threads CMB+galaxies in a sister theory; it is the OneField Letter's own thesis, and fixes its c_s-vs-abundance conflation: abundance=C, clustering=c_s~0, different aspects of one mode). **STATUS: contradiction -> known reconciliation, DEMONSTRATION UNBUILT.** Owed: (1) a direct one-field Boltzmann computation (hi_class/EFTCAMB) of the peaks from the khronon dust — not inheriting CAMB's LCDM-CDM proxy; (2) show the same field gives galactic curves with no double-count; (3) mimetic c_s=0 instabilities need the (box-phi)^2 regulator (a tuning). **CORPUS FIX DONE 2026-07-19:** added a "Dark-component identification" note to Cos_03 (after its VALUE/IC block) and Cos_04 (after its IC line) making Omega_c = khronon dust mode explicit with honest unbuilt-demonstration status. Report stays "live falsification risk" — reconciliation != discharge. **RUNG-0 PASS 2026-07-19 (FRW reduction of GR-II's khronometric structure — refines the identification, less clean than "forced"):** (1) the khronon AETHER gives NO dust on FRW — all aether invariants collapse to functions of H (K_{munu}K^{munu}=3H^2, theta^2=9H^2, a_mu=0) so its contribution is ~H^2 (a G-renorm), confirming the no-go from the action; the homogeneous khronon is slaved to cosmic time (no free DOF, no integration constant). (2) BUT the a^-3 dust STRUCTURE is native via the **conserved bandwidth current** (GR-II 3.1-3.2: div J=0, J=rho*u -> rho*a^3=C on FRW), NOT the khronon — the integration constant C is the volume memory the aether lacks; no Lagrange-multiplier bolt-on needed. (3) **THE FORK (the real result):** native dust lives in the MATTER current = a SEPARATE cold component that clusters in galaxies = DOUBLE-COUNTS against khronon-MOND; the clean one-field/no-double-count escape needs the dust to BE the khronon (Orthogonality) but that requires an ADDED mimetic constraint (ED's NON-projectable branch doesn't inherit projectable-Horava's dark-matter-as-integration-constant free). Can't get native AND one-field-no-double-count from what's on the page. **VERDICT: dust structure NATIVE; the "dark + one-field + no-double-count" package = OPEN.** "Essentially forced" downgraded. Cos_03/04 notes refined to match. Next real fork (Rung 3): can a separate native dark dust dodge the galactic double-count (likely not), or is the added mimetic constraint substrate-justifiable in the non-projectable branch? **DÉJÀ-VU RECONCILIATION 2026-07-19 (AP flagged "we've been here" — correct; this is the authoritative framing):** the whole cosmology/mimetic map ALREADY EXISTS in `foundations/KhrononMOND_Round5_Cosmology.md` §2/§5/§6 + KM-II §6.1 — §2.1 aether→G-renorm (~H², no dust); §5.1 mimetic DM = constrained-khronon = pressureless dust; §5.2 integration-constant DM = projectable/neighboring branch not ED's; §6.72 "non-projectable branch NOT shown to contain the dust automatically"; abundance = integration constant (existence form-level / 5:1 value-inherited). So the Rung-0 pass RE-DERIVED Round-5 (not new), and its "native dust via GR-II conserved current" was a CONFLATION (that current is ordinary-matter continuity = baryons, not dark dust) — DROPPED; Cos_03/04 notes rewritten to cite Round-5/KM-II §6. **P11/P13 → mimetic derivation (the ask): NEGATIVE.** Canonical Paper_087: P11 = discrete irreversible commitment events (not a continuous unit-gradient flow); P13 = time-TRANSLATION symmetry (not a clock-gradient constraint). Coarse-grain the arrow to φ=∫R dτ; mimetic clock = proper time (|∂φ|=1) iff commit-rate R=const, but P-LinRate gives R∝b (non-uniform near matter) → ED's natural clock is non-unit-gradient = khronometric (no dust). Arrow points to khronometric, AWAY from mimetic; the constraint is an ADDITION, confirming+sharpening Round-5 §6.72. **LIVE HYPOTHESIS (not banked):** R∝b uniform on smooth FRW (→mimetic dust) vs variable in lumpy galaxies (→MOND) ⟹ the mimetic/MOND split may BE the Orthogonality A→0/A-large split — one commit-rate clock, dust cosmologically + MOND galactically, no double-count, emergent from P11+P-LinRate (architecture A, naturally). Unproven step: does coarse-graining a uniform-rate discrete arrow yield the mimetic dust stress-energy? = Round-6 candidate (c). **COARSE-GRAINING CALC DONE 2026-07-19 (first-pass analytic) — SPLIT verdict:** φ=commitment clock, R=dφ/dτ=αb (P-LinRate) → mimetic scalar X=−(∂φ)²=α²b²; constraint X=1 ⟺ b=const. **Step 1 (regime split): HOLDS** — X const where b uniform (cosmology→mimetic), varies where b lumpy (galaxy→MOND); the mimetic/MOND split IS the Orthogonality A→0/A-large split, straight from R∝b. **Step 2 (dust): matter route NEGATIVE** — mimetic dust is a conserved a⁻³ density, but P11 irreversibility makes commitments ACCUMULATE (comoving count grows, not a⁻³; actively firing at recombination) → the arrow's density is a SOURCE not dust. **Gravitational route UNRESOLVED+RELOCATED** — the true mimetic dust is the emergent metric's freed conformal/volume mode; whether ED's g~1/b frees one that gravitates as a⁻³ in the uniform-b regime = the graph→metric bridge / curvature-emergence conformal sector (already-open frontier). NET: architecture A NOT free; dust stays an addition; but kills the naive route, keeps the regime split, pins the remainder to ONE known frontier. Tier: first-pass analytic; Step-2 negative is structural. **CONFORMAL-MODE BUILD-OUT DONE 2026-07-19 — the "one known frontier" is CHECKED and closes; dust is an ADDITION, TRIPLE-confirmed:** the conformal mode of g~1/b is ln b = the bandwidth field itself; in ED it is a **slaved static participation density** (`Paper_MetricFromTheGraph_ForcedTo3D`: static, fixed-point read-off, NOT a propagating DOF — dynamical regime = the khronometric keystone, which Rung-0 already showed gives H² not dust), and the curvature-emergence arc is EXHAUSTED (`foundations/ContinuumCurvature_ArcExhaustion_GeometryNativeFieldsAreShadows`) with the verdict that ED casts geometry native but smooth *dynamical fields* (a dust field is one) only as coarse-grained SHADOWS — so a free conformal-mode dust field is not a native ED object by ED's own tested monism. Freeing it = impose the mimetic constraint = the addition. **THREE independent confirmations now (Rung-0 FRW; P11/P13 primitives; conformal mode).** ED's honest CMB position is structurally FINAL: (1) ADD the mimetic constraint (faithful-but-added, abundance value-inherited, = AeST) → CMB becomes an added-field (AeST-class) computation; or (2) accept CMB as a live falsification risk. No third "hidden native dust" option. Reframe: ED's monism POSITIVELY predicts DM-dust is not fundamental (consistent with "not a particle"), which is WHY the CMB is hard — ED's shadows coarse-grain back to the baryons already there, not a 5×-extra decoupled component. Report stays "live risk"; nothing about ED's status changed — bookkeeping clarified + debt mapped. **CORRECTION + HORIZON/SATURATION REGIME-SPLIT 2026-07-19 (AP push — "addition" was OVER-CLOSED; correct status = OPEN):** calling it an "addition" prejudges failure and is as ad-hoc as a DM particle — reject. The three confirmations ruled out THREE routes (khronon aether, arrow primitives, conformal mode) but did NOT test the horizon-at-recombination or the saturated regime. AP's unification demand: ED does galactic DM via the horizon (a₀=cH/2π), so the CMB DM must be the same horizon or an honest distinction. **Horizon calc (done):** a₀(z_rec)=cH(z_rec)/2π≈2×10⁴·a₀(now)≈2×10⁻⁶ m/s²; perturbation g~7×10⁻¹⁰ → g/a₀~3×10⁻⁴≪1 → **CMB plasma is DEEP MOND (~50× boost); the horizon IS active at recombination.** BUT that boost is COUPLED to baryons (deepens the sloshing well, moves with the fluid); the CMB needs a DECOUPLED held-still well — wrong type, which is why all MOND dies at the CMB. So galactic-MOND and CMB-DM are different manifestations naively. **Open unifier (hypothesis):** pre-CMB = SATURATION regime; ED's CoarseGrain arc shows ED goes NON-FICKIAN/trapping/sub-diffusive in dense regimes → trapped structure is HELD = decoupled. So unsaturated→coupled MOND boost (galaxies); saturated→trapped decoupled well-maker (CMB); same horizon+substrate, regime difference = saturation, mechanism = non-Fickian trapping. Native, no 14th primitive, OPEN. Unchecked: whether saturated trapping gives decoupled a⁻³ ~5× quantitatively. So #5b status: NOT "addition" — an OPEN derivation with the horizon/saturation route as the natural, non-ad-hoc, ED-native candidate that unifies galactic MOND + the CMB. **UDM SHARPENING 2026-07-19 (AP pointer — the saturation mechanism is a VALIDATED ED law):** the pre-CMB saturation analog is **UDM = Universal Degenerate-Mobility** (`soft-matter/Paper_085`; `predictions/Paper_ED_Postdictions_PassedTests` §2): `D(c)=D_0(1−c/c_max)^β`, β≈2, from P4 mobility-capacity-bound, VALIDATED across 10–11 crowded systems R²>0.986 (ED's ONLY externally-validated prediction, strongest anchor). Move: the CMB dust needs c_s≈0/pressureless/no-free-stream/clusters-all-scales; **zero mobility IS that** — so crowded pre-CMB → UDM mobility→0 → frozen pressureless decoupled dust, the well-maker, from a validated law not a bolt-on. P11 locks the freeze permanent → frozen stock stays cold, dilutes a⁻³, clusters (the dark dust = frozen STOCK from the saturation era, not the ongoing commitment flux). Unification (Q6): one substrate, one UDM mobility curve — dilute(galaxies)→mobile→horizon MOND; crowded(CMB)→degenerate→frozen dust; two ends of one curve. TIER: SOLID (UDM validated/native); EXTRAPOLATION (validated in soft-matter diffusion; cosmo-substrate use extends P4 to a new regime); UNCHECKED teeth-calc = does recombination-era crowding c/c_max reach the degenerate regime, give ~5× baryons, reproduce peaks? Net: the open route now rests on ED's strongest validated law + P11. Cos_03/04 notes name UDM. No prior UDM-cosmology link (new). **CROWDING CALC 2026-07-19 — NEGATIVE, robustly; UDM does NOT fire at recombination:** crowding c/c_max = ρ(z_rec)/ρ_Planck ≈ 5×10⁻¹⁸ / 5×10⁹⁶ ≈ **10⁻¹¹⁴** — 114 orders FROM saturation; UDM D=D_0(1−10⁻¹¹⁴)²≈D_0 → mobility fully undegenerate, no dust. Robust (vs nuclear packing 10⁻³⁶; vs Planck tick-rate 10⁻⁵⁷). The universe at recombination is astronomically DILUTE in substrate terms; UDM's degenerate regime is PLANCK-ERA, not z~1100. Survives only as a Planck-era freeze relic (untestable, 5× unexplained) — NOT the validated-law-at-CMB story. WALK-BACK of the prior enthusiastic framing. **CMB dark component honest state (harder): NO native mechanism found — mimetic NO (3 routes), UDM-at-recombination NO (114 orders), horizon-boost active but COUPLED (wrong type); only known working option = added field (AeST) = the ad-hoc addition AP rejects. Genuinely OPEN.** Door ajar only if the UDM papers use an epoch-varying occupation c_max reaching ~1 at recombination (not density-vs-Planck) — asked AP. **DECROWDING / INFLATION-EXIT DIRECTION 2026-07-19 (AP reframe — the native lead; crowding was backwards):** read Paper_ED_Cos_01 + Paper_ED_CCC. Inflation IN ED = the SATURATION regime (uniform Ψ, w=−1); its END = "exit from saturation: Ψ becomes non-uniform" = DECROWDING = OPEN (Route C1 / CC-OPEN-1–4). NATIVE PICTURE (direction, not result): the decrowding exit shatters uniform Ψ → ∇Ψ gradients = primordial fluctuations (Cos_01 row 16) + b-gradients = gravity; a fraction gets P11-COMMITTED (single-channel, frozen) → decoupled (if committed to a NEUTRAL/non-EM channel; charge=EM-winding B4), cold (frozen), a⁻³ (comoving frozen stock, P11 irreversible). = the decoupled well-maker at the RIGHT epoch (end of inflation), from ED's own inflation dynamics, NOT an addition. Ringing soup = the un-committed EM-coupled (baryonic) fraction; one transition makes both ripples and dark relics. PENROSE DIVERGENCE (helps): Penrose keeps previous-aeon Planck relics (erebons) as DM; ED §3.8 DERIVES they DISSOLVE at the crossover; + ED's own Scenario-C remnants are future (BHs unevaporated). So neither remnant pop = CMB DM → ED's DM must be inflation-exit-made THIS aeon (reinforces the line). CONTINUATION: (Q1 core) committed:uncommitted split = DM:baryon ratio — forced ~5:1? = Route C1 (PRIZE = a real prediction if forced); (Q2) decoupling = P11 commitment to neutral vs charged channels (ties DM to gauge/charge, T4/MS-II); (Q3) a⁻³ comoving check; (Q4) avoid re-thermalization. NEXT: read the Route-C1/CC-OPEN memos before proposing. TIER: direction, no fatal flaw (unlike UDM), might still wall. **BARYOGENESIS ADVANCE 2026-07-19 (read Paper_ED_Baryogenesis, M3):** ED's baryogenesis IS the saturation-exit admission mechanism — single-template R4: coherence functional 𝓕_coh with a NO-SLACK capacity frontier (P04×P07) forecloses all but one committed template; asymmetry locks in during saturation + PERSISTS after (§3.5). Reuse the SAME machinery on the CHANNEL axis: EM/charged-channel commitment→baryonic; neutral-channel commitment→DARK (decoupled). Gives decoupling (neutral, no EM winding), cold/a⁻³ (committed bound chains), and survival (committed matter persists — the Q4 failure risk, CLEARED) all natively — NOT an addition, the neutral-channel partner of baryogenesis. CATCHES: (1) 5:1 abundance likely value-INHERITED (η_B is) — not a forced prediction unless channel multiplicity forces it; (2) 'neutral'≠'dark' → needs a SEPARATE COLD NEUTRAL sector distinct from SM. → the CMB dark question REDUCES to a matter-sector question: does ED's channel structure (T4/MS-II) HAVE/force/forbid a cold neutral dark sector? DARK-PARTICLE TENSION (AP): a committed cold neutral chain IS an emergent dark 'particle' (antimatter-analogy) — but this REOPENS double-counting vs khronon-MOND (a cold dark particle clusters in galaxies, spoils MOND). Fork: MOND-only / dark-particle / both. Reading T4/MS-II next. **SUPERFLUID-RELIC CANDIDATE 2026-07-19 (AP spitball → the candidate unification; repairs OneField):** MOND and the 5× dark matter = ONE substance read two ways = superfluid dark matter (Khoury), ED-native: the committed relic is DISPERSED=CDM (clusters/CMB, native a⁻³ abundance) and CONDENSED-in-galaxies=a coherent state whose collective mode IS the khronon=MOND. Galactic clumping IS the MOND (not additive) → dissolves double-counting; phase boundary = a₀ (horizon scale). PROBE (khronon derivable as the relic's collective mode?): YES-path — a superfluid phase θ has ∂θ = preferred timelike frame = foliation scalar = the khronon; relic density = the a⁻³ dust; and superfluid phase–density conjugacy = the mimetic constraint, so the "mimetic addition" the session flagged becomes a DERIVED consequence of the relic being a superfluid. Ties everything: abundance=density, MOND=phase, mimetic=conjugacy, decoupling=neutral. Repairs OneField's overclaim ("khronon carries the dust" / "no particle" — both flagged with a correction banner 2026-07-19; ED only "does not refute a particle"). BRAKES (hypothesis, not derived): (1) does the relic condense? (light-boson condition vs ~GeV abundance = mass tension resurfaces); (2) does the phase give ED's specific khronometric structure (c_s=c, α₂=0)?; (3) reconcile with the khronon's arrow-in-the-law derivation (GR-II) — same object? NEXT PROBE = (3). **RECONCILIATION (3) DONE 2026-07-19 — PARTIAL/promising:** GR-II's arrow-khronon and the superfluid-phase khronon are the SAME TYPE; GR-II derives it by DIVIDING OUT the normalization (khronometric → keeps direction=MOND, discards size=density), the superfluid KEEPS the size = the a⁻³ dust. So GR-II is the density-blind projection; restoring what it divided out gives the dust (not a naming coincidence). 3 open tensions (khronon speed c_s=c vs slow MOND phonon; ED's clock khronometric-not-mimetic; regime mapping) all trace to condensation. **CONDENSATION PROBE (the crux):** standard BEC needs light bosons (~eV) → re-confirms the mass tension (light-to-condense vs ~GeV-abundance). ED-NATIVE alternative: condensation = V5-COHERENCE phase-locking (not thermal BEC) — survives in CALM environments, so **below a₀ (galaxies)→coherent→condensed→khronon=MOND; above a₀/hot (clusters)→decohered→dispersed→CDM; a₀ = the V5 coherence boundary** (ED-native, ties the MOND scale to coherence; MIGHT relax the mass condition — unshown). Crux sharpened to: is galactic condensation standard-BEC (mass-bound) or V5-coherence (a₀-bound, possibly tension-free)? = the terminal armchair point (needs real V5-coherence dynamics of a relic gas). **The full dark-matter picture is now a coherent ED-native RESEARCH PROGRAM — a picture, not a result — converged on V5-coherence condensation. Consolidation/write-up point.** Detail: memory `project_cmb_dust_mimetic_debt`; `physics-papers/dark-sector/Paper_ED_DarkSector_Position.md` §5.5.

5c. **Can GRF Gaussianity be derived? — test it before trusting it (bounds the S1 motif sub-arc).** The motif-conditioned-distribution invariant (Paper_EDSC_Motif / SC-4.7) rests on a *GRF regime hypothesis* (Gaussian participation field), currently assumed not derived. Three standard routes: (1) **CLT for a finite-reach field** — coarse-grain over R_cg ≫ the finite kernel reach (V1 width ℓ_V1, V5 memory τ_V5) so each cell sums many near-independent contributions → Gaussian; cleanest, uses structure already present, would give "Gaussian in the hydrodynamic window, *measured*." (2) **Max-entropy** — if DCGT keeps only the two-point (V1+V5) correlator and higher cumulants vanish, the field is Gaussian; really a restatement of (1). (3) **RG-irrelevance at a Gaussian fixed point** — if ξ_canonical is a free fixed point of the DCGT flow, non-Gaussian vertices are irrelevant; since ξ_canonical is already an RG fixed point (Paper_096 / 4.2), this is the only route that gives *forced-from-primitives* — and the hardest. **The obstruction is ED's own:** all three need decorrelation/mixing, but the CoarseGrain / ShadowEmergence arc measured ED's dynamics as **committal/trapping, not mixing** ("locks configs, doesn't decorrelate"; p≈0.45 not 0.25, sub-diffusive) — the *same wall* as PDE-emergence ("you reach the PDE only by leaving ED"). Gaussianity and the diffusion PDE are cousins (both want a mixing substrate), so Gaussianity is not free, and there's real probability the coarse field is **non-Gaussian** (persistent correlations, heavy tails) — which would *refute* the regime hypothesis and bound the whole S1 motif sub-arc. **The test (cheap, could-say-no):** add a cumulant readout — skewness, kurtosis, Wick-factorization residual (does ⟨ΨΨΨΨ⟩ factor into pairwise ⟨ΨΨ⟩?) — to the *existing* r* GRF-linearization pipeline, sweeping R_cg through the hydrodynamic window. Three clean outcomes: cumulants→0 (Gaussian, upgraded assumed→measured), cumulants persist (Gaussianity false — honesty result), or Gaussian-only-in-a-band (maps the validity window); all three publishable. **The proof (hard):** only route (3) gives forced — needs the scaling dimension of the leading non-Gaussian vertex under the DCGT flow shown irrelevant at ξ_canonical, exactly what the continuum arc measured *not* to hold, so expect failure or a narrow window. **Honest expectation:** best realistic outcome is "Gaussian as a measured, regime-conditional window property" (assumed→measured, not assumed→forced); real probability on the negative (non-Gaussian), which is the more interesting result because it bounds how far the S1 motif statistics can be trusted. First step = the cumulant test on the existing simulator. **DONE 2026-06-26** (`evaluation/CoarseGrain_Arc/grf_cumulant_test.py` + `GRF_Cumulant_Findings.md`) — ran on the certified Σ-substrate (the literal r* GRF pipeline isn't on disk, so tested the dynamical event-density field, the meaningful target; 10 seeds, uniform IC, R_cg sweep). **Result: qualified MILD SUPPORT.** One-point cumulants are weak at fine scale and Gaussianize under coarse-graining (skew 0.26→0.09, excess kurtosis −0.19→~0); a small spatial Wick residual persists (~−5 to −9%, no trend to 0). **Verdict: coarse field APPROXIMATELY Gaussian in the hydrodynamic window with a small persistent spatial residual** — "assumed → measured-as-approximate-window-property" (the predicted best-realistic outcome), and slightly more favorable than the committal/trapping prior expected. NOT clean-Gaussian, NOT refuted. **Scrutiny flag:** the weak non-Gaussianity may be because the uniform-IC field is sparse-deposit-on-smooth-background, under-weighting the committal structure — firm with a deposit-dominated/denser-seed regime + larger lattice + the actual Ψ. Scope: one IC/field/config. **FIRMING DONE 2026-06-26 — OVERTURNS the mild-support reading** (`grf_cumulant_deposit_test.py`): deposit-density sweep shows the base config (seedfrac=0.04) sat **near a skew zero-crossing** (artifact); at sparse seeding (0.01) the field is **strongly skewed (+0.95)**, at dense (0.25) the kurtosis **GROWS under coarse-graining (0.12→0.77, anti-CLT)**. **Corrected verdict: the GRF regime hypothesis is NOT robustly supported** — the coarse field is non-Gaussian across deposit densities and coarse-graining does NOT Gaussianize it (kurtosis grows) — **vindicating the committal/trapping prior** (ED doesn't decorrelate; CLT route blocked, the same wall as PDE-emergence). The "honesty result" #5c flagged: **the S1 motif sub-arc's GRF assumption holds at best in a narrow accidental window, NOT generically** — it should be treated as a bounded, regime-conditional approximation. Lesson: pressure-testing the surprise was load-bearing (the favorable first run didn't survive it). **DECOMPOSITION RE-CHECK 2026-06-26** (`evaluation/CoarseGrain_Arc/grf_gaussian_decomp.py`) — after #2 flipped via the coherent/disorder split, tested whether #5c flips the same way (is the field Gaussian + sparse committal spikes?). **It does NOT flip.** At sparse seeding (isolated filaments = the candidate spikes), removing the filament cells drives skew→0 but **kurtosis→−1** — you never reach Gaussian; under the filaments is a **flat/sub-Gaussian background, NOT a Gaussian.** The field is *flat background + super-Gaussian filaments, neither Gaussian* — **there is no Gaussian signal to recover** (unlike #2's hidden Coulomb). So #5c's "no" is a **real internal finding** (the coarse field is non-Gaussian — the GRF/motif assumption fails), **NOT promoted to an external/official prediction** (per AP, pending understanding — notebook documentation only). **The discriminator (from #2 vs #5c): some CG-failures are measurement errors (a real continuum signal masked by entropy — #2 Maxwell), others are genuine features (no signal there — #5c). #3 (diffusion) is the swing vote.** #5c CONFIRMED non-Gaussian (robust across mild+sparse configs). **DECISIVE TEST 2026-06-26 — route 3, the Fourier-phase test** (`evaluation/CoarseGrain_Arc/fourier_phase_test.py` + `Fourier_Phase_Finding.md`): a GRF is DEFINED by random Fourier phases (amplitude-Gaussianity is a CLT-in-Fourier trap). Built the phase-randomized surrogate (ED's exact power spectrum, randomized phases) = **perfectly Gaussian (skew 0.003, exkurt −0.001)**; ED's actual field strongly non-Gaussian (skew 0.96) → **100% (97% at R_cg=4) of the non-Gaussianity is in the PHASES.** A GRF has random phases (structureless beyond its spectrum); ED has **correlated phases = structure (filaments).** So **"ED non-Gaussian" = "ED builds coherent structure / commits, doesn't mix" — the same statement, the distributional face of the arrow.** #5c CONFIRMED decisively (not flipped) and sharpened to its cleanest form; the right test (cosmology's own non-Gaussianity test). **Gaussianity debt UNPAID at this substrate layer** (CMB-Gaussian bill, if payable, is cosmological-layer, not here); routes 1 (correlation-length) + 2 (log-density) remain as ways a Gaussian *description* might appear at another scale, but the field itself is not a GRF. Atlas connection: ED = structure-making, hyperbolic-pole, phase-correlating substrate; Gaussianity = structureless thermal shadow.

## C. Smaller open threads / sweeps designed but not run

6. **A3 topology / reach / Σ sweep — RUN 2026-07-01.** `evaluation/Bits/examples/a3_topology_sigma_sweep.py` + `evaluation/Bits/docs/A3_TopologySigmaSweep_Results.md`. Swept topology (chain/tree/grid), Σ-coefficient balance (balanced/coherence/strain/gradient-dominated), and reach grading (bridge bandwidth 0.5→0.001, hard-decoupled reference), reusing the certified sim + M1/M2/M3/Delta pipeline unchanged. **Result: perfect severance (M2≈shuffle-floor) holds across every topology and every Σ regime with real signal to sever** (chain/tree strong; grid and gradient-dominated Σ had near-zero baseline signal, so "holds" there is a weak test, not a strong one). **Genuine discovery on the reach axis:** bandwidth is NOT wired into the certified update rule at all (`compute_sigma`/`admissible_neighbors` only read the binary `decoupled` flag) — all six graded-bandwidth configs gave byte-identical, non-severed results; only the hard-decoupled case severs. So reach in the current rule is a strict on/off switch, not graded — an honest finding about current scope, not a bug. **Resolves bits-state angle #3.** **Follow-on RUN 2026-07-01** (`A3b_GradedSigma_Results.md` + `examples/a3b_graded_sigma_experiment.py`, experimental/uncertified variant, certified sim untouched): wired bandwidth into Σ as a genuine multiplicative gain (Σ_graded = bw × Σ_certified) and re-ran the reach sweep. **Severance snaps in even faster than the binary case, not slower** — sharp transition between bw=0.5 and 0.25, then flat (M2 at shuffle-floor) across four further orders of magnitude down to bw=0.0001. Read as a real, deeper structural finding, not a failed experiment: the maximal-Σ winner-take-all selection rule means any consistent relative disadvantage for a channel, however small, produces near-total avoidance once an alternative exists — severance looks like a generic consequence of winner-take-all dynamics, not merely an artifact of the certified rule's binary decoupled flag. **Anomaly CHASED 2026-07-01:** the bw=0.5 near-zero-M1 reading resolved — a larger direct-correlation batch (N=40) shows a real positive correlation at bw=0.5 (+0.42), just markedly weaker than at bw=0.25 (+0.87); the original histogram MI estimator (2 bins) likely undercounted the weaker signal. Working (not fully verified) mechanism: exact ties between bridge and internal Σ give the winner-take-all tie-break more room for early bridge-crossings, decorrelating the two halves more often. Headline finding (severance snaps in sharp, not gradual) stands; the precise tie-break mechanism is the one remaining loose thread.
7. **Cross-architecture determinability theory + the "canonical-observable" hypothesis.** **UPDATED 2026-07-02: now 1 of 4 bits-measurement next-angles open, not 2** — channel-capacity = **closed (A1)**; topology/Σ/reach = **closed (A3/A3b)**; emergent-boundaries = **CLOSED (A2, see item 8)**, not merely "partly addressed" as this line previously said (that phrasing itself overstated an unverified GR-arc borrow; see item 8's correction). The one genuinely open angle: is there a *canonical* observable that makes the scalar Δ intrinsic rather than observable-dependent? This remains pure conceptual territory — no proposed mechanism, no candidate observable, no test design anywhere in the corpus (A1's own "transfer entropy / common-cause decomposition" gesture is a reformulation direction, not a built one).
8. **A2 emergent boundaries — CLOSED 2026-07-02** (`evaluation/Bits/docs/A2_EmergentBoundary_Bridge_Results.md`, `evaluation/Bits/examples/a2_emergent_boundary_bridge.py`). **CORRECTION to the prior entry below:** "now largely REALIZED" (as previously written here) overstated what had actually been verified — pointing at the GR-arc's dynamical-bandwidth rule (b→0 horizon) is not the same as running the Bits determinability-boundary measurement (Δ/M1/M2/capacity) on the resulting emergent surface and confirming it reproduces A1's severance signature; nobody had. That's now done: bridged the GR-arc rule into the certified Bits capacity pipeline (edge bandwidth = min, not mean, of its two endpoints — a first attempt using mean was a real construction bug, caught and fixed, that produced a spurious 0.8-1.1 bit "leak"), and the resulting dynamically-grown horizon reproduces A1's exact-zero channel capacity (near-boundary and far-field readouts both noise-floor, verified via distinct-strata + live-commit-count checks, not just a summary number). First-pass scope (36×36 grid, 60 trials/K, K∈{2,4}, one coupling regime) — not a sweep, but the qualitative result (clean zero vs. a large positive leak) is not a close call. The admissability caveat this arc's foundations note flagged (is a bandwidth-collapse law ED-admissible) is inherited from the GR-III rule's own existing audit, not independently re-litigated here. *(Prior entry, superseded by the above:* "A2 emergent boundaries, the dynamical version — now largely REALIZED. A2 showed decoupling is emergent only under a dynamical-bandwidth-collapse rule on P04 — and that rule was built and run (#1's dynamical rule; the b→0 frozen horizon is the emergent A2 cut, realized by build-and-run). The residual is the same as #1's (3D build, exact profiles). The "shared infrastructure with the GR keystone" prediction held: one rule, both jobs.")
8b. **Close the Gleason-analog inner-product derivation.** Paper_007 (Hilbert-space emergence) is honest that Hilbert space itself is not assumed — it emerges as the Cauchy-completion of the substrate's own motif algebra, GIVEN an inner product. But that inner product is labeled **I (inherited)**, not derived, at Paper_007's own level. Paper_004's attempt to derive it substrate-side (a Gleason-type uniqueness argument) only **partially** closes: the sesquilinear form falls out **conditional on two added postulates** — **P-Channel-Orthogonality** (distinct channels are orthogonal) and **P-Gleason-Compatibility** (probability assignment is non-contextual) — and the paper states plainly that whether those two reduce to the canonical 13 is an **open conjecture**, not shown either way.

    **RECONCILIATION AUDIT RUN 2026-07-02 — this is NOT a bookkeeping fix, and it is NOT a blank page either.** Two independent, non-cross-referenced prior attempts at this exact question were found in the corpus: `arcs/born_gleason/` + `arcs/U2/` + `arcs/U2_continuum/` (2026-04-26, registered as **T10** "Born Rule" and **T11** "Inner-Product/Bell-Tsirelson," both previously marked FORCED-unconditional). Both theorem registrations have now been **corrected/downgraded** (see `theorems/T10.md`, `theorems/T11.md`) after finding: (i) the primitive files those arcs cite have since moved to `archive/quantum_primitives_review/` — a retired, non-equivalent enumeration to the current canonical P02/P04/P07/P09/P11 (old-P11 pre-loaded a Born-like selection weight into the primitive itself, which the current P11 correctly does not); (ii) T10's "unconditional" status directly contradicts the arc's own same-day closing memo, which says "CONDITIONAL on U2"; (iii) T11's channel-orthogonality closure step is circular — it invokes born_gleason's bandwidth-non-contextuality result to license a *different* claim (inner-product diagonality) that result never actually established, the exact conflation Paper_004 was careful to keep separate; (iv) the "apparatus cannot change $b_K(u)$" step in born_gleason Memo 02 resolves operational contextuality by definition, not derivation. Paper_004 (2026-05-13) shows zero awareness either arc exists.

    **Corrected target:** not "derive from primitives," but **rehabilitate the two specific flawed arguments** against the current, narrower primitive set, under real adversarial review (the 2026-05-13 M-series pivot's own discipline, which these same-day-closed April arcs never got): (1) born_gleason Memo 02's non-contextuality argument, fixing the apparatus-loophole-by-definition problem — this is the closest existing draft of **P-Gleason-Compatibility**; (2) U2 Memo 03 §4.4's channel-orthogonality argument, fixing its circular reliance on (1) — this is the closest existing draft of **P-Channel-Orthogonality**, but needs its own independent justification, not a borrowed one. Real, bounded, well-scoped work — but starting from two salvageable drafts with named flaws, not from zero and not already done.

    **REHABILITATION ATTEMPTED 2026-07-02** (`foundations/Gleason_Rehabilitation_Attempt.md`, direct derivation attempt against current P02/P04/P07/P09/P11, not just another scoping pass). **One real partial win, one sharpened negative.** **P-Gleason-Compatibility: partially DERIVED** — a cleaner argument than Memo 02's, using only P02+P04 (bandwidth is defined as a two-argument $(K,u)$ function with no decomposition-slot at all in the current primitives, so a decomposition choice has nowhere to enter the probability ratio), with no dependency on the retired old-P11 wording and no need for Memo 02's definitional-fiat apparatus-loophole move. Honest limit: this closes only the *bookkeeping* reading (relabeling can't change the math), not the physically-loaded Kochen-Specker reading (a genuinely different measurement context changing the probability) — that half stays open, named precisely rather than glossed over. **P-Channel-Orthogonality: three independent derivation routes tried (P04 additivity, P07 distinctness, P11 phase-randomization) and all three fail or apply to the wrong layer** — confirming Paper_004's own diagnosis independently rather than just citing it. **Sharper reframe found:** the real question isn't "prove $\langle K|L\rangle=0$ inside an already-free vector space" (true by construction there) but "is the free/orthogonal representation physically correct at all." **A candidate regime-bound was then tried and WITHDRAWN, same session, a few hours later:** the corpus's own sublinear-composition rule ($c_{12}$, `theory/visibility_to_bandwidth.md`) looked at first like it might show cross-channel coupling vanishing in the same regime the Born/Gleason chain needs — checked directly and found not to hold, on two counts: $c_{12}$ is identified with interferometric *visibility* in the concrete KDTLI model, which is large (not small) for thin-participation systems (backwards from what was claimed), and more fundamentally $c_{12}$ (detector-position interference visibility) is probably not even the same object as $\langle K|L\rangle$ (channel-basis orthogonality) — conflating them repeats the exact Memo 03 §4.4 error this whole audit exists to catch. **Net: 8b is now better understood, not fully closed** — one real partial derivation banked (P-Gleason-Compatibility, bookkeeping sense), one clean structural negative (P-Channel-Orthogonality, three routes fail), and one attempted regime-bound checked and correctly discarded rather than left as an unverified claim. Paper_004/Paper_007 updated to reflect the (corrected) state.

    **RECONSTRUCTED 2026-07-08 — the blocking postulate is discharged at reconstruction/account tier** (`foundations/Gleason_Complementarity_Reframe_Scoping.md` + `foundations/Gleason_Soler_LatticeVerification.md`). The three routes all failed because they hunted orthogonality as a *kinematic metric fact inside an assumed inner product*; ED has no prior inner product. Reframe: **Move 1** — define the inner product *operationally* from P11 commitment/Born statistics, then orthogonality = perfect operational distinguishability. Built (`evaluation/ChiralGauge/move1_operational_orthogonality.py`): (A) distinct ED channels are perfectly distinguishable (`p(K|K)=1, p(K|L)=0`, from P07+P11+Born, metric-free); (B) perfect distinguishability ⟺ orthogonality (best confusion-free detection `= 1−c²`, `=1` iff `c=0`). So `⟨K|L⟩=0` is **forced by the commitment frequencies, given the representation**. **Move 2** — the inner-product *form* is forced by the P04-vs-P09 complementarity (the non-Boolean gate PASSES, `gleason_nonboolean_probe.py`: a relative phase requires shared participation, so which-channel and phase measurements cannot be jointly sharp) via Piron–Solèr; the ℂ field is selected (P09 phase rules out ℝ; V5/063 composites rule out ℍ) → **T14 upgraded to account**. Covering law grounded (P11 irreversibility *enforces* first-kind repeatability); the preferred-basis question resolved as **primitive einselection** (ED commits only in the channel basis; basis-democracy emergent); physical (Kochen–Specker) non-contextuality resolved (intrinsic `b_K` + einselection + stochastic commitment). Both of Paper_004's postulates handled. **Honest tier: a reconstruction with grounded ingredients + account-tier steps, NOT a closed theorem.**

    **CIRCULARITY-AUDITED 2026-07-10 — the flag clears** (`foundations/Gleason_CircularityAudit_2026-07-10.md`). The 2026-07-08 work left a circularity flag on Move 1 (theorem proven *within* the representation) and on the Solèr angle condition (equal-norm presupposes the inner product). Audit verdict: **non-circular**, once two notions of orthogonality are separated — **lattice-`⟂`** (`K ≤ L'` via orthocomplementation, grounded in P11 exclusivity, **metric-free**, the INPUT to Piron–Solèr) vs **metric-`⟂`** (`⟨K|L⟩=0`, the forced OUTPUT). Solèr's angle condition is fed metric-free (infinite ← P03; orthogonal ← P11 lattice-exclusivity; equal-norm ← P03 equal-bandwidth → equal Born weight, an operational frequency not the metric norm). Move 1 is a consistency bridge, not a bootstrap. **The residual is correctly re-scoped: NOT orthogonality (discharged, non-circular) but Solèr lattice rigor** — the covering-law exchange property, the **metric-free establishment of the full orthomodular identity** (the honest soft spot: the primitives give non-distributivity; whether they give the full orthomodular identity without the metric is the tightest open screw), infinite-dim/`*`-ring rigor — plus the observability of the two distinctive predictions (primitive einselection; emergent multi-context). **Net: #8b's blocking postulate is discharged and audited; the keystone's open core is now lattice-theoretic rigor + a falsifier frontier, not the orthogonality postulate.**

## D. Flagged — deliberately NOT to chase (crank / base-rate traps)

9. **The constants frontier (19 free parameters).** Verdict: **THINK, don't CHASE — and now CONFIRMED, not just asserted.** By A1 + Facts, values are inherited-in-principle (no intrinsic scalar). Arc FSC (`arcs/arc-FSC/`, **CLOSED 2026-05-25**, found uncited + cross-referenced into this doc + the position paper 2026-07-05) actually ran the chase for a substrate-level fine-structure-constant analogue: three independent routes (topological loop-holonomy quantization via P09, V1 cross-overlap regularization, one-loop substrate-RG fixed points for the marginal sector) were each closed negatively (P09's U(1) is continuous not compact under primitive dynamics; the V1 integral is regulator-shape-dependent/INHERITED; the marginal RG flow has no IR-attractive fixed point). One real positive side-finding survives: a genuine substrate-RG universal (Perron-Frobenius fixed-point ratio on the P04 band-allocation transition matrix), joining the Arc RG 0.6 ratio and V5 cross-scale invariance in the closed-arc universal inventory — but it does not bridge to QED's alpha. Don't grind on alpha further; the negative result is now earned by direct primitive-level test, not merely assumed.
10. **`Θ_ED ≈ 10⁻¹²²` (Λ-smallness).** **SHARPENED 2026-07-06, promoted from "flagged open item" to "substrate constant."** Scoping pass (`docs/Scoping_ThetaED_FirstPrinciples_2026-07-06.md`) found: (a) a genuine citation bug — Θ_ED was mis-attributed corpus-wide to "Paper_087 P12 (ED-threshold)," but canonical P12 is a stability-landscape functional with no threshold content; the real (unverified) origin is `Paper_ED_CCC_ConformalCyclicCosmology.md` §3.2/§3.7 — corrected with dated banners across ~11 files in each repo, 2026-07-06; (b) no move in ED's demonstrated toolkit (counting, geometric/holographic, RG-fixed-point, topological-quantization) produces or suppresses across many orders of magnitude, let alone 122 — every successful ED derivation of a number is an O(1) coefficient riding on an already-inherited scale; (c) one direct attempt exists and failed (`Memo_ED_DCGT_LambdaSuppression.md`, Path-L-1: standard hydrodynamic coarse-graining gives only polynomial corrections). **Conclusion: this isn't just a deprioritized target, it's the same tier as `ℓ_P, c, ħ`** — Paper_027 already calls those three "substrate constants," explicitly separate from the 13 structural primitives (P01-P13): inherited numbers the primitive *structure* needs but never fixes. Θ_ED belongs in that same bucket, for the same reason A1 gives (the only observer-independent substrate quantity is zero; a global cosmological-scale number is "like asking one molecule for the temperature" — not something local coarse-graining reaches). It does **not** need or deserve a P14 primitive slot — that would be a category error (P14 is for new *structural* postulates, Θ_ED is a *number*). Route A4 uses `Θ_ED` to source `H₀`/`ξ_canonical`, so it's the one inherited constant the whole SCBU arc leans on — same standing as ℓ_P, c, ħ leaning under Paper_027's `G`.

## E. Organizing / writing threads (not research)

11. **The post-Facts spine** — "form yes / flesh no, proved six times, named once." Organizing frame for the map, not a book. Live.
12. **The information-subtraction lens** — good organizing lens; needs *one derived rung* (not asserted) to graduate from "filing system" to "program." Open direction, low priority.
13. **CCC ↔ ED resonance (Penrose)** — cosmic symmetry at both relational-structure-poor endpoints; possible corpus paper. Hanging idea.
14. **Outreach (distribution).** The binding constraint on the program's *goal* (being seen/read), distinct from the research above. Drafts banked in `ED Generative/internal notes/ED_Outreach_Drafts.md` (French — declined graciously; Lincoln, Clauset, Balaguer — ready; Tong — **shelved**, the chiral gap #2b means there's no honest answer to his N–N objection yet). Not research; tracked here so it isn't lost. **ED Interpretation Chronicle** (`Desktop/ED Important/` — catalog + main, ~28 science-news articles reframed through ED): an *accessible outreach asset*, but two honest caveats before it ships (flagged 2026-06-28, to discuss): (i) it leans hard on **geometry/manifold/"field"/curvature** language as if primitive — the very shadow-as-primitive leak the ontology rail warns against ([[feedback_field_is_emergent_not_primitive]]); (ii) **every entry scores 4.3–5.0** with no "doesn't fit" — the unfalsifiable-consilience pattern (find-whatever-you-look-for), evidentially weak and crank-adjacent. Useful for reach; needs an edge (an article ED *can't* reframe, or a real prediction) to be evidence, not just interpretation.

---

## The one-line read

The **closed column is large, clean, and honestly labeled** (A1, B4-skeleton, Primes, the CoarseGrain trilogy, Bits, Q-Compute, Soft-matter, Black-hole, Entanglement, ED-10/Information-structure, SCBU-to-M3 — all verified 2026-06-23 — plus the gravity quartet GR-I/II + KM-I/II and the One-Field letter). The **GR structural arc is now complete too** (R1–R12 + the dynamical rule built-and-run; horizon thermo delivered).

The **real open work has shifted again (2026-06).** The GR keystone (#1) is now **CLOSED** — α₁/α₂/PPN done by GR-IV (α₂=0 exact, α₁ safe ≥70 orders, sparsity measured); only bookkeeping remains, and #1 no longer gates the B-column it used to (e.g. #4 unitary completeness is unblocked). So the genuine open *research* is now: **#2b the chiral-gauge/parity gap** (the sharpest discreteness challenge; "open and promising," reduced to the T4 spinor gate — a real derivation), the **prediction inventory** (the north-star build — find ED's falsifiable experiment-facing predictions), and finishing **3c** (the layers paper — a voice pass, not research). **Charge→Maxwell (#2)** and **diffusion (#3)** remain Wall-1 probes; SCBU/MOND (#5/#5b) are M3 *tightening*.

**If pointing energy at one thing:** for *research* that peaks the passion, **#2b (chiral gauge)** is the live hard front (a genuine derivation via T4, not a tidy-up). For the *goal* (being undeniable), the **prediction inventory** is the highest-leverage build — it's the weapon, distinct from the targets that feed it. **#1 is no longer where the energy goes — it's done.**
