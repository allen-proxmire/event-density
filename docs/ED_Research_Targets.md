# ED Research Targets — Live Open/Closed Map

*The current state of the open and closed work. Snapshot maintained by hand; update as arcs close. Pairs with `NEW_SESSION_ONBOARDING.md`. The one-line read is at the bottom.*

---

## A. The big open research targets (real work, not tightening)

1. **Phase-3 GR → Einstein closure — the "big E."** **Status refined 2026-06-13 (read from the foundations docs, not the older framing).** The R1–R12 structural arc is *complete* and published as GR-I/GR-II: field-equation form forced (Lovelock + mode count → **khronometric**, not pure Einstein). The "flux ↔ Levi-Civita covariance" hinge that the older note called open is **DONE** — R5 forced it (vector covariance via P05+P04 typing → band↔edge map forced), R6 reduced rank-2 conservation to `∇·T=ρa^ν ⟺ geodesic motion`, R7/R8 ran the Einstein/Nordström fork and it landed **Einstein** (anisotropic metric, factor-of-two bending 2.09). The genuinely-open derivation-side items are now narrower:
   - **(a) Timelike geodesic identity** — R7 proved the *null* half (Fermat); the *timelike/massive* half was assumed. **Advanced 2026-06-13** (`foundations/Phase3_GR_TimelikeGeodesicIdentity.md`): reduced to the massive eikonal of the proven null result — the Jacobi functional `J(b)=b⁻¹√(E²−m²b)` is *limit-forced* (→ Fermat at m→0, → Maupertuis/Newton in weak field); residual = the explicit Σ-rule massive-front computation (simulable). Removes the "timelike geodesic assumed" caveat all four gravity papers carry.
   - **(b) `α=1`** (P-Commitment-Linear) — the postulate selecting the Einstein branch. **DONE 2026-06-13** (`foundations/Phase3_GR_AlphaOne_CommitmentLinear.md`): **forced** from the P04 band law — `α=1−β` with `reserve~b_int^β`; the commitment-reserve is a distinct, *monotone-draining* band (no replenishment, P11/R2§5) set by commitment history, not the ambient internal band, so `β=0` (`α=1`, Einstein); the Nordström endpoint `β=1` needs reserve↔`b_int` replenishment, which P11 irreversibility forbids → **Nordström positively excluded**. The arrow (P11) selects Einstein at the lapse — same commitment that forces the khronon. R8's load-bearing assumption → derived; residual is sub-leading (reserve profile, needs full `F`; can't flip the branch) + the band-accounting premise. Could-say-no, came back Einstein.
   - **(c) The explicit `F` / dynamical-bandwidth-collapse rule on P04** — the #1/#8 *shared* keystone (simulation/construction). **FIRST BUILD DONE 2026-06-13** (`evaluation/DynamicalBandwidth/dynamical_bandwidth.py`, `foundations/Phase3_GR_DynamicalRule_FirstBuild.md`): the forced rule `ḃ = D∇²b − κρ` (P02 adjacency-sharing − P11 matter-concentration sink, monotone-draining reserve, *not* tuned to Poisson) — steady state = Newtonian field eq `∇²b~ρ` (corr 0.999, the forced terms' fixed point); deficit **linear in source** (Schwarzschild `r_s∝M`, emergent); strong coupling → **finite-radius `b→0` horizon** (`g_rr→∞`) that is a **frozen A2 cut** (reserve exhausted) — **realizing dynamically the R4§6 identity (A2 cut = metric horizon = `b→0`), so #8 is now realized by build-and-run.** **HYPERBOLIC BUILD DONE 2026-06-13** (`evaluation/DynamicalBandwidth/hyperbolic_modes.py`, `foundations/Phase3_GR_DynamicalRule_HyperbolicBuild.md`): the retarded single-transport `F` carries **2 tensor + 1 scalar** modes (khronon physical because the arrow P11/P13 breaks the time-gauge — R10's count, now *from `F`*); tensor at `c` (single P05 cone). Khronon speed measured `c_s/c = √(1+ε)` exactly, `ε` a foliation-specific kinetic term: **the minimal forced rule (`ε=0`) gives `c_s = c`** — khronon at light speed, **resolving KM-II §6 toward maximal predictivity** (sharper than generic khronometric). `α₁,α₂` now **localized to the single knob `ε`** (numbers still deferred). **`ε` DERIVED = 0, DONE 2026-06-13** (`foundations/Phase3_GR_DerivingEpsilon_KhrononSpeed.md`): the only candidate for a foliation-specific `λθ²` kinetic term (`ε≠0`, a second cone) is the commitment-reserve sector, which **P11 makes one-way / dissipative** → it contributes **damping** (imaginary `ω`; confirmed numerically — the reserve damps + overdamps the khronon without shifting its cone), NOT a conservative kinetic term. Forced rule (P05 transport + P11 drain) has no `λθ²`; minimality forbids adding one → **`ε=0`, `c_s = c`: ED's scalar GW polarization is at light speed**, damped near matter / clean in vacuum (obs-consistent). **KM-II §6 resolved as a derivation, maximal-predictivity horn.** The arrow's THIRD Einstein/khronometric selection (with `α=1` lapse + the mode count). **`α₁,α₂` front — STRUCTURAL ANALYSIS DONE 2026-06-13** (`foundations/Phase3_GR_PreferredFrame_Alpha12.md`): ED is driven into the maximally-suppressed corner by **five** independent derived mechanisms (universal-not-differential LV → α₁,α₂ sub-leading, GR-II; `c_T=c`; `c_s=c` via `ε=0` = most-suppressed khronometric regime; metric-only matter coupling removing the direct khronon–matter α₁ source, KM-I; and the ED-specific **dissipative near-field** — the overdamped khronon near matter can't build the frame-dependent response α₁,α₂ measure, P11 again). Strong lean to PPN-safety — **but the VALUES are NOT computed and deliberately NOT fabricated**: closing the front needs `κ,D` pinned + the Foster–Jacobson/BPS khronometric PPN formulas, or a direct PPN expansion of `F`. **The falsification front stays formally OPEN as a number** (the lean ≠ the number); the remaining computation is now definite and foliation-knob-free. Still also open: sub-leading reserve speed-renorm, 3D `1/r`, pin `κ,D`, B-column #4 ED-10 numbers (`S=A/4`, Hawking `T`).
   Wall 1, attainable-in-principle. Still the keystone several B-column items depend on.
2. **Charge → Maxwell — the hard unique-signature target (#2).** Open. B4's reframe set the question: does the **DCGT continuum limit of the holonomies** yield Maxwell/Coulomb? No Maxwell derivation claimed. Wall 1, open.
3. **Diffusion / continuum, the one untested route.** Does a *different* ED model (the Q-C / UDM diffusion, posited at the continuum) actually produce diffusion? The certified Σ-substrate provably can't (the CoarseGrain trilogy: contraction not mixing). Caveat baked in: it may be diffusive *by construction*, in which case it's consistent, not deep. Honest framing: "diffusion lives a model/layer up, at the named kinetic→hydrodynamic rung." **Untested.**

## B. Open consequences of the big targets (mostly numbers waiting on #1)

4. **ED-10 horizons + ED-Information.** Structure and scalings are done **EFE-free**; the actual numbers (e.g. `S = A/4`, Hawking `T`) wait on the Phase-3 GR keystone. Unitary completeness is open (Page-curve *structure* reproduced; full unitarity not).
5. **SCBU.** M3-closed; what's left is **tightening only**. The one live EFE-free derivation is the per-projection exponents (the ED-10 R3 scaling method is the tool). No sim/rerun needed.

## C. Smaller open threads / sweeps designed but not run

6. **A3 topology / reach / Σ sweep.** A behavior-space plan exists (a topology-class factory on the ParticipationGraph API), invariants held; **not run**. Resolves bits-state angle #3.
7. **Cross-architecture determinability theory + the "canonical-observable" hypothesis.** Two of the four bits-measurement next-angles still open (the scalar Δ is observable-dependent → is there a *canonical* observable that makes it intrinsic?). Channel-capacity angle is **closed (A1)**; emergent-boundaries angle is **partly addressed (A2)**.
8. **A2 emergent boundaries, the dynamical version.** A2 showed decoupling is emergent *only* under extended rules (a dynamical-bandwidth-collapse rule on P04 — **the same extension GR #1 needs**). Building/running that rule is open, and it's **shared infrastructure with the GR keystone**.

## D. Flagged — deliberately NOT to chase (crank / base-rate traps)

9. **The constants frontier (19 free parameters).** Verdict: **THINK, don't CHASE.** By A1 + Facts, values are inherited-in-principle (no intrinsic scalar). Don't grind on it.
10. **`Θ_ED ≈ 10⁻¹²²` (Λ-smallness).** Same posture: **think-don't-chase, with a reason.** Value-inherited in principle; not a derivation target.

## E. Organizing / writing threads (not research)

11. **The post-Facts spine** — the "form yes / flesh no, proved six times, named once" through-line. Organizing frame for the map, not a book. Live.
12. **The information-subtraction lens** — good organizing lens; would need *one derived rung* (not asserted) to graduate from "filing system" to "program." Open direction, low priority.
13. **CCC ↔ ED resonance (Penrose)** — a spawn-task was flagged for a possible corpus paper (cosmic symmetry at both relational-structure-poor endpoints). Hanging idea, unstarted.
14. **Video one-pagers** — internalization scaffold for the post-Facts arcs (the "haven't sunk in yet" problem). Exists as a set; ongoing personal-organization use.

---

## The one-line read

The **closed column is large and clean** (A1, B4, Primes, the whole CoarseGrain trilogy, Bits, SCBU-to-M3, the two Facts OQs, A2-static — plus the post-pivot gravity quartet GR-I/II + KM-I/II and the One-Field letter) — and all of it is committed and pushed.

The **real open work narrows to three things, and they're entangled:** GR closure (#1) is the keystone, it shares its enabling extension with A2-dynamical (#8), and it unlocks the numbers in ED-10 / Information / SCBU (#4–5). **Charge→Maxwell (#2)** and **diffusion-in-a-different-model (#3)** are the two independent Wall-1 probes still standing. Everything else is either a sweep scoped-but-not-run (#6–7), a deliberate don't-chase (#9–10), or organizing (#11–14).

**If pointing energy at one thing: #1 / #8 is highest-leverage** — the admissible dynamical-bandwidth extension on P04 turns the most other cards face-up.
